# HANDOFF — U+유모바일 S-02 요금제 선택

> 목적: 새 AI 세션이 프로젝트를 빠르게 이해하고 안전하게 이어서 작업하기 위한 진입 문서입니다.
> 상세 지식은 `docs/` 하위 문서를 참조합니다. 작업 규칙은 `CLAUDE.md`에서 시작합니다.

## 1. 프로젝트

| 항목 | 값 |
|---|---|
| 대상 | U+유모바일 가입신청서 요금제 선택 화면 `S-02` |
| 목적 | S-02 UX 개선 |
| 대표 산출물 | `index.html` (단일 HTML 통합 리포트) |
| 배포 | https://jiwonida-dotcom.github.io/260730planlist_3/ |
| 레포 | https://github.com/jiwonida-dotcom/260730planlist_3 (`main`) |
| 파일 규모 | **486KB · 3,398줄** |
| 기준 커밋 | **`aa654f0`** (2026-08-14) |
| 원본 피그마 | https://www.figma.com/design/SRasUqBc8lp9g0VT5mvjCu/2026.03-가입신청서?node-id=40005698-6576 |
| 피그마 file key | `SRasUqBc8lp9g0VT5mvjCu` / 화면설계서 캔버스 `40005698:6576` |
| 정량 근거 | Microsoft Clarity 2026-06-29 ~ 2026-07-29 (30일) |
| 작성자 | 허지원 책임 |

기술 사양 — 단일 HTML, 외부 리소스·CDN 의존 없음, 인라인 CSS + Vanilla JS, **브라우저 저장소 미사용**(상태는 메모리만).

## 2. 현재 산출물 구조

`index.html`은 **6개 리포트 탭**으로 구성됩니다. `R_TABS = ['spec','proto','func','req','plan','tc']` 순서가 화면 노출 순서입니다.

| 탭 | section id | 시작 줄 | 내용 |
|---|---|---:|---|
| **01 화면설계** | `rt-spec` | **1456** | **← 주 작업 대상.** S-02 화면설계 (영역 명세 · 상태 정의 · 접근성 · 데이터 매핑) |
| 02 프로토타입 | `rt-proto` | 882 | 동작 프로토타입 + 시나리오 시뮬레이터 |
| 03 기능정의 | `rt-func` | 1121 | 화면/URL 정의 · 검증 시퀀스 · 예외 처리 · 데이터 연동 |
| 04 요구사항 정의서 | `rt-req` | 1298 | 문제 정의(P1~P5) · 개선 원칙 · 공통 기준 · 측정 기준 |
| 05 개선안 | `rt-plan` | 931 | 개선 배경(데이터 근거) · AS-IS/TO-BE · 화면 개선 |
| 06 테스트 케이스 | `rt-tc` | 1406 | 검증 케이스 92건 + 수행 기록 시트 링크 |

> 탭 번호와 섹션 id가 어긋나 있습니다(`rt-spec`이 01번). **id는 바꾸지 않습니다** — 앵커·CSS가 전부 id 기준입니다.

### 01 화면설계 탭 내부 구조

```
문서정보  #fs-sub-info
 ├ 0 문서 사용 안내 (.r-sec)        문서 단계 · 구성 라벨 표 · 표기 규칙 표 · 코드 칩 사용법
 └ PAGE INFORMATION  article#fs-info   기본 정보(코드/타이틀/URL/진입/아웃) + HISTORY

화면설계  #fs-sub-design
 ├ 변경 이력          details#fs-log    기본 접힘 · 검색 + 20건 단위 더보기
 ├ 화면 구성 · INDEX   details#fs-index  기본 펼침 · A~G 인덱스
 ├ 전체 화면 구성      details#fs-full   기본 접힘 · 모바일 3종 + PC 기본
 └ A~G 영역 프레임     article#fs-a ~ #fs-g

부록      #fs-sub-appendix
 1 레이아웃 규격(모바일/PC 대조) · 2 상태 정의 · 3 노출·운영 규칙 · 4 접근성 명세 · 5 데이터 매핑
```

### 06 테스트 케이스 (92건)
기능 25 · 예외 5 · 화면 41 · 접근성 13 · PC 8 / 우선순위 High 55건 / 프로토타입으로 지금 확인 가능 76건.
**정의는 이 문서, 수행 기록은 외부 시트**가 담당합니다. 명세가 바뀌면 이 탭을 먼저 고치고 시트 A~I열을 갱신하며, 수행 기록(J~M열)은 덮어쓰지 않습니다.

## 3. 현재 작업 범위

| | |
|---|---|
| 대상 | `index.html`의 `<section id="rt-spec">` = 01 화면설계 탭 — **1456 ~ 3398줄**(파일 끝) |
| 전용 CSS | `/* ===== 05 화면설계 — 피그마 프레임 형식 ===== */` **486 ~ 575줄**, `코드 칩 호버 상세` **576 ~ 590줄** |
| 전용 JS | `fsLogRows` · `fsLogNorm` · `fsLogPaint` · `fsLogFind` · `fsLogMore` · `fsLogHead` · `fsSyncSticky` · `fsToolbarPassed` · `fsPinToolbar` · `fsSub` · `fsCurTab` · `fsSpy` · `fsSpyQueue` · `fsTipInit` · `fsTipBuild` · `fsTipShow` · `fsTipHide` · `fsTipGoto` · `fsTipBack` · `fsTipCodes` |
| 손대지 않는 곳 | 02~06 탭, 헤더/푸터, 프로토타입 엔진 |

마크업 규약은 `docs/MARKUP.md`, 금지사항은 `docs/RULES.md`를 따릅니다.

## 4. 현재 상태

- **C-01~C-47**: 확정 47건 — **41건은 「05 개선안」 탭**, **6건(C-28 · C-32 · C-36 · C-41 · C-42 · C-43)은 「04 요구사항 정의서」 탭**에 정의
- **결정 대기 0건** — O-17 · O-19 · O-20 해소 완료, **O-18만 이월**
- **P1~P5 전건 해소**
- 01 화면설계 · 03 기능정의 · 06 테스트 케이스는 **참조만** 하고 정의를 두지 않습니다
- **47건 전문은 `docs/REQUIREMENTS.md` 3절**에 있습니다 (원본은 `index.html`, 불일치 시 HTML 우선)

### 화면설계 영역 (피그마 PAGE INDEX 기준)

| | 영역 | 고정 | PAGE CODE |
|---|---|---|---|
| A | 헤더 | 상단 고정 | S-02_A |
| B | 타이틀 | 스크롤 | S-02_B |
| C | TAB | 상단 STICKY | S-02_C |
| D | 목록 컨트롤 (필터 패널 포함) | 스크롤 | S-02_D |
| E | 요금제 목록 카드 | 스크롤 | S-02_E |
| F | FOOT STICKY (Worning/Error Message · 선택 요금제 정보 · CTA) | 하단 고정 | S-02_F |
| G | Modal Bottom Sheet (비교 담기 · 비교 시트) | Modal | S-02_G |

**PC 상태 규칙** — PC 상태는 각 영역의 **마지막 상태 블록**에 두고, 모바일과 차이가 있는 영역(A·B·D·E·F·G)에만 둡니다. 차이가 없는 C.TAB은 모바일 명세에 "PC도 동일"로 표기합니다.

**PC 레이아웃 전제** — PC 신청서는 전체 페이지가 아니라 **오버레이 위 중앙 풀 팝업**입니다. 원 서비스는 1000px 캔버스에 `zoom:0.7`로 700px 표시하므로, 설계서는 **실렌더 값(프레임 700 / 콘텐츠 644 / 목록 2열)**을 기준값으로 씁니다.

**브레이크포인트(C-46)** — **769px 이상 = PC 레이아웃** 단일 기준. 목록 2열 · 필터 조건 2열 · 비교 중앙 모달이 같은 지점에서 동시 전환됩니다. PC는 700px 고정 팝업이라 추가 분기를 두지 않습니다.
> 이것은 **설계 대상 화면(S-02)**의 브레이크포인트입니다. 리포트 문서 자체의 좌우↔세로 배치 전환 기준인 **1180px과는 별개**입니다.

### 현재 물량 (실측)

| 항목 | 수 |
|---|---:|
| 프레임 `.fs-frame` | 11 |
| 상태 블록 `.fs-state` | 23 |
| 요소 명세 행 `.fs-row` | 76 |
| 번호 콜아웃 `.fs-cal` | 63 |
| 영역 마커 `.fs-am` | 24 |
| 접이식 `<details>` | 3 |

## 5. 이월 항목

| 코드 | 내용 | 담당 | 상태 |
|---|---|---|---|
| O-18 | E. 카드 정보 밀도 — 카드 높이 약 300px로 첫 화면에 1~2개만 노출. 스펙 면·혜택 면 압축으로 240px대까지 줄이면 2개 노출 가능하나 정보량 축소 범위 미정. 프로토타입 실측 **혜택 없음 298px / 혜택 있음 353px** | 기획 · 디자인 | **이월** — 화면설계는 목업 단계 기준 유지, 압축 범위는 디자인 시안 단계에서 검토 |

해소된 항목은 `docs/DECISIONS.md` 3절에 이력으로 남겨 두었습니다.

## 6. 가장 중요한 미처리 사항

1. **`C-26` 추적성 태그 누락** — 선택 상태 3중 단서 요건을 내용상 반영하고 있으나 **01 화면설계(`rt-spec`)에 코드 태그가 없습니다**. `C-41`은 반대로 정의 탭(04) 외 05에도 표기. `docs/REQUIREMENTS.md` 4절 실측 근거 참조.
2. **표기 교정 정책 미문서화** — `Hisotry.back()`→`History.back()`, `NO ACION`→`NO ACTION`은 교정했으나 `Worning` · `LABLE` · `Abled`는 원문 유지.
3. **1180px 경계 표기 정합** — 리포트 문서 레이아웃 기준. 문서는 "1180px 미만에서 세로 배치", 구현은 `max-width:1180px`(1180px 자체가 세로).
4. **A~G 프레임 접이식 적용 여부** — 현재 항상 펼침. 프레임 7개 × 상태 23개라 검토 대상.
5. **피그마 원본 오타/오류 미조치** — `docs/NEXT_ACTIONS.md` 참조.

## 7. 커밋 · 배포

레포 루트에서 **`push.cmd`** 실행 → `push.ps1`이 잠금 파일 정리 → `git add -A` → 커밋 → push → GitHub Pages 자동 배포(1~2분).

```
push.cmd                          기본 메시지로 커밋 후 push
.\push.ps1 "docs(05): 변경 내용"    메시지 지정
```

커밋 메시지 형식 — `docs(NN):` / `fix(NN):` / `feat(NN):` 접두어(NN = 탭 번호) + 변경 항목.
브라우저 캐시로 이전 버전이 보이면 URL 뒤에 `?v=2`.

`push.ps1`이 `git add -A`를 쓰므로 **작업 폴더에 임시 파일을 남기지 마세요.** 제외 대상은 `.gitignore`에 정의되어 있습니다.

## 8. 새 작업 지시

> `index.html`의 **01 화면설계 탭(`<section id="rt-spec">`, 1456~3398줄)**을 이어서 작업한다.
> `docs/RULES.md`(문서 성격 · 구현 제약)와 `docs/MARKUP.md`(마크업 규약)를 반드시 지키고,
> 수정 후 `docs/VALIDATION.md`의 6폭 × 3구성 검증을 실행한 뒤 결과를 보고한다.
> 파일은 직접 수정하고, 완료 후 수정된 `index.html`을 전달한다.

**오늘 할 일:**
> [사용자 요청 입력]
