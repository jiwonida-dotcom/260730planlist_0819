# HANDOFF — U+유모바일 S-02 요금제 선택

> **새 세션 진입점.** 이 문서는 짧게 유지합니다 — **지금 할 일**과 **진행 중 맥락**만 담습니다.
> 수치(규모 · 확정 건수 · 테스트 케이스 · 물량)는 여기에 적지 않습니다. `docs/STATUS.md`가 단일 출처입니다.
>
> 2026-08-20 개편 — 종전 HANDOFF 는 상태 수치를 손으로 적어 두 세대 뒤처져 있었습니다
> (59건/106건/구 탭 번호/구 줄 번호). 낡은 진입 문서는 없는 것보다 나쁩니다.

## 0. 먼저 이 한 줄

```
node tools/check.mjs --status        # 레포 루트에서 실행 (cd 필요)
```

HEAD · 파일 규모 · 확정 건수 · 물량을 실측해 `docs/STATUS.md`에 씁니다. **그 값을 씁니다.**
작업 규칙은 `CLAUDE.md`에서 시작합니다.

사람이 손으로 돌릴 때는 레포 루트의 **`check.cmd`** 를 더블클릭해도 됩니다(`push.cmd` 와 같은 방식).
인자 없이 실행하면 `--status`, 레이아웃 18조합까지 보려면 `check.cmd --full` — 단 `--full` 은
`playwright` 가 있는 환경에서만 동작하고, 없으면 정적 검사만 하고 그 사실을 출력합니다.

## 1. 지금 할 일

`docs/NEXT_ACTIONS.md` 우선순위 1 부터 순서대로. 최상위 두 건만 옮겨 적으면 —

1. **수행 기록 시트의 테스트 케이스 번호 갱신** — 05 테스트 케이스에 4장 「레이어 · 팝업」
   (`TC-L-01 ~ 17`)을 신설하며 이관·폐지한 번호를 외부 시트 A~I열에 반영. 수행 기록(J~M열)은 덮어쓰지 않습니다.
2. **가입신청서 작성 내용 확인 화면 인계** — `C-62`로 제휴카드가 그 화면으로 넘어갔습니다.
   **S-02 범위 밖**이므로 넘길 내용(진입 조건 `C-61` · 레이어 구성 `C-60` · 표기 원칙 `C-52` · 결과 행 구성)을 정리해 인계.

외부 확인이 필요해 막혀 있는 항목은 `docs/OPEN_QUESTIONS.md`에 모여 있습니다 (카피 확정 · 원본 부호 · 피그마 원본).

## 2. 진행 중 맥락 — 최근 확정

**2026-08-19 제휴카드가 이 화면을 떠났습니다 (`C-62`)**
제휴카드 선택 일체를 **가입신청서 작성 내용 확인(마지막 단계)**으로 이관했습니다.
S-02 하단 고정 영역은 **2행 고정**이며 제휴카드 요소가 없습니다.
04 요구사항 정의서 7장에 **「바」 S-02 범위 밖 그룹**을 두어 `C-52` · `C-60` · `C-61` · `C-62`를 모았습니다.
→ 이 4건은 **02 화면설계 · 05 테스트 케이스 적용 대상이 아닙니다.**

**`C-63`** — NPay 네이버인증은 입력 폼이 아니라 **외부 인증 호출**입니다 (원본 93p 오독 정정).
**`C-65`** — 약정 안내 팝업 2종(약정형 CONTRACT · 더알뜰플랜2 CONTRACT2) 원문 반영.
**`C-66`** — 타이포 · 간격 스케일 정리, 레이어 높이를 뷰포트가 아니라 **프레임**에 묶음.

**2026-08-19 문서 구조** — 04 개선안 + 05 요구사항 정의서를 **「04 요구사항 정의서」 한 탭으로 통합**하고
변경 이력을 1뎁스 탭으로 분리했습니다. 확정 사항 전건이 한 탭(`rt-req`)에 있습니다.
`rt-plan`은 제거되었습니다. **섹션 id 는 바꾸지 않습니다** — 앵커 · CSS · 코드 칩 이동이 전부 id 기준입니다.

**2026-08-20 프로세스 개편** — 검증 자동화(`tools/check.mjs`) · 실측 단일 출처(`docs/STATUS.md`) ·
⓪ 범위 게이트 신설 · 코드 번호 불변 규칙. 배경과 근거는 `docs/PROCESS_REVIEW.md`.

## 3. 프로젝트 기본

| 항목 | 값 |
|---|---|
| 대상 | U+유모바일 가입신청서 요금제 선택 화면 `S-02` |
| 목적 | S-02 UX 개선 |
| 대표 산출물 | `index.html` (단일 HTML 통합 리포트 + 동작 프로토타입) |
| 배포 | https://jiwonida-dotcom.github.io/260730planlist_0819/ |
| 레포 | https://github.com/jiwonida-dotcom/260730planlist_0819 (`main`) |
| 원본 피그마 | https://www.figma.com/design/SRasUqBc8lp9g0VT5mvjCu/2026.03-가입신청서?node-id=40005698-6576 |
| 원본 화면설계서 | v27.6 slide 75~93 전문 → `docs/ORIGIN_MO9_0301.md` (**pptx 를 열지 않습니다**) |
| 정량 근거 | Microsoft Clarity 2026-06-29 ~ 2026-07-29 (30일) → `docs/EVIDENCE.md` |
| 작성자 | 허지원 책임 |

기술 사양 — 단일 HTML, 외부 리소스·CDN 의존 없음, 인라인 CSS + Vanilla JS,
**브라우저 저장소 미사용**(상태는 메모리만).

**주 작업 대상** — `index.html`의 `<section id="rt-spec">` = 02 화면설계 탭.
프레임은 **A ~ H**(H = 레이어 · 팝업), 부록은 0 ~ 6.
> 줄 번호는 적지 않습니다 — 작업 1건마다 낡습니다. `<section id="rt-...">` 로 찾습니다.

영역 체계 · PC 레이아웃 전제 · 브레이크포인트(C-46 · 769px)는 **`docs/PROJECT_CONTEXT.md`**가 단일 출처입니다.

## 4. 어떤 문서를 보는가

`CLAUDE.md` 1절의 표를 따릅니다. 요약하면 —
**항상** `docs/STATUS.md` · 이 문서 · `docs/RULES.md` /
**필요할 때** `MARKUP`(마크업) · `REQUIREMENTS`·`DECISIONS`(정의) · `ORIGIN_MO9_0301`(원본) ·
`VALIDATION`(검증) · `NEXT_ACTIONS`(남은 일) · `OPEN_QUESTIONS`(외부 확인) ·
`CHANGELOG`·`PROJECT_STATE`(경위).

## 5. 커밋 · 배포

레포 루트에서 **`push.cmd`** 실행 → `push.ps1`이 잠금 파일 정리 → `git add -A` → 커밋 → push
→ GitHub Pages 자동 배포(1~2분). 로컬 환경에는 네트워크가 없어 **push 는 사용자가 실행**합니다.

```
push.cmd                          기본 메시지로 커밋 후 push
.\push.ps1 "docs(04): 변경 내용"    메시지 지정
```

커밋 메시지 — `docs(NN):` / `fix(NN):` / `feat(NN):` 접두어(NN = 탭 번호) + 변경 항목.
**`docs: 내용 갱신` 같은 메시지를 쓰지 않습니다** — 이력이 무용지물이 됩니다.
`docs/CHANGELOG.md` 항목의 `Commit:` 칸에 해시를 채워 이력과 커밋을 잇습니다.

`push.ps1`이 `git add -A`를 쓰므로 **작업 폴더에 임시 파일을 남기지 마세요.**
브라우저 캐시로 이전 버전이 보이면 URL 뒤에 `?v=2`.
