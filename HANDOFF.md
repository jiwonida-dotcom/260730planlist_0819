# 인수인계 — U+유모바일 가입신청서 요금제 선택(S-02) 화면설계서

> **작성 2026-08-14 · 기준 커밋 `febb81a` (2026-08-13 16:26)**
> **사용법** — 새 Claude 세션의 첫 메시지로 이 문서 전체를 붙여넣고, 함께 받은 `index.html`을 첨부하세요.
> 이전 계정의 프로젝트 지식은 새 계정으로 넘어가지 않으므로, **이 문서 한 장에 필요한 맥락을 모두 담았습니다.**
> 맨 아래 11번이 실제 지시문입니다.

---

## 1. 프로젝트 개요

U+유모바일 가입신청서의 **요금제 선택 화면(S-02)** UX 개선 프로젝트입니다.
산출물은 **단일 HTML 통합 리포트** 하나이며, 그 안에 5개 탭이 들어 있습니다.

| 항목 | 값 |
|---|---|
| 배포 | https://jiwonida-dotcom.github.io/260730planlist_2/ |
| 레포 | https://github.com/jiwonida-dotcom/260730planlist_2 (`index.html` 단일 파일) |
| 현재 파일 | **411KB · 2,993줄** (기준 커밋 `febb81a`) |
| 원본 피그마 | https://www.figma.com/design/SRasUqBc8lp9g0VT5mvjCu/2026.03-가입신청서?node-id=40005698-6576 |
| 피그마 file key | `SRasUqBc8lp9g0VT5mvjCu` / 화면설계서 캔버스 `40005698:6576` |
| 정량 근거 | Microsoft Clarity 2026-06-29 ~ 07-29 (30일) |
| 작성자 | 허지원 책임 |

기술 사양 — 단일 HTML, 외부 리소스·CDN 의존 없음, 인라인 CSS + Vanilla JS, **브라우저 저장소 미사용**(상태는 메모리만).

### 리포트 탭 구성 (탭 번호는 2026-08-13에 재정렬됨 — 주의)

| 탭 | 섹션 id | 내용 |
|---|---|---|
| **01 화면설계** | `rt-spec` | **← 이번 작업 대상.** S-02 화면설계서 (영역 명세 · 상태 정의 · 접근성 · 데이터 매핑) |
| 02 프로토타입 | `rt-proto` | 동작하는 요금제 선택 화면 + 시나리오 시뮬레이터 |
| 03 기능정의 | `rt-func` | 화면·URL 정의 · 검증 시퀀스 · 예외 처리 · 데이터 연동 |
| 04 요구사항 정의서 | `rt-req` | 문제 정의(P1~P5) · 개선 원칙 · 공통 기준 · 측정 기준 |
| 05 개선안 | `rt-plan` | 개선 배경(데이터 근거) · AS-IS/TO-BE · 화면 개선 |

> 탭 번호와 섹션 id가 어긋나 있습니다(`rt-spec`이 01번). JS의 `R_TABS = ['spec','proto','func','req','plan']` 순서가 화면 노출 순서입니다. **id는 바꾸지 마세요** — 앵커·CSS가 전부 id 기준입니다.

### 확정/미확정 코드 정의 위치

- `C-01`~`C-44` 확정 사항 44건 — **38건은 「05 개선안」 탭, 6건(C-28·C-32·C-36·C-41·C-42·C-43)은 「04 요구사항 정의서」 탭**에 정의
- 01 화면설계 · 03 기능정의는 **참조만** 하고 정의를 두지 않음
- `O-nn` 미확정 3건 — 아래 9번 참조

---

## 2. 작업 범위

| | |
|---|---|
| 대상 | `index.html` 안의 `<section id="rt-spec">` = **01 화면설계 탭** — **1337 ~ 2993줄** (파일 끝) |
| 전용 CSS | `<style>` 내 `/* ===== 05 화면설계 — 피그마 프레임 형식 ===== */` **485 ~ 785줄** (약 300줄) |
| 전용 JS | `fsLogRows` · `fsLogNorm` · `fsLogPaint` · `fsLogFind` · `fsLogMore` · `fsSyncSticky` · `fsToolbarPassed` · `fsPinToolbar` · `fsSub` |
| 손대지 않은 곳 | 02 프로토타입 · 03 기능정의 · 04 요구사항 정의서 · 05 개선안 탭, 헤더/푸터, 프로토타입 엔진 |

작업 방식은 **`index.html` 직접 수정**입니다. 해당 구간은 구조 태그마다 줄바꿈이 들어가 있어 grep·부분 치환이 가능합니다.

---

## 3. 문서 성격 — 되돌리지 말 것

이 문서는 **프로토타입 · 화면설계 단계 산출물이며 디자인 시안 단계 이전**입니다. 사용자의 명시적 요청으로 디자인 표현을 전부 삭제했습니다.

**명세에 쓰지 않음** — 색상 hex(`#5B4BE1`), 폰트 크기/굵기(`17.5px / 800`), radius, 그림자, 그라데이션, hover/press 시각 표현, 요소 단위 치수(`62 × 44px`, `높이 107px`)

**유지 대상** — 접근성 요건(터치 타깃 44px, 색상 단독 금지 + 보더·채움·라디오 3중 단서, WCAG 항목), 구조 규칙(제공량 3열 1행, PC 2열, 혜택 유무에 따른 카드 높이 분기, 배지 최대 2개), 표기 규칙(판매가 취소선 + 프로모션가), 레이아웃 구조(프레임 폭 모바일 394 / PC 700px, 콘텐츠 폭 360 / 644px, 브레이크포인트)

치수를 지울 때는 **요건이 함께 사라지지 않게** 바꿔 적었습니다.
예) "보더 2px로 늘어날 때 padding 1px 축소(16→15)" → "선택 전후 카드 크기가 변하지 않아야 함 — 목록 흔들림 방지"

SCREEN 목업은 프로토타입 렌더이며 디자인 확정안이 아님을 문서 상단에 명시해 두었습니다.

---

## 4. 현재 구조

01 화면설계 탭 = 상단 **sticky 툴바**(구성 탭 + 영역 이동) + 구성 3개. 선택한 구성만 노출됩니다.

```
문서정보  #fs-sub-info
 ├ 0 문서 사용 안내 (.r-sec)        문서 단계 · 구성 라벨 표 · 표기 규칙 표 · 문서 구성 설명
 └ PAGE INFORMATION  article#fs-info   기본 정보(코드/타이틀/URL/진입/아웃) + HISTORY

화면설계  #fs-sub-design
 ├ 변경 이력          details#fs-log    기본 접힘 · 검색 + 20건 단위 더보기 · 결정 대기 3건(O-17/18/19)
 ├ 화면 구성 · INDEX   details#fs-index  기본 펼침 · A~G 인덱스
 ├ 전체 화면 구성      details#fs-full   기본 접힘 · 모바일 3종(기본·비교시트·0건) + PC 기본
 └ A~G 영역 프레임     article#fs-a ~ #fs-g

부록      #fs-sub-appendix
 1 레이아웃 규격(모바일/PC 대조) · 2 상태 정의 · 3 노출·운영 규칙 · 4 접근성 명세 · 5 데이터 매핑
```

### 현재 물량 (실측)

| 구분 | 수 |
|---|---|
| 프레임 | 11 (PAGE INFO · 변경 이력 · INDEX · 전체 화면 + A~G) |
| 상태 블록 | 23 (모바일 + PC 상태 포함) |
| 요소 명세 행 | 76 |
| 번호 콜아웃 `.fs-cal` | 63 |
| 영역 마커 `.fs-am` | 24 |
| 접이식(`<details>`) | 3 (변경 이력 · INDEX · 전체 화면) |

### 영역 체계 — 피그마 PAGE INDEX 기준

| | 영역 | 고정 | PAGE CODE |
|---|---|---|---|
| A | 헤더 | 상단 고정 | S-02_A |
| B | 타이틀 | 스크롤 | S-02_B |
| C | TAB | 상단 STICKY | S-02_C |
| D | 목록 컨트롤 (필터 패널 포함) | 스크롤 | S-02_D |
| E | 요금제 목록 카드 | 스크롤 | S-02_E |
| F | FOOT STICKY (Worning/Error Message · 선택 요금제 정보 · CTA) | 하단 고정 | S-02_F |
| G | Modal Bottom Sheet (비교 담기 · 비교 시트) | Modal | S-02_G |

**PC 상태 규칙** — PC 상태는 각 영역의 **마지막 상태 블록**에 두고, 모바일과 차이가 있는 영역(A·B·D·E·F·G)에만 둡니다. 차이가 없는 C.TAB은 모바일 명세에 "PC도 동일"로 표기합니다.

**PC 레이아웃 전제** — PC 신청서는 전체 페이지가 아니라 **오버레이 위 중앙 풀 팝업**입니다. 원 서비스는 1000px 캔버스에 `zoom:0.7`로 700px 표시하므로, 설계서는 **실렌더 값(프레임 700 / 콘텐츠 644 / 목록 2열)**을 기준값으로 씁니다. PC 2열 격자는 개통 방법 선택 화면(S-01) 격자 재사용입니다.

---

## 5. 프레임 내부 마크업 규약

```html
<article class="fs-frame" id="fs-e">
  <div class="fs-fhead">                     영역명 + PAGE CODE
  <div class="fs-fbody"><div class="fs-fmain">
    <div class="fs-state sp">                상태 1개 = 화면(좌·sticky) | 조건+명세(우)
      <div class="fs-screen"><div class="fs-mock">…목업…</div></div>
      <div class="fs-cond">                  노출 조건
      <div class="fs-rows">
        <div class="fs-row"><div class="fs-no">01</div><div class="fs-dl">
          <div><dt>Component</dt><dd><p>…</p></dd></div>
          <!-- 라벨 순서: Component · Interaction · Action · Value · Memo -->
    <div class="fs-state sp more">           2번째 이후 상태 (점선 구분)
  <div class="fs-fside">                     HISTORY 띠 (프레임 최하단)
```

- **접이식**은 `<details class="fs-frame">` + `<summary class="fs-fhead">` (JS 토글 아님 — 키보드·스크린리더 기본 동작 확보 목적)
- **목업**은 프로토타입 CSS 클래스를 그대로 재사용: `.card .card-main .badges .bdg .rank .radio .pname .prices .info .chips .benefit .cmpbox` / `.tabbar .ctrls .chip .filterbox` / `.sheet .ctable .selbtn` / `.ph-sticky .sum .cta` / `.empty .toast`
- **콜아웃(빨간 번호)** = `<b class="fs-cal">01</b>` — 부모 요소에 `style="position:relative"` 필요. 변형: `.o` 좌측 바깥(-32px) · `.r` 우측 · `.r.o` 우측 바깥 · `.up` 중앙 상단
- **영역 마커** = `<b class="fs-am">A</b>` (전체 화면 구성용)
- **콜아웃 번호와 명세 행 번호는 1:1 대응**입니다. `SYS` 행(화면에 그려지는 요소 없음)만 번호를 붙이지 않습니다. 행을 추가·삭제하면 **양쪽 번호를 함께 재정렬**하세요.

---

## 6. 반드시 지킬 제약 — 실제로 사고가 났던 항목

1. **`.fs-frame`에 `overflow:hidden` 금지.** 스크롤 컨테이너가 생겨 화면 sticky가 무력화됩니다. 모서리는 `>*:first-child` / `>*:last-child` 라운딩으로 처리했습니다.
2. **목업 내부의 `overflow:hidden`은 해제.** 바깥으로 뺀 콜아웃이 잘립니다. `.filterbox`(펼침 애니메이션용), `.sheet-b`는 이미 해제해 두었습니다.
3. **클래스명 충돌 주의.** `.fs-dim`(흐린 텍스트)과 `.fs-veil`(비교 시트 딤)은 별개입니다. 과거 이름이 겹쳐 텍스트 span이 전면 오버레이로 변해 **페이지 전체가 어두워진 사고**가 있었습니다.
4. **`<table>`의 직접 자식으로 콜아웃을 넣지 말 것.** HTML 파싱 규칙(foster parenting)으로 테이블 밖으로 밀려나 위치가 어긋납니다. 래퍼 `div`를 쓰세요.
5. **가로로 넘칠 수 있는 flex 컨테이너에 `justify-content:center` 금지.** 좌우로 동시에 넘쳐 왼쪽이 잘리고 스크롤로도 닿지 않습니다. 첫/마지막 항목에 `margin:auto`를 쓰세요.
6. **sticky 오프셋 하드코딩 금지.** `fsSyncSticky()`가 탭바·툴바 높이를 실측해 `--tbh`/`--tlh`에 넣고, 앵커 `scroll-margin-top`이 이를 참조합니다. 구성 전환·리사이즈 시 재측정됩니다.
7. **구성 전환 시 툴바 고정 로직을 건드리지 말 것.** `fsSub()`는 전환 **전에** `fsToolbarPassed()`로 위치를 판정하고, 전환 후 `fsPinToolbar()`로 툴바를 탭바 바로 아래에 되돌립니다. 짧은 구성으로 바꾸면 문서가 짧아져 스크롤이 잘리고 툴바가 떨어지기 때문입니다. `fsPinToolbar()`는 `html{scroll-behavior:smooth}`를 우회해 **즉시 이동**해야 합니다(`behavior:'instant'`, 실패 시 `scrollBehavior='auto'` 폴백).
8. **1180px 이하에서는 좌우 배치 → 세로 배치**로 되돌아갑니다(명세 폭 확보). 이 경계를 임의로 바꾸지 마세요.
9. **전체 화면 구성의 목업은 `zoom:.88`.** 3~4화면을 한 행에 넣기 위한 것이고, 폭을 줄이는 방식은 탭이 두 줄로 떨어져 실제 레이아웃을 왜곡합니다.
10. **변경 이력 표는 작성일자 역순으로 마크업에 고정**입니다(JS 정렬 없음). 행을 추가할 때 **맨 위**에 넣으세요. 검색·더보기(20건 단위)는 `fsLogPaint()`가 처리합니다.

---

## 7. 검증 방법 — 수정 후 반드시 실행

Chromium은 `/opt/pw-browsers/chromium`에 있습니다(Playwright 브라우저 설치 불필요).

**6개 폭(1400 / 1280 / 1180 / 1000 / 768 / 420) × 3개 구성**에서 확인:

- 콜아웃·마커 잘림 **0건** — 상위 요소를 거슬러 올라가며 `overflow != visible`인 조상과 경계를 비교. **단, 가로 스크롤 가능한 조상(`scrollWidth > clientWidth`)은 제외** (스크롤로 도달 가능하므로 잘림이 아님)
- 뷰포트 35% 이상을 덮는 **예상 밖 오버레이 0건** (비교 시트 딤 `.fs-veil`은 목업 내부이므로 정상)
- 좌우/세로 레이아웃 전환이 1180px 경계에서 동작
- JS 오류 0건 · `html.parser` 태그 정합성 0건
- 5개 리포트 탭 전환 및 프로토타입 카드 선택 동작
- 앵커 이동 시 프레임 상단이 툴바 하단 **+18px**(768 이하 +22px)에 멈춤
- 접이식 프레임 접기/펼치기 왕복, 접힌 프레임으로 앵커 이동 시 자동 펼침
- **구성 전환(문서정보 ↔ 화면설계 ↔ 부록) 시 툴바가 화면에서 움직이지 않을 것** — 툴바를 지나 스크롤한 상태 / 아직 위에 있는 상태 두 경우 모두
- **콜아웃 번호 ↔ 명세 행 번호 1:1 정합** (SYS 행 제외)

**요소 단위 스크린샷만 믿지 마세요.** 과거 페이지 전면 오버레이 사고를 요소 스크린샷으로 놓쳤습니다. **페이지 전체 스크린샷**을 함께 확인하세요.

---

## 8. 직전 세션까지의 진행 (2026-08-13)

| 커밋 | 내용 |
|---|---|
| `cf214f5` | 화면설계 탭을 피그마 프레임 형식으로 전면 재구성 (영역 A~G · 2열 레이아웃 · 디자인 시안 영역 삭제) |
| — | 검수 리포트 작성 — 6폭 × 3구성 18조합 전수 검증 **전 항목 통과**, 명세 ↔ 프로토타입 정합성 일치, 끊긴 요구사항 참조 0건 |
| — | 04/05 개정 — **O-18 ID 충돌 해소**(카드 정보 밀도로 원복, 비교 시트는 O-19 신규), C-nn 정의 위치 안내 블록 추가 |
| `9fff433` | 변경 이력 역순·검색·더보기 + Worning/Error Message 통합 정의 · 미기술 상태 보강 |
| `28cb895` | 구성 전환 시 sticky 툴바 고정 · 콜아웃 넘버링 1:1 정합 · **변경 이력 초기화**(전 이력을 `26.08.13 문서 생성` 1건으로 리셋, 프레임 HISTORY도 동일) |
| `febb81a` | **PC 화면설계 추가** — 영역별 PC 상태 · PC 전체 화면 도면 · 레이아웃 규격(모바일/PC 대조) 보강 |

---

## 9. 미결 · 다음 작업 후보

### 결정 대기 (문서에 명시된 3건)

| 코드 | 내용 | 담당 |
|---|---|---|
| O-17 | E. 프로모션 적용 기간 표기 — 적용 기간·종료 후 금액 없음. 혜택 면 "(24개월)"과 기준 불일치. 표시·광고 규제 검토 | 기획 · 법무 |
| O-18 | E. 카드 정보 밀도 — 첫 화면 동시 노출 수 부족, 비교 시트 의존도 높음. 프로토타입 실측 카드 높이 **혜택 없음 298px / 혜택 있음 353px** | 기획 · 디자인 |
| O-19 | G. 비교 시트 상세 목업 별도 정의 | 기획 |

### 검수 리포트 지적 중 미처리

- **추적성 태그 `C-26` 누락** — 선택 상태 3중 단서 요건을 내용상 반영하고 있으나 코드 태그가 붙어 있지 않습니다(`C-41`은 2곳에 표기됨). 요구사항 개정 시 영향 범위 추적에서 누락됩니다.
- **표기 교정 정책 미문서화** — `Hisotry.back()`→`History.back()`, `NO ACION`→`NO ACTION`은 교정했으나 `Worning`(9회) · `LABLE`(1회) · `Abled`(2회)는 원문 유지입니다. "피그마 컴포넌트/프레임 명칭은 원문 유지, 서술문은 교정" 같은 한 줄 규칙을 문서 사용 안내에 넣는 것을 권합니다.
- **1180px 경계 표기** — 문서는 "1180px 미만에서 세로 배치"인데 구현은 `max-width:1180px`이라 1180px 자체가 세로입니다. 표기를 맞추는 편이 좋습니다.
- **A~G 프레임 접이식 미적용** (선택 사항) — 현재 항상 펼침. 프레임 7개 × 상태 23개라 문서가 길어 접이식 전환을 검토할 만합니다.

### 피그마 원본 미조치

- 오타 5건 — `Compoent`→Component, `Hisotry.back()`→History.back(), `NO ACION`→NO ACTION, `FOOT STICIKY`→FOOT STICKY, `E. 요금제 코드`→E. 요금제 목록 카드. **문서는 교정, 피그마 원본은 미수정** (허지원 책임)
- 오류 1건 — B.타이틀 노출 조건이 "상단 고정"으로 되어 있으나 실제는 스크롤(A에서 복사된 것으로 판단). 문서는 스크롤로 표기. 확인 필요

---

## 10. 커밋 · 배포

이전 세션에서는 레포가 세션 승인 목록에 없어 **push가 불가**했습니다(git proxy 403).
새 세션에서 이 레포를 소스에 추가하면 Claude가 직접 push할 수 있습니다. 그렇지 않으면 수동 반영하세요.

수동 반영: `index.html`을 레포 루트에 덮어쓰고 커밋 → GitHub Pages가 1~2분 내 자동 배포. 브라우저 캐시로 이전 버전이 보이면 URL 뒤에 `?v=2`.

커밋 메시지 형식 — `docs(05):` / `fix(05):` / `feat(05):` 접두어 + 변경 항목 불릿.

---

## 11. 새 세션에 줄 지시문

> 첨부한 `index.html`의 **01 화면설계 탭(`<section id="rt-spec">`, 1337~2993줄)**을 이어서 작업한다.
> 위 3번(문서 성격)과 6번(구현 제약)을 반드시 지키고, 수정 후 7번 검증을 6개 폭 × 3개 구성으로 실행한 뒤 결과를 보고해라.
> 파일은 직접 수정하고, 완료 후 수정된 `index.html`을 전달해라.
>
> 오늘 할 일: **(여기에 요청 사항을 적으세요)**
