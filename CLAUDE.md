# CLAUDE.md — AI 작업 진입점

## 0. 시작 전 필수 확인
**작업 시작 시 `git log --oneline -5`로 실제 HEAD를 확인하고 `docs/PROJECT_STATE.md`의 기준 커밋과 대조합니다.**
어긋나면 문서가 낡은 것이므로 먼저 문서를 최신 `index.html` 기준으로 갱신한 뒤 작업합니다.
(2026-08-17에 실제로 문서가 커밋 12개만큼 뒤처져 있던 사례가 있습니다 — `MIGRATION.md` 3절)

## 1. 작업 원칙
이 프로젝트에서 AI는 단순 코드 생성기가 아니라 **기존 프로젝트 지식을 유지하면서 변경을 수행하는 작업자**로 동작합니다.

작업 전에 반드시 아래 순서로 확인합니다.

1. `HANDOFF.md` — 현재 상태 진입점
2. `docs/PROJECT_STATE.md` — 기준 커밋 · 물량 · 미처리
3. `docs/RULES.md` — 금지사항과 그 사고 이력
4. `docs/MARKUP.md` — `rt-spec` 프레임 마크업 규약 (코드 수정 시 필수)
5. `docs/DECISIONS.md` — 확정 / 이월
6. `docs/REQUIREMENTS.md` — C-01~C-59 전문 · 명세 원칙 · 추적성
7. `docs/VALIDATION.md` — 검증 매트릭스
8. 작업과 직접 관련된 `index.html` 영역

## 2. 작업 순서 — 반드시 이 순서로

새 요청은 여러 차례 나누어 들어옵니다. 매 요청마다 아래 3단계를 **순서대로** 밟고, 각 단계가 끝나야 다음으로 넘어갑니다.

```
① 프로토타입 셋팅 완료  →  ② 화면설계 적용 완료  →  ③ 이외 문서 반영
   02 rt-proto              01 rt-spec              03 · 04 · 05 · 06 · docs/
```

| 단계 | 대상 | 완료 기준 |
|---|---|---|
| **① 프로토타입** | 02 프로토타입 (`rt-proto`) | 동작이 실제로 구현되고 **브라우저에서 검증**됨. 사용자가 직접 눌러 확인할 수 있는 상태 |
| **② 화면설계** | 01 화면설계 (`rt-spec`) | SCREEN 목업 + 요소 명세 반영. **콜아웃 ↔ 명세 행 1:1 정합 0건** |
| **③ 이외 문서** | 03 기능정의 · 04 요구사항 · 05 개선안 · 06 테스트 케이스 · `docs/` | 정의 · STEP · 예외 · 테스트 케이스 · 변경 이력까지 정합 |

### 왜 이 순서인가
- **목업은 프로토타입 CSS 클래스를 그대로 재사용**합니다(`docs/MARKUP.md` 3절). 프로토타입에서 요소를 확정해야 목업이 그 마크업을 옮겨 담을 수 있습니다. 순서를 뒤집으면 목업을 두 번 그리게 됩니다.
- 프로토타입에서 **구조적 문제가 먼저 드러납니다.** 실제로 속도 안내 버튼을 카드 선택 버튼 안에 넣으면 버튼 중첩(C-41 위반)이 되는 것을 프로토타입 단계에서 잡았습니다.
- 명세를 먼저 쓰면 **구현 불가능한 정의**가 남을 수 있습니다.

### 각 단계에서 보고할 것
① 동작 검증 결과 · ② 콜아웃 정합 결과 · ③ 18조합 검증 결과 및 영향 문서 목록

### 예외
문구 교정처럼 **화면 요소가 바뀌지 않는 변경**은 ①을 건너뛰고 ②부터 시작합니다. 건너뛴 이유를 보고에 적습니다.

## 3. 변경 원칙
- 기존 동작을 먼저 이해한 후 수정합니다.
- 요청 범위를 벗어난 리팩터링을 하지 않습니다.
- 기존 ID, 클래스, 앵커, 탭 구조를 임의 변경하지 않습니다.
- 요구사항의 의미를 삭제하지 않고 표현만 개선합니다.
  - 예) "보더 2px로 늘어날 때 padding 1px 축소(16→15)" → "선택 전후 카드 크기가 변하지 않아야 함 — 목록 흔들림 방지"
- 디자인 확정안이 아닌 화면설계/프로토타입이라는 프로젝트 성격을 유지합니다.
- 변경으로 인해 다른 요구사항 / 화면 / 테스트 케이스가 영향을 받는지 확인합니다.
- **C-nn을 추가·수정하면 정의(04 또는 05 탭) · `docs/REQUIREMENTS.md` · 06 테스트 케이스 근거 열을 함께 갱신합니다.**

## 4. 작업 후 반드시 수행
- `docs/VALIDATION.md`의 6폭 × 3구성 검증 실행 (이전 PASS 결과 승계 금지)
- `docs/PROJECT_STATE.md`에 현재 상태 반영
- `docs/CHANGELOG.md`에 의미 있는 변경 기록
- 검증 결과를 `docs/VALIDATION.md` 기록 형식으로 반영
- `HANDOFF.md`의 "현재 상태"와 "다음 작업"을 최신 상태로 갱신
- 작업 폴더에 임시 파일을 남기지 않음 (`push.ps1`이 `git add -A`를 씀)

## 5. 새 세션에서의 기본 응답 형식
작업 시작 시 다음을 먼저 보고합니다.

- 확인한 HEAD 커밋과 문서 기준 커밋의 일치 여부
- 이해한 작업 범위
- 수정 대상
- 수정하지 않을 대상
- 관련 규칙 (`docs/RULES.md`의 해당 항목 번호)
- 예상 영향 범위
- 검증 계획

작업 완료 시 다음을 보고합니다.

- 변경 내용
- 영향 범위
- 검증 결과 (폭 × 구성 조합별)
- 미해결 사항
- 다음 작업

## 6. 원칙
**모르는 것은 추정하지 않습니다.**
프로젝트 문서에서 확인할 수 없는 내용은 "확인 필요"로 표시합니다.
숫자·수치는 출처 없이 생성하지 않습니다.
AI의 제안과 사람의 확정 결정을 반드시 구분해 기록합니다.
