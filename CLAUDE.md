# CLAUDE.md — AI 작업 진입점

## 0. 시작 전 필수 확인

```
node tools/check.mjs --status
```

**이 한 줄이 세션 시작 절차입니다.** HEAD · 파일 규모 · 확정 건수 · 테스트 케이스 · 물량을
실측해 `docs/STATUS.md`에 씁니다. 실측값을 손으로 적은 문서는 반드시 낡습니다 —
2026-08-17에 문서가 커밋 12개만큼 뒤처져 있었고(`MIGRATION.md` 3절),
2026-08-20에도 `HANDOFF.md`가 두 세대(59건/106건/구 탭 번호) 뒤처져 있었습니다.

**수치가 필요하면 `docs/STATUS.md`를 봅니다.** 다른 문서의 수치 표기는 참고이며,
어긋나면 `STATUS.md`가 맞습니다.

## 1. 작업 원칙

이 프로젝트에서 AI는 단순 코드 생성기가 아니라 **기존 프로젝트 지식을 유지하면서 변경을 수행하는 작업자**로 동작합니다.

### 항상 읽는 것 (3종)

1. `docs/STATUS.md` — 실측 현재 상태 (0절에서 생성)
2. `HANDOFF.md` — 지금 할 일 · 진행 중 맥락
3. `docs/RULES.md` — 금지사항과 그 사고 이력

### 작업 유형에 따라 읽는 것

| 하려는 일 | 읽을 문서 |
|---|---|
| `index.html` 마크업 수정 | `docs/MARKUP.md` |
| `C-nn` 추가 · 개정 | `docs/REQUIREMENTS.md` · `docs/DECISIONS.md` |
| 원본 정의 · 문구 · 코드 확인 | `docs/ORIGIN_MO9_0301.md` (**pptx 를 열지 않습니다**) |
| 검증 항목 · 판정 기준 | `docs/VALIDATION.md` |
| 왜 이렇게 됐는지 추적 | `docs/CHANGELOG.md` · `docs/PROJECT_STATE.md` |
| 남은 일 · 우선순위 | `docs/NEXT_ACTIONS.md` |
| 외부 확인 대기(법무 · 기획 · 피그마) | `docs/OPEN_QUESTIONS.md` |

전부 읽지 않습니다. **필요한 것만 읽고, 읽지 않은 문서의 내용을 추정하지 않습니다.**

## 2. 작업 순서 — 반드시 이 순서로

새 요청은 여러 차례 나누어 들어옵니다. 매 요청마다 아래 단계를 **순서대로** 밟고, 각 단계가 끝나야 다음으로 넘어갑니다.

```
⓪ 범위 · 대안 판정  →  ① 프로토타입 셋팅  →  ② 화면설계 적용  →  ③ 이외 문서 반영
   (만들기 전에)         01 rt-proto           02 rt-spec         03 · 04 · 05 · 06 · docs/
```

| 단계 | 대상 | 완료 기준 |
|---|---|---|
| **⓪ 범위 · 대안** | 판단 | 아래 3문항에 답하고 `docs/DECISIONS.md`에 한 줄 기록 |
| **① 프로토타입** | 01 프로토타입 (`rt-proto`) | 동작이 실제로 구현되고 **브라우저에서 검증**됨. 사용자가 직접 눌러 확인할 수 있는 상태 |
| **② 화면설계** | 02 화면설계 (`rt-spec`) | SCREEN 목업 + 요소 명세 반영. **콜아웃 ↔ 명세 행 1:1 정합 0건** |
| **③ 이외 문서** | 03 기능정의 · 04 요구사항 정의서 · 05 테스트 케이스 · 06 변경 이력 · `docs/` | 정의 · STEP · 예외 · 테스트 케이스 · 변경 이력까지 정합 |

### ⓪ 범위 · 대안 판정 — 만들기 전에 묻는다

1. 이 요소가 **이 화면(S-02)에 있어야 하는가?** 없어도 되는 근거는 무엇인가?
2. 대안 중 **가장 과감한 안**은 무엇이고, 그것을 택하지 않는 이유는 무엇인가?
3. 이 판단이 뒤집히면 **폐기되는 작업량**은 얼마인가?

> **왜 이 단계가 생겼는가 — 실제 손실 2건**
> · `C-60` 제휴카드 선택 화면: ① 프로토타입을 4차에 걸쳐 만들고 목록 높이 기준값을
>   368.5 → 420.7 → 418.6px 로 세 번 인계한 뒤, `C-62`로 화면 밖으로 나가며 **전량 폐기**됐습니다.
> · `C-51` FOOT STICKY: 대안 A · B · C 를 실측하고 **B(169px)** 를 채택해 스크롤 축약까지
>   구현한 다음 날, **C(123px · 제휴카드를 Sticky 밖으로)** 에 해당하는 `C-62`를 확정하며
>   `stickyMini` 를 제거했습니다. **정답이 이미 그 표에 있었습니다.**
> 두 건 모두 1번 문항에 30분을 썼다면 며칠을 아꼈습니다.

### 왜 ①②③ 이 순서인가
- **목업은 프로토타입 CSS 클래스를 그대로 재사용**합니다(`docs/MARKUP.md` 3절). 프로토타입에서 요소를 확정해야 목업이 그 마크업을 옮겨 담을 수 있습니다. 순서를 뒤집으면 목업을 두 번 그리게 됩니다.
- 프로토타입에서 **구조적 문제가 먼저 드러납니다.** 실제로 속도 안내 버튼을 카드 선택 버튼 안에 넣으면 버튼 중첩(C-41 위반)이 되는 것을 프로토타입 단계에서 잡았습니다.
- 명세를 먼저 쓰면 **구현 불가능한 정의**가 남을 수 있습니다.

### 목업은 가능하면 렌더 함수에서 파생시킨다
`H. 레이어 · 팝업` 프레임은 목업을 직접 쓰지 않고 프로토타입의 `alertHtml()` · `depHtml()` 을
그대로 호출해 그립니다(`fsPopPaint()`). 문구를 옮겨 적지 않으므로 **프로토타입과 갈라질 수 없습니다.**
반면 직접 쓴 목업은 갈라집니다 — F 영역 목업이 구 구조(`.sum .amt` · `.ccbtn`)로 남아
관련 CSS 를 지우지 못했고, 2026-08-19 에 「화면설계를 프로토타입 현재 버전으로 맞춤」 작업을
따로 해야 했습니다. **새 목업은 렌더 함수 파생을 먼저 검토합니다.**

### 각 단계에서 보고할 것
⓪ 범위 판단 · ① 동작 검증 결과 · ② 콜아웃 정합 결과 · ③ 검증 결과 및 영향 문서 목록

### 예외
문구 교정처럼 **화면 요소가 바뀌지 않는 변경**은 ①을 건너뛰고 ②부터 시작합니다. 건너뛴 이유를 보고에 적습니다.

## 3. 변경 원칙
- 기존 동작을 먼저 이해한 후 수정합니다.
- 요청 범위를 벗어난 리팩터링을 하지 않습니다.
- 기존 ID, 클래스, 앵커, 탭 구조를 임의 변경하지 않습니다.
- 요구사항의 의미를 삭제하지 않고 표현만 개선합니다.
  - 예) "보더 2px로 늘어날 때 padding 1px 축소(16→15)" → "선택 전후 카드 크기가 변하지 않아야 함 — 목록 흔들림 방지"
- 디자인 확정안이 아닌 화면설계/프로토타입이라는 프로젝트 성격을 유지합니다.
- 변경으로 인해 다른 요구사항 / 화면 / 테스트 케이스가 영향을 받는지 확인합니다.
- **C-nn을 추가·수정하면 정의(04 요구사항 정의서 탭) · `docs/REQUIREMENTS.md` · 05 테스트 케이스 근거 열을 함께 갱신합니다.**
- **부여된 코드(`C-nn` · `TC-*` · `O-nn`)는 재부여 · 재사용 · 폐지하지 않습니다** — `docs/RULES.md` 6절.

## 4. 작업 후 반드시 수행

```
node tools/check.mjs --full --status --record
```

- 전건 PASS 를 확인합니다. **이전 PASS 결과를 승계하지 않습니다** — 스크립트가 매번 다시 측정합니다.
- `--status` 가 `docs/STATUS.md`를 갱신합니다. **실측 수치를 다른 문서에 옮겨 적지 않습니다.**
- `--record` 출력을 `docs/VALIDATION.md` 5절에 붙이고 `Commit` · `Scope` 만 채웁니다.
- `docs/CHANGELOG.md`에 **왜 그렇게 했는지**를 남깁니다 (`Change` 뿐 아니라 `Reason` · `Note`).
- `docs/PROJECT_STATE.md`에 작업 절을 추가합니다 — **판단과 경위**를 적고, 수치는 `STATUS.md`에 맡깁니다.
- `HANDOFF.md`의 「지금 할 일」과 「진행 중 맥락」을 갱신합니다.
- 작업 폴더에 임시 파일을 남기지 않습니다 (`push.ps1`이 `git add -A`를 씀).
- **커밋 메시지를 규약대로 씁니다** — `docs(NN):` / `fix(NN):` / `feat(NN):` + 변경 항목.
  `docs: 내용 갱신` 같은 메시지는 이력을 무용지물로 만듭니다. `CHANGELOG` 항목의
  `Commit:` 칸에 해시를 채워 이력과 커밋을 잇습니다.

## 5. 새 세션에서의 기본 응답 형식
작업 시작 시 다음을 먼저 보고합니다.

- `check.mjs --status` 결과 (HEAD · 규모 · 전건 PASS 여부)
- 이해한 작업 범위
- ⓪ 범위 · 대안 판정 결과
- 수정 대상 / 수정하지 않을 대상
- 관련 규칙 (`docs/RULES.md`의 해당 항목 번호)
- 예상 영향 범위 · 검증 계획

작업 완료 시 다음을 보고합니다.

- 변경 내용 · 영향 범위
- 검증 결과 (`check.mjs` 출력)
- 미해결 사항 · 다음 작업

## 6. 원칙
**모르는 것은 추정하지 않습니다.**
프로젝트 문서에서 확인할 수 없는 내용은 "확인 필요"로 표시하고 `docs/OPEN_QUESTIONS.md`에 등록합니다.
숫자·수치는 출처 없이 생성하지 않습니다 — **측정하거나, 측정한 문서를 인용합니다.**
AI의 제안과 사람의 확정 결정을 반드시 구분해 기록합니다.
