# MIGRATION — 문서 구조 재구성 이력

## 1. 현재 구조
문서 일체가 **레포 루트**에 있습니다.

```
요금제 개편/                    ← 레포 루트
├ index.html                  통합 리포트 (6탭)
├ CLAUDE.md                   AI 작업 진입점
├ HANDOFF.md                  현재 상태
├ README.md                   사람용 설명
├ MIGRATION.md                이 문서
├ .gitignore / .gitattributes
├ push.cmd / push.ps1         커밋·배포
├ docs/                       프로젝트 지식 12종
└ Storage/                    구 버전 보관
```

## 2. 재구성 이력

### 2026-08-17 (2) — 최신 기준 재동기화 · 루트 이동
문서 기준이 `febb81a` 스냅샷에 머물러 있어 실제 레포(`aa654f0`)와 어긋나 있었습니다.

| 항목 | 이전 문서 (`febb81a`) | 갱신 (`aa654f0`) |
|---|---|---|
| index.html | 2,993줄 · 411KB | **3,398줄 · 486KB** |
| 리포트 탭 | 5개 | **6개** — `rt-tc` 06 테스트 케이스(92건) 신설 |
| 확정 요구사항 | C-01~C-44 | **C-01~C-47** (C-45·C-46·C-47 추가) |
| 결정 대기 | O-17·O-18·O-19 3건 | **0건** — O-17·O-19·O-20 해소, O-18 이월 |
| `rt-spec` 줄 범위 | 1337~2993 | **1456~3398** |
| 전용 CSS | 485~785 | **486~575** (+ 코드 칩 576~590) |
| 전용 JS | 9개 함수 | **20개** — `fsTip*` · `fsSpy*` · `fsCurTab` · `fsLogHead` 추가 |

함께 수행한 것:
- `docs/REQUIREMENTS.md` 3절에 C-01~C-47 **전문 47건** 수록, 4절 추적성 매트릭스 신설
- 문서 일체를 `project_structure/` 하위 → **레포 루트로 이동** (문서가 전제하는 경로와 실제 위치 일치)
- `.gitignore` 신설 — `push.ps1`의 `git add -A`가 임시 파일을 쓸어 담는 문제 차단
- 구 `project_structure/` 및 잔여 zip은 `_to_delete/`로 이동 (Git 제외)

### 2026-08-17 (1) — ChatGPT 재구성본 보정
HANDOFF 단일 문서를 문서 계층으로 재구성한 패키지를 원본과 대조해 보정했습니다.

| # | 항목 | 조치 |
|---|---|---|
| 1 | 레포/배포 URL | `260730planlist_2` → **`_3`** 로 확정 |
| 2 | C-nn 정의 위치 | 부정확한 서술을 실측 분포로 정정 |
| 3 | `RULES.md` 근거 소실 | 금지 항목에 **실제 사고 이력과 이유** 복원 |
| 4 | 마크업 규약 누락 | `docs/MARKUP.md` **신규 작성** |
| 5 | `REGENERATED_SCOPE.md` 파손 | 문자열 리터럴 조각이 노출되던 파일을 이 문서로 대체 |

함께 복원한 상세 — 검증 판정 세부(가로 스크롤 조상 제외, 오버레이 35%, 앵커 +18px / 768↓ +22px), PC 전제(1000px 캔버스 `zoom:0.7` → 실렌더 700px), O-18 실측 카드 높이, 명세 "유지 대상" 목록, 표기 교정 현황, 피그마 오타·오류 내역.

### 2026-08-14 — 문서 계층 도입
HANDOFF 한 장을 `CLAUDE.md` + `README.md` + `HANDOFF.md` + `docs/`로 분리.

## 3. 교훈
- **문서의 기준 커밋을 항상 명시하고, 작업 시작 시 `git log`로 실제 HEAD와 대조합니다.** 프로젝트 지식에 올라간 `index.html` 사본은 시간이 지나면 낡습니다.
- `push.ps1`이 `git add -A`를 쓰므로 작업 폴더 위생이 곧 커밋 위생입니다.
