# CHANGELOG — 변경 이력

> 최신 항목이 위에 옵니다.
> `index.html` 내부의 "변경 이력" 표(`#fs-log`)와는 별개입니다. 그쪽도 역순 고정이며 행 추가 시 맨 위에 넣습니다.

## [2026-08-20] playwright 설치 완료 — Edge 폴백 실증 · 절대 치수 판정 원칙
```text
Commit: —
Type: docs · chore
Target: package.json · package-lock.json (playwright 1.62.1 devDependency) /
        docs/VALIDATION.md 3절 / docs/STATUS.md
        **index.html 무변경**
Change: 1) 사용자 PC 에 playwright 1.62.1 설치. node_modules 18MB · 180파일 —
           .gitignore:13 에 걸려 git 이 잡지 않는 것을 확인했다(push.ps1 이 git add -A 를 쓴다).
        2) **npx playwright install chromium 없이 --full 21항목 전건 PASS.**
           브라우저 탐색 순서의 Edge 폴백(channel msedge)이 실제로 동작했다 —
           Chromium 내려받기가 필요 없었다. B1 행에 「Edge (channel msedge)」로 찍힌다.
        3) **절대 치수는 환경에 따라 다르다** — 같은 파일 · 같은 검사에서 카드 높이가
           클라우드 Chromium 360×325.94 / Windows Edge 360×320.94 로 5px 달랐다.
           폰트 렌더링 차이이며 결함이 아니다. VALIDATION 3절에 원칙을 명시했다 —
           판정은 「선택 전 = 선택 후」 같은 **불변량**으로 하고(D15 가 그렇게 본다),
           문서의 절대 px 은 「그 값을 측정한 환경에서의 기록」으로 읽는다.
           새 기준값을 적을 때는 측정 환경을 함께 적는다.
Reason: 문서 곳곳에 절대 px 기준값이 적혀 있다(카드 324.78 · 하단 고정 122.2 · 목록 418.6 …).
        환경이 둘 이상이 된 이상, 그 값을 환경 표기 없이 비교하면 없는 결함을 만들어낸다.
Note:   불변량 판정을 처음부터 그렇게 설계한 것이 결과적으로 맞았다 —
        절대값을 기대값으로 박아 두었다면 Edge 에서 전건 FAIL 이 났을 것이다.
검증:   Windows Edge 로 --full **21항목 전건 PASS** (사용자 실행) ·
        클라우드 Chromium 으로도 21항목 전건 PASS. 두 환경 결과 일치(치수 표기만 다름).
```

## [2026-08-20] playwright 로컬 설치 경로 마련 — 브라우저 탐색 순서 · node_modules 제외
```text
Commit: —
Type: feat(tools)
Target: package.json (신설) / .gitignore / tools/check.mjs / docs/ENVIRONMENT.md 4절 · PROJECT_MAP.md
        **index.html 무변경**
Change: 1) **브라우저를 순서대로 찾는다** — PW_CHROMIUM 환경변수 → /opt/pw-browsers →
           playwright 가 내려받은 Chromium → **Edge(channel msedge)** → Chrome.
           Windows 에는 Edge 가 항상 있으므로 Chromium 내려받기가 막힌 환경에서도
           `npm i -D playwright` 만으로 --full 이 돈다. 어느 경로로 열렸는지는
           출력의 B1 행에 찍는다 — 무엇으로 검증했는지 모르는 채 PASS 를 믿지 않기 위해서다.
           전건 실패 시에는 시도한 경로와 오류를 함께 출력하고 FAIL 로 끝난다.
        2) **.gitignore 에 node_modules/ 추가** — push.ps1 이 git add -A 를 쓰므로
           제외하지 않으면 npm install 직후 수만 개 파일이 커밋된다. playwright 산출물
           (playwright-report/ · test-results/ · ms-playwright/)도 함께 제외했다.
           package.json · package-lock.json 은 추적한다(버전 고정).
        3) **package.json 신설** — npm run check / check:full / check:static.
        4) **ENVIRONMENT 4절 정정** — 종전에 「로컬 환경에는 네트워크가 없습니다」로 적혀
           Windows 에서도 설치가 불가한 것처럼 읽혔다. 네트워크가 없는 것은
           **Cowork 세션 샌드박스**이고 사용자 PC 는 push 가 되는 환경이다.
           두 환경을 표로 갈라 적고 설치 절차를 넣었다.
Reason: --full 21항목 중 18항목이 브라우저를 요구한다. 사용자 PC 에서 정적 3항목만 도는 상태로는
        「검증했다」고 말할 수 없다. 설치 경로를 열어 두고, 설치가 막히면 Edge 로 우회한다.
검증:   클라우드에서 --full 21항목 전건 PASS · B1 = 「playwright 내려받은 Chromium」 표기 확인.
```

## [2026-08-20] 검증 실행 편의 · Python 의존 제거 · push 확인 강화
```text
Commit: —
Type: fix(tools)
Target: check.cmd (신설) / tools/tagcheck.mjs (신설 · tagcheck.py 대체) / tools/check.mjs /
        push.ps1 / HANDOFF.md 0절 / docs/VALIDATION.md · PROJECT_MAP.md · ENVIRONMENT.md
        **index.html 무변경**
Change: 1) **check.cmd 신설** — 레포 루트가 아닌 곳에서 node tools/check.mjs 를 실행하면
           MODULE_NOT_FOUND 가 난다. push.cmd 와 같은 방식으로 cd 를 스크립트가 처리한다.
           인자 없이 실행하면 --status. node 가 없으면 그 사실을 알리고 멈춘다.
        2) **태그 검사를 Node 로 이관** — Windows 에서 `python3` 이 스토어 스텁으로 잡혀
           "Python" 만 출력하고 exit 1 로 끝났다(S2 FAIL). 런타임을 하나로 줄이는 편이
           안전하므로 html.parser 구현을 tools/tagcheck.mjs 로 옮겼다.
           같은 파일 · 같은 결함 주입에서 결과가 동일함을 대조 확인했다
           (여분 </div> → stray 3 · mismatch 2 · 줄 번호까지 일치).
           이제 정적 검사는 **node 만으로** 돈다.
        3) **push.ps1** — 성공 안내 URL 이 구 저장소(_3)를 가리켜, push 에 성공해도
           옛 화면을 보게 됐다. URL 을 _0819 로 정정하고, push 후 fetch 해서
           로컬 HEAD 와 origin/main 을 대조해 같을 때만 완료로 보고한다.
           올릴 커밋이 0건이면 그 사실을 명시하고 캐시(?v=2)를 안내한다.
           커밋 실패(user.name 미설정 등)를 성공으로 보고하던 문제도 고쳤다.
Reason: 자동화의 값은 **정확하게 동작할 때만** 생긴다. 검증 스크립트가 실행 위치 때문에
        못 돌고, 태그 검사가 런타임 문제로 FAIL 을 내고, push 스크립트가 잘못된 URL 을
        안내하면, 도구가 오히려 판단을 흐린다.
Note:   tools/tagcheck.py 는 추적에서 제외하고 _to_delete/ 로 옮겼다 —
        같은 검사를 두 언어로 두면 갈라진다(RULES 단일 출처 원칙).
검증:   node tools/check.mjs --full **21항목 전건 PASS** · S2 태그 정합 0건 ·
        로컬 Windows 에서 python 없이 --status 동작 확인.
```

## [2026-08-20] 프로세스 개편 — 검증 자동화 · 실측 단일 출처 · 진입 문서 정정
```text
Commit: 50b698f · a3c1d74 · daf2f2f
Type: feat(tools) · docs
Target: tools/check.mjs · tools/tagcheck.py (신설) / docs/STATUS.md (자동 생성) /
        docs/OPEN_QUESTIONS.md · docs/PROCESS_REVIEW.md (신설) /
        CLAUDE.md · HANDOFF.md / docs/RULES.md · TRACEABILITY.md · PROJECT_MAP.md ·
        VALIDATION.md · ENVIRONMENT.md · PROJECT_STATE.md · NEXT_ACTIONS.md
        **index.html 무변경**
Change: 1) **검증 자동화** — VALIDATION.md 2절의 판정 기준을 tools/check.mjs 로 옮겼다.
           17항목 × 18조합을 눈으로 보고 결과를 손으로 5절에 전사하던 작업이 사라졌다.
           항목의 성격이 섞여 있던 것이 문제였다 — 태그 정합 · id 중복 · 행 중첩 ·
           콜아웃 정합 · 죽은 칩 · 결번 · 물량 · 배지 규격 · 도면 생성 · 구성 소속은
           **폭과 무관**해 1회 로드로 끝난다. 이걸 18번 확인하고 있었다.
           레이아웃 항목(잘림 · 오버레이 · 좁은 폭 열 폭 · 탭 전환)만 --full 에서 18조합.
           태그 정합은 브라우저가 자동 교정하므로 html.parser(tools/tagcheck.py)로 따로 본다 —
           2026-08-19 의 여분 </div>(G 프레임 이탈)와 SYS 행 중첩이 이 검사의 대상이다.
        2) **실측 단일 출처 docs/STATUS.md** — HEAD · 파일 규모 · 확정 건수 · TC · 물량 ·
           탭 구성을 스크립트가 쓴다. 종전에는 같은 수치가 7개 문서에 사본으로 있었고
           CLAUDE.md 4절이 작업 1건마다 4문서 수동 갱신을 요구했다.
           8/18~19 이틀에 작업 15건이 들어왔으니 갱신 지점이 60곳이었다.
        3) **낡은 진입 문서 정정** — HANDOFF(규모 558KB·3,987줄 · 기준 커밋 f14d366 ·
           확정 59건 · TC 106건 · 구 탭 번호 · 구 줄 번호 · remote 미전환) ·
           CLAUDE.md 2절 도식의 뒤바뀐 탭 번호 · RULES.md 3절의 구 R_TABS 배열 ·
           TRACEABILITY 전면(C-01~C-59 · TC 92~106 · rt-plan) ·
           VALIDATION 4절(같은 절에서 116건과 106건 공존) ·
           PROJECT_MAP 줄 번호 표 폐지 · ENVIRONMENT 손으로 적은 Git 스냅샷.
           **HANDOFF 는 진입점으로 축소** — 지금 할 일 · 진행 중 맥락만 담는다.
        4) **⓪ 범위 · 대안 게이트 신설** (CLAUDE.md 2절) — ①②③ 앞에 3문항.
           이 요소가 이 화면에 있어야 하는가 / 가장 과감한 안은 무엇이고 택하지 않는 이유는 /
           뒤집히면 폐기되는 작업량은.
        5) **코드 번호 불변 규칙** (RULES.md 6절) — C-nn · TC-* · O-nn · EX-nn 은
           재부여 · 재사용 · 폐지하지 않는다. 재분류는 번호를 유지하고 속성만 바꾼다.
        6) **docs/OPEN_QUESTIONS.md 신설** — 외부 확인 대기 7건(카피 확정 · 원본 반환금 부호 ·
           피그마 원본 오타/오류 · C-61 사유 · O-18 · 접이식 · 목업 파생)을 세 문서에서
           한 곳으로 모았다. 이제 한 번에 발송할 수 있다.
        7) **NEXT_ACTIONS 재번호** — 음수 우선순위(-3.5 ~ 0) 폐지, 남은 것만 1~8.
Reason: 산출물 품질은 높은데 **품질을 유지하는 부대 비용이 산출물 제작 비용을 넘어섰다.**
        12개 문서 중 7개가 낡았고, 그 원인은 게으름이 아니라 같은 수치가 7곳에 사본으로 있고
        매 작업마다 수동 동기화를 요구하는 구조였다.
        CHANGELOG 12차에 스스로 적어 둔 원칙 — "중복은 양이 아니라 신뢰의 문제다.
        같은 사실이 두 곳에 있으면 한쪽이 낡았을 때 어느 쪽이 맞는지 알 수 없다" —
        을 index.html 안에서는 적용했지만 docs/ 자체에는 적용하지 않았다.
        진단 전문은 docs/PROCESS_REVIEW.md.
Note:   **판정 로직을 다시 쓰지 않았다.** 정의 수집은 페이지의 fsTipBuild(),
        도면은 fsPopPaint(), 변경 이력은 fsLogHead() 와 같은 셀렉터를 쓴다.
        H 프레임이 프로토타입 렌더 함수를 재사용하는 것과 같은 이유다 —
        검사가 구현을 다시 구현하면 둘이 갈라지고, 그때는 검사를 믿을 수 없게 된다.
        커밋 메시지 규약을 다시 세웠다 — 종전 커밋이 전건 `docs: 내용 갱신` 이어서
        CHANGELOG 항목과 커밋을 이을 수 없었다.
검증:   node tools/check.mjs --full **21항목 전건 PASS** (V-2026-08-20-01).
        index.html 무변경이므로 이 결과는 이후 작업의 기준선이다.
        실측 대조에서 드러난 문서 표기 차이 — 파일 규모 650KB·4,926줄 → **708KB·5,294줄** ·
        프레임 11 → **12** · 상태 블록 24 → **33** · 명세 행 91 → **154** · 콜아웃 70 → **125**.
        좁은 폭 값 칸 하한 **186px @360** 은 2026-08-19 7차 기록과 일치한다(검사가 같은 것을 본다는 확인).
```

## [2026-08-20] 06 변경 이력 — 공유 이전 구간 정리 (40 → 9행)
```text
Commit: —
Type: docs(06)
Target: index.html — #fs-log tbody 31행 삭제 / section#rt-log 리드 1문장 추가
Change: 1) **공유 이전 구간은 작성자 1인의 작업 과정**이었다. 도면 번호 지시선 · 배지 규격 ·
           마크업 정합 · 코드 칩 중복 정리처럼 **결과가 바뀌지 않는 기록**을 덜어내고,
           **설계 결정과 산출물 구성이 바뀐 항목만** 남겼다.
        2) **남긴 9행** — 26.08.20 외부 시트 링크 / 26.08.19 문서 구성 6탭 통합 ·
           제휴카드 S-02 밖 이관(C-62) · 약정 안내 팝업 2종(C-65) · 휴대폰번호 서식(C-64)과
           NPay 외부 인증 정정(C-63) · H 레이어·팝업 신설 · 테스트 케이스 116건 재구성 /
           26.08.17 원본 v27.6 대조 누락 12건 반영(C-48 ~ C-59) / 26.08.13 문서 생성
        3) 06 리드에 **정리 사실과 기준**을 명시했다 — 이후 변경은 전건 기록한다.
Note:   삭제한 31행의 전문은 **본 파일(docs/CHANGELOG.md)에 그대로 남아 있다** —
        개발 측 이력은 여기가 단일 출처이고, `#fs-log` 는 **공유 대상 이력**만 담는다.
        26.08.19 「문서 구성」 행에 남아 있던 삭제 행 참조(「메뉴 구조 재편」) 1건도 함께 정리했다.
검증:   변경 이력 9행 × 5칸 전건 정상 · 건수 라벨 9건 자동 반영 · 더보기 숨김 ·
        6탭 전환 PASS · C-01 ~ C-66 죽은 칩 0 · 테스트 케이스 116행 유지 · JS 오류 0
```

## [2026-08-20] 03 기능정의 · 05 테스트 케이스 — 외부 구글시트 링크
```text
Commit: —
Type: feat(03) · feat(05) · docs(06)
Target: index.html — section#rt-func 리드 하단 .tc-links 신설 / section#rt-tc .tc-links 링크 대상 교체 /
        #fs-log 행 1건 추가
Change: 1) **기능정의서 · 테스트 케이스를 구글시트로 내보내고 두 탭에서 바로 열 수 있게 했다.**
           문서 = **S02_요금제선택_기능정의서_TC**
           `1RRYAh9CI_20UEQuE5cRFI4Vo7mckr0Vmzp7kWIQQ0Rk`
           4시트 — 표지·개요(gid 641867070) · 기능정의서 (FRONT)(**1109240766**) ·
                  판정·예외(52555895) · T_C(**636168722**)
        2) **왜 시트로 빼는가** — 요소 명세와 테스트 케이스는 개발 · QA가 **행 단위로 필터링하고
           수행 결과를 적는** 산출물이다. 리포트 안의 표는 읽기 전용이라 그 작업을 받아낼 수 없다.
        3) **양식은 기존 「유픽폰_유심요금제_기능정의서」와 통일** — 열 구성 · 헤더(#2F3B52) ·
           그룹 구분행(#E8ECF3) · 확인필요(#FFF2AB) · 9pt · 틀고정 A2를 그대로 따랐다.
        4) **03 기능정의** — 「기능정의서 시트 열기」 신설. 02 화면설계 A~H 요소 명세 **154행**을
           컴포넌트 단위 **15열**로 옮겼다. 이 탭은 **판정**, 시트는 **요소 단위 행 정의**.
        5) **05 테스트 케이스** — 링크 대상을 구 수행 기록 시트(`10Jh9dU…`)에서 **새 문서의 T_C 시트**로
           교체. 종전 시트는 **95건 기준**이라 TC-L-01~17 신설 이후 번호 체계가 반영돼 있지 않았다.
           새 시트는 **116건 전건**이며 수행 기록 열이 **J~M → N · O열**로 바뀌었다.
Note:   마크업은 02 화면설계의 `a.fs-figma`(피그마 원본)와 같은 규약을 따른다 —
        `target="_blank"` · `rel="noopener noreferrer"` · `title="새 창으로 열립니다"` · `aria-label`.
        CSS는 기존 `.tc-links` / `.tc-link`를 **재사용**했고 새 클래스를 만들지 않았다.
        시트의 **결과(N) · 실제결과/비고(O)는 덮어쓰지 않는다.** 명세가 바뀌면 리포트를 먼저 고치고
        시트 **A~M열**을 갱신한다.
검증:   6탭 전환 PASS · 링크 2종 href · target · rel · aria-label 전건 확인 ·
        C-01 ~ C-66 죽은 칩 0 · 테스트 케이스 116행 유지 · 변경 이력 39 → 40행 · JS 오류 0
```

## [2026-08-19] 04 개선안 + 05 요구사항 정의서 → 「04 요구사항 정의서」 통합
```text
Commit: —
Type: refactor(docs)
Target: index.html — section#rt-plan 제거 / section#rt-req 8장 재구성 / r-tabs / R_TABS /
        FS_TIP_TAB / r-top 번호(tc 06→05 · log 07→06) / 본문 상호 참조 32건 / docs 3종
Change: 1) **합친 이유** — 확정 사항 66건이 60건은 04 개선안, **6건만 05 요구사항 정의서**에 있어
           요구사항 정의서에 요구사항이 거의 없었다. 정의를 찾으려면 두 탭을 오가야 했고
           코드 칩 툴팁도 두 탭을 뒤졌다. 05는 4,350자로 얇았고 그 내용(문제 · 원칙 · 문구 · 측정)은
           04 1장 「개선 배경」과 성격이 겹쳤다.
        2) **구성 8장** — 1 개선 배경(가 · 나) → 2 핵심 문제 · 개선 원칙(P1~P5 · G-01~G-06) →
           3 프로세스 개선(다) → 4 화면 개선(라) → 5 데이터 · 노출 규칙(마) →
           6 공통 기준(사 · 아 · 미확정) → 7 S-02 범위 밖(바) → 8 표준 문구 · 측정 기준.
           **왜 → 무엇을 지키는가 → 무엇을 만드는가 → 무엇으로 판정하는가** 순.
        3) **번호 이동** — 테스트 케이스 06 → 05 · 변경 이력 07 → 06. 탭바 · R_TABS ·
           FS_TIP_TAB · r-top 번호 · 본문 참조 32건 갱신. 변경 이력의 과거 행 번호도 현재 구성으로
           맞추고, 「메뉴 구조 재편」 행에는 당시 7탭 구성임을 명시했다.
        4) **삭제한 안내** — 「확정 66건 중 60건은 04, 6건은 본 탭」 KPI · 「ID 정의 위치」 안내의
           두 탭 분산 서술 · 부록 0의 「60건/6건」 표기.
Note:   섹션 id 는 `rt-req` 를 유지했다 — 코드 칩 이동(`view` 필드)과 앵커가 id 기준이다.
        프로토타입의 `screen:'plan'` 상태값은 화면 상태이므로 건드리지 않았다.
검증:   6탭 전환 PASS · rt-plan · rb-plan 잔재 0 · C-01 ~ C-66 **66건 전건이 rt-req** ·
        죽은 칩 0 · 툴팁 이동 라벨 6종 · 18조합 전수 PASS · JS 오류 0 · 도면 검증(번호 · 지시선 · 겹침) 전건 유지
```

## [2026-08-19] 도면 번호 지시선 방향 · 표기 통일 · 배지 규격
```text
Commit: —
Type: fix(02)
Target: index.html — .fs-cal.rr:after · .fs-cal.dn:after · .fs-cal.fs-cal 신설 /
        fsCalign() 재작성(열 배정 표시 · 실측 보정 · 지시선 재계산) / 부록 0 규칙 문장
Change: 1) **우측 열 지시선이 반대 방향으로 뻗었다.** 선을 왼쪽으로 돌리는 규칙이
           마크업 표기(.r)에만 걸려 있어, fsCalign() 이 실측으로 우측 열에 배정한 배지는
           선이 요소 반대쪽으로 나갔다(D 목록 컨트롤 04 · 필터 패널 05 · PC 03 · 05 등 4건).
           실측 배정에 표시 클래스(.rr)를 부여해 같은 규칙을 받게 했다.
        2) **줄 가운데 번호만 표기가 달랐다.** 여백 열 배지에는 지시선이 있는데 요소 위 배지에는
           없어 다른 체계처럼 읽혔다 → 아래로 내려가는 짧은 지시선(.dn)을 붙여 하나로 맞췄다.
        3) **배지가 컨테이너 규칙에 찌그러졌다.** 검증 실패 도면의 인라인 오류 영역에
           `.dep-err b` 규칙이 있어 배지가 23 → 14px 로 줄고 지시선이 9px 짧아졌다 →
           `.fs-cal.fs-cal` 로 규격을 컨테이너 규칙보다 우선 고정.
        4) **실측 보정 단계 추가.** left · top 은 컨테이닝 블록의 **패딩 박스** 기준이라
           요소의 테두리 · 패딩만큼 어긋난다(최대 3.9px 관측). 1차 배치 → 실측 차이 보정 →
           지시선을 보정 후 실측값으로 그리는 3단계로 나눴다.
Note:   지시선 끝점이 요소 모서리에 닿는지는 계산이 아니라 **실측으로 검증**한다.
        배지를 새 컨테이너에 붙일 때는 그 컨테이너의 자식 선택자(예: `b`)와 충돌하는지 본다.
검증:   지시선 끝점 오차 **0건**(허용 2px) · 콘텐츠/배지 겹침 0 · 번호 순서 불일치 0 ·
        배지 렌더 실패 0 · 420px 가로 스크롤폭 **234**(선재 값 복귀) · 18조합 PASS · JS 오류 0
```

## [2026-08-19] 02 화면설계 — 도면 번호 재정비 · `H. 레이어 · 팝업` 신설 · 06 재구성
```text
Commit: —
Type: feat(02) · docs(06) · fix
Target: index.html — .fs-cal CSS · fsCalign() 신설 / FS_POP · fsPopPaint() 신설 /
        article#fs-h 신설(상태 9 · 명세 66행 · 콜아웃 57) / fs-nav · INDEX · 부록 0 · 부록 4 · 부록 6 /
        rt-tc 4장 「레이어 · 팝업」 신설 · 장 번호 재배열 / alertKey() · alertFocus() /
        .fs-figma / fs-sub-design 닫힘 태그 / fsTipBuild id 중복
Change: 1) **도면 번호(콜아웃) 배치 규칙 신설 · 전수 재정비.** 번호를 요소 옆에 직접 두어
           열이 네댓 갈래로 흩어지고 일부가 콘텐츠를 덮고 있었다(E 상태 1 실측 — x 좌표 5종,
           제공량 면 안쪽 2건). 규칙 넷 — ① 콘텐츠를 덮지 않는다 ② 줄에 요소가 하나면 여백 열,
           여럿이면 왼쪽 요소 좌측 열 · 오른쪽 요소 우측 열 · 가운데 요소는 요소 바로 위
           ③ 번호는 위 → 아래, 같은 줄에서는 왼 → 오른 ④ 세로로 겹치면 아래로 어긋내고 지시선으로 잇는다.
           중첩 깊이와 PC 목업 zoom 이 달라 CSS로 열을 맞출 수 없어 fsCalign() 이 렌더 후 실측으로 정렬한다.
           부록 0에 「도면 번호 표기」 규칙을 기재.
        2) **넘버링 자체도 재정비** — 규칙 ③에 어긋난 4개 상태의 번호와 명세 행 순서를 함께 고쳤다
           (A 헤더 2건 — 뒤로 01 · 타이틀 02 / E 상태 1 — 라디오 10 → 03, 이후 번호 이동 /
           E 선택 상태 — 카드 01 · 라디오 02). E 목업에 속도 안내 버튼(qbtn)과 sprow 를 반영해
           번호 09가 가리킬 요소를 만들었다(C-58).
        3) **H. 레이어 · 팝업 프레임 신설 — 레이어 9종** 진입 안내(C-55) · 경로 전환(EX-03) ·
           약정 안내 2종(C-65) · 속도 안내(C-58) · 종속 입력 3종(C-53) + 검증 실패(인라인 오류).
           종전 「팝업 목업은 디자인 시안 단계에서 작성」 방침을 폐지했다.
           도면은 프로토타입과 **같은 렌더 함수**(alertHtml · depHtml)로 그리고 조작 속성만 걷어낸다 —
           문구를 옮겨 적지 않으므로 프로토타입과 갈라지지 않는다.
           분담 — 부록 6 = 조건 · 처리 형태 · 버튼 귀결 · 공통 규칙 / H = 도면 · 요소별 명세.
           부록 6의 시트 표는 「케이스 대조」로 바꾸고 C-53의 단일 출처 표기도 정정.
           영역 이동 · INDEX · 부록 0 · 03 · 05의 A~G 표기를 A~H로 갱신, FS_SPY_IDS 에 fs-h 추가.
        4) **06 테스트 케이스 재구성** — 레이어 케이스가 기능 · 예외 · 화면 장에 흩어져 같은 팝업을
           두 곳에서 확인하고 있었다. 4장 「레이어 · 팝업」 17건(TC-L-01 ~ 17)으로 모으고
           TC-F-26 · 27 · 29 · 30 · 31 · TC-E-06 · 07 · 08 · TC-U-44 를 이관 · 번호 폐지.
           현행과 어긋난 케이스 4건 정정 — TC-E-06(선택 시점 → 확정 시점) ·
           TC-E-07(취소 시 선택 유지 → 선택 해제 + 재선택 안내) ·
           TC-E-08(온라인신청 전환 → 상담사 개통 전환 후 확정 계속) ·
           TC-F-29(절차 「동의」와 기대 「미동의 기록」이 상충 → TC-L-07 · 08 로 분리).
           집계 정정 — 116건(기능 26 · 예외 7 · 화면 45 · 레이어 17 · 접근성 13 · PC 8) ·
           High 74 · 프로토 확인 가능 95. 종전 KPI 106건은 실제 행 107건과 어긋나 있었다.
        5) **레이어 키보드 모델 구현** — 경로 전환 · 속도 안내 팝업에 Esc 귀결 + Tab 순환을 넣고
           열릴 때 포커스를 레이어 안으로 옮겼다(alertKey · alertFocus). 부록 4에
           「레이어 포커스 순서」(WCAG 2.4.3) · 「레이어 닫기」 2행 신설.
        6) **피그마 원본 버튼** — 라벨의 「새 창」 표기 제거, 좌우 여백 15 → 13px · 아이콘 간격 7 → 6px.
           새 창 안내는 title · aria-label 로 유지.
        7) **선재 결함 2건** — ① F 영역 선택 상태 블록에 여분의 </div> 가 있어 **G 프레임이
           fs-sub-design 밖으로 빠져** 문서정보 · 부록 구성에서도 보이고 있었다 → 태그 정정.
           ② 부록 4의 같은 WCAG 기준을 쓰는 행마다 id 를 생성해 **id 중복**이 나던 문제 → 첫 행만 부여.
Note:   도면 번호 위치는 마크업이 아니라 fsCalign() 이 정한다 — 목업을 고쳐도 열이 어긋나지 않는다.
        H 프레임 목업은 JS로 그려지므로 새 레이어를 추가할 때 FS_POP 에 항목(html · cal)만 더하면 된다.
검증:   목업 **30개** — 번호 순서 불일치 **0** · 콘텐츠/배지 겹침 **0** · 렌더 실패 배지 **0** ·
        콜아웃 ↔ 명세 행 1:1 불일치 **0** · H 프레임 상태 9 / 도면 9 / 명세 66행 / 콜아웃 57 ·
        id 중복 **0** · fs-sub-design 소속 프레임 **10(index · full · A~H)** ·
        18조합 전수 PASS · JS 오류 0 · 카드 360×324.78 불변 · 44px 미만 0 · Sticky 122.2px ·
        420px 가로 스크롤폭 654 (선재 값과 동일) · Esc 귀결 실측(속도 안내 닫힘 · 경로 전환 취소 = 선택 해제)
```

## [2026-08-19] 메뉴 구조 재편 — 변경 이력 1뎁스 분리 · 탭 순서 재배열
```text
Commit: —
Type: feat
Target: index.html — r-tabs 마크업 / R_TABS / rt-log 섹션 신설 / details#fs-log 이동 /
        FS_TIP_TAB / FS_SPY_IDS / fs-nav / 탭 번호 표기 49건 · docs 7종
Change: 1) 「변경 이력」을 화면설계 안의 프레임에서 꺼내 1뎁스 탭(rt-log · 07)으로 분리.
           문서 전체를 대상으로 하는 이력이라 특정 문서 아래 둘 이유가 없다.
           프레임 헤더 PAGE CODE/S-02 → 범위/문서 전체, 기본 펼침,
           화면설계 「영역 이동」 링크 제거, FS_SPY_IDS 에서 fs-log 제거.
        2) 탭 순서 — 01 프로토타입 · 02 화면설계 · 03 기능정의 · 04 개선안 ·
           05 요구사항 정의서 · 06 테스트 케이스 · 07 변경 이력. 기본 진입 탭도 프로토타입.
           동작 → 명세 → 기능 → 근거 → 검증 순으로 읽히게 한 것.
        3) 본문 탭 번호 표기 49건 + 코드 칩 이동 라벨(FS_TIP_TAB) 갱신.
           docs 상태 문서 7종 동반 갱신, 이력 문서(CHANGELOG 등)는 원문 유지.
Note:   id 는 종전대로 유지 — rt-* · fs-log. 앵커 · CSS · 코드 칩 이동이 id 기준이다.
검증:   탭 7개 전환 · 변경 이력 30건 렌더 · 검색/더보기 · 코드 칩 툴팁 라벨 갱신 ·
        변경 이력 탭 내 코드 칩 32개 동작 · 18조합 전수 PASS · 콘솔 0 · 파서 0.
```

## [2026-08-19] 03 기능정의 정리 — 프로토타입 정합 · 누락 반영
```text
Commit: —
Type: docs(03)
Target: index.html — rt-func 1 영역 구성 / STEP 6 · 10 · 10b→13a · 13b · 13c /
        4 예외 처리 EX-03 · 소제목 / 3 노출 데이터 / 5 데이터 연동
Change: 1) 낡은 서술 4건 정정 — 영역 구성 Sticky 행의 「제휴카드 계산 버튼」(C-62로 이관됨) 삭제,
           STEP 10 의 「제휴카드 할인금액(예상) 별도 행」 삭제, STEP 10b → STEP 13a 이동,
           EX-03 행의 발생 조건(STEP 10b) · 버튼 라벨(상담사 개통으로 작성) 갱신.
        2) 03 에 참조 0건이던 C-63(NPay 외부 인증) · C-64(입력 서식) 를 STEP 13c 에,
           C-65(약정 팝업 구성) 참조를 STEP 13b 에 추가.
        3) C-49 셀프개통 불가 제약 라인을 STEP 6 · 영역 구성 · 노출 데이터 · 데이터 연동 4곳에 반영.
        4) 4 예외 처리의 「처리 규칙」 · 「Alert을 두지 않는 근거」에 EX-02 태그 —
           일반 규칙으로 오해되던 절.
검증:   검증 시퀀스 1→1b→2→3→4→4b→5→6→7→8→9→10→11→12→13→13a→13b→13c→14 ·
        C-nn 정의 66건 · 죽은 칩 0 · 18조합 전수 PASS · 시나리오 15건 PASS · 콘솔 오류 0.
미반영: 06 TC-E-06~08 전제 · 01 E 영역 SYS 행 · F 영역 목업 (순차 진행).
```

## [2026-08-19] C-49 셀프개통 불가 표기 · EX-03 확정 시점 이동 · 라벨 정정
```text
Commit: —
Type: feat
Target: index.html — PLANS(P11 limit) · cardHtml(limLine) · pick() · confirmPlan(STEP 13a) ·
        pathYes() · SCN E3 · runScn E3 / 05 C-49 · C-50
Change: 1) C-49 — 셀프개통 불가를 제약 라인으로 상시 표기.
           「셀프개통 신청 불가 — 상담사 개통으로 작성」을 selfBlock 에서 자동 생성하고,
           자격 제한과 함께 걸리면 두 줄로 나란히 둔다. P11 limit 의 중복 문구 제거.
        2) EX-03 트리거를 STEP 10b(선택 시) → STEP 13a(확정 시)로 이동.
           선택 시점에는 팝업이 뜨지 않아 「선택은 그 자리에서 완결」 원칙이 예외 없이 성립한다.
           pathYes 는 전환 후 confirmPlan 을 이어서 호출 — CTA 를 다시 누르게 하지 않는다.
        3) 버튼 라벨 「상담사 개통으로 작성」 → 「상담사 개통하기」 (두 줄 접힘 해소).
Reason: 사용자 지적 2건 — 청소년 요금제의 셀프개통 불가 안내 누락, 확정 시점 팝업 요구,
        버튼 텍스트 2줄 문제.
검증:   콘솔 오류 0 · 시나리오 15건 PASS · 선택 시 Alert 0 · 확정 시 팝업 1 ·
        상담사 개통하기 → S-03 진행 · 취소 → 선택 해제 + 토스트 · 버튼 48px 한 줄 ·
        18조합 전수 PASS · C-nn 66건 결번 0 · 죽은 칩 0.
미반영: 03 STEP 10b → 13a · 06 TC-E-06~08 전제 · 01 E 영역 SYS 행 (순차 진행).
```

## [2026-08-19] 05 개선안 정리 — 최종 프로토타입 기준 정합 · 중복 제거 · 구조 재편
```text
Commit: —
Type: docs(05)
Target: index.html — rt-plan 「라 화면·UI」 표 구조 / C-38 · C-49 · C-51 · C-52 · C-53 ·
        C-60 · C-61 · C-62 · C-63 · C-64 · C-65 · C-66 · C-45 · C-46 · C-47 · C-48 · C-55 · C-57 /
        SCN C53b 설명 / docs/PROJECT_STATE.md
Change: 1) 프로토타입 불일치 3건 정정
           - C-38 터치 타깃 대상에서 「제휴카드 계산 버튼 C-52」 삭제(C-62로 S-02 밖으로 이관됨)
             → 「종속 입력 시트 C-53」으로 교체
           - C-49 원본 Alert 건수가 4건/6종 두 곳에 나뉘어 상충하던 것을 6종 단일 서술로 통합
           - 시나리오 C53b 설명이 C-63(외부 인증 호출)과 모순 — 프로토타입 시트에 입력 칸이
             없는데 「네이버 ID·비밀번호 두 칸 입력」으로 적혀 있었다. 정정.
        2) 단일 출처 정리 — 제휴카드 위치 이관은 C-62, 시트 요소 명세는 01 부록 6,
           약정 팝업 귀결은 C-50, 터치 타깃은 C-38로 모으고 나머지는 참조만 남김.
        3) 정의문에서 개정 이력 서술 12건 제거 — 이력 단일 출처는 01 변경 이력 + CHANGELOG.
           정의문에는 확정일만 유지(8건).
        4) 「라 화면·UI」 24행을 5개 하위 묶음으로 재구성 —
           목록 구성(7) · 카드(7) · 선택·확정(5) · 팝업·레이어(3) · 공통 규격(2).
Reason: 정의문에 변경 이력이 쌓이고 같은 규칙이 여러 행에 반복되면서, 무엇이 현재 확정인지
        읽어서 판단해야 하는 상태가 됐다. C-62(제휴카드 이관) 이후 낡은 서술도 남아 있었다.
Note:   요구사항은 한 건도 삭제하지 않았다 — CLAUDE.md 3절(의미 삭제 금지, 표현만 개선).
        정의 본문 10,491 → 9,409자(-10%).
검증:   C-nn 정의 66건 · 결번 0 · 중복 정의 0 · 죽은 칩 0 · 툴팁 맵 92건 ·
        18조합(6폭 × 3구성) 전수 PASS · 콘솔 오류 0 · html.parser 오류 0.
```

## [2026-08-19 · 14차] 화면설계 프로토타입 기준 정합 · 7탭 상단 통일 · 피그마 바로 가기
```text
Commit: —
Type: fix · feat · refactor
Target: index.html — rt-spec(E 상태2 · 부록 3 · 부록 6) / 7탭 상단 .r-top 신설 /
        .fs-figma 신설 / rt-req KPI 위치

■ 화면설계를 프로토타입 현재 버전으로 맞춤
 1) E 상태 2 — 제약 라인 2줄
    프로토타입은 자격 제한과 셀프개통 불가를 각각 한 줄로 표기하고
    둘 다 걸리면 두 줄이 되는데, 목업은 시니어 1줄만 있었다.
    목업을 청소년 안심 요금제(만 19세 미만 + 셀프개통 신청 불가)로 교체하고
    명세를 01 자격 라인 · 02 경로 라인 · 03 비교 담기 3행으로 재정렬.
    01 Value 에 자격 문구 3종을 열거하고 「요금제명을 반복하지 않는다」를 명시.
 2) E 배지 · 요금제명 — 약정형 · 더알뜰플랜2 표기를 명세에 넣고,
    「약정 조건 배지는 앞자리에 둔다」 규칙 추가(2개 제한에 밀려 가려지면
    선택 전 인지가 무너진다).
 3) 부록 6 — EX-03 을 「선택 시점」에서 「확정 시점」으로 이동.
    프로토타입은 이미 STEP 13a 에서 판정하고 라벨도 「상담사 개통하기」이며
    확인 시 확정을 이어서 진행하는데, 화면설계는 선택 시점으로 서술하고 있었다.
 4) 부록 3 — 「셀프개통 가능 여부」 · 「약정 요금제」 규칙 2행 신설,
    가입자 유형 제약 행을 제약 라인 기준으로 정정.
    해소된 O-17 이 「기획·법무 결정 대기」로 남아 있어 해소 표기로 정정(C-45).

■ 7탭 상단 골격 통일
 상단이 탭마다 달랐다 — 03·05·06·07 은 제목+설명이 있었고 01·02·04 는 없었다.
 KPI 위치도 05 는 제목 위, 06 은 제목 아래로 서로 반대였다.
 일곱 탭 모두 「제목 → 역할 한 문장 → (있으면) KPI」 순서로 맞추고
 01·02·04 에 상단 블록을 신설했다.
 제목에 탭 번호 배지를 붙였다 — 탭바에는 번호가 있는데 본문 제목에는 없어
 스크롤한 뒤 어느 탭인지 위로 올라가 확인해야 했다.

■ 피그마 원본 바로 가기
 02 화면설계 툴바 우측에 새 창으로 열리는 원본 링크를 뒀다
 (node-id=40005698-6576). 문서와 피그마를 왕복하는 동선이 실제로 가장 잦은데
 주소를 따로 찾아야 했다.
 터치 타깃 44px · 접근 가능한 이름 · rel="noopener" 적용(C-38 · C-41).

검증: 7탭 전환 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0 ·
      죽은 코드 칩 0건 · 카드/목록 불변량 유지 · 44px 미만 0건 ·
      변경 이력 34건 · 피그마 버튼 157×44px 우측 정렬
```

## [2026-08-19 · 13차] 05 요구사항 정의서 재구성 — 기능정의와 같은 규칙
```text
Commit: —
Type: refactor · fix
Target: index.html — rt-req 전면(4장 108줄 → 5장 122줄) / 문서 헤더 확정 상태

역할 재정의:
 이 탭이 답할 질문을 「기준」으로 좁혔다 — 왜 고치는가(문제) · 무엇을 지키는가(원칙) ·
 화면 전체에 걸리는 공통 규칙 · 무엇으로 개선을 판정하는가.
 확정 사항 상세는 04 개선안, 요소 명세는 02 화면설계가 단일 출처임을 리드에 명시했다.

그룹 라벨 충돌 해소 (★):
 「바」가 두 탭에서 서로 다른 그룹을 가리키고 있었다 —
 04 의 바는 제휴카드, 05 의 바는 상태·예외 정책.
 05 를 「사 상태·예외 정책 → 아 구현 규칙·접근성」으로 재부여했다.
 종전에는 05 안에서도 사 → 바 역순이었다.

죽은 코드 칩 해소 (★):
 O-23 이 문서에서 2회 참조되는데 정의가 없었다.
 미확정 표에 「제휴카드 유지 여부 → C-62 로 대체」로 추가해 해소.
 전 문서 죽은 칩 0건.

KPI 정정 3건 — 코드가 늘어난 뒤 갱신되지 않은 값들:
 · 확정 사항 KPI            59 → 66건
 · 정의 위치 안내           「44건 중 38건」 → 「66건 중 60건」
 · 문서 헤더 확정 상태      47 → 66

정합 정정 · 중복 제거:
 · 표준 문구의 하단 고정 영역 표기가 구식이었다
   (「{요금제명} 월 {프로모션가}원」) → 「요금제명 + 부가세포함 월 {금액}원」 한 행 (C-51)
 · 미확정 표의 긴 배경 서술은 docs/DECISIONS.md 가 단일 출처이므로
   ID 와 결과만 남기고, 해소 6건(O-21·O-22·O-24~O-27)을 한 행으로 묶었다
 · C-41 의 접근성 구현 나열이 02 부록 4 와 통째로 겹쳐
   원칙 수준으로 줄이고 부록 4 를 단일 출처로 지정
 · 3장 말미의 「가·나·다·라·마 그룹은 개선안 탭에」 문장은
   같은 절 상단 안내와 중복이라 삭제

추가:
 · 표준 문구에 「목록 컨트롤」 행 신설
 · 카피 확정 대기 2건(진입 안내 C-55 · 약정 안내 C-65)을 경고 블록으로 올리고,
   원본 반환금 비율의 음수 부호 확인 건을 함께 적어 카피 확정의 단일 창구로 만들었다

구조: 1 핵심 문제 정의 / 2 개선 원칙 / 3 공통 기준(사·아 + 미확정) /
      4 표준 문구·명칭 / 5 측정 기준

Why:  절 수가 4 → 5 로 늘었지만 이는 흩어져 있던 표준 문구·측정 기준을
      독립 절로 올린 결과다. 찾을 자리를 만드는 것과 내용을 늘리는 것은 다르다.

검증: 7탭 전환 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0 ·
      죽은 코드 칩 0건 · 불변량 유지 · rt-req 표 7개 · 행 33
```

## [2026-08-19 · 12차] 03 기능정의 재구성 — 판정 중심으로 좁히고 중복 제거
```text
Commit: —
Type: refactor
Target: index.html — rt-func 전면(5장 200줄 → 4장 112줄) / rt-req 교차 참조 1건

역할 재정의:
 이 탭이 답할 질문을 「판정」으로 좁혔다 — 무엇을 · 어떤 순서로 · 어떤 조건으로 판단하는가.
 영역별 요소 명세(02 A~G · 부록 4 접근성 · 부록 5 데이터 매핑)와
 개선 근거(04 개선안)는 다른 탭이 단일 출처이므로 참조만 한다.

삭제한 중복 3건:
 1) 1장 「S-02 영역 구성」 표 — 02 의 「화면 구성 · INDEX」와 같은 내용
 2) 3장 「노출 데이터」 전체(카드 · 컨트롤/Sticky · 비교 시트 · S-01 추가 영역 4표)
    — 02 의 A~G 요소 명세와 겹침
 3) 5장 데이터 연동 표의 표기용 데이터 — 02 부록 5 와 겹침

살려서 옮긴 고유 요건:
 · 「주간 순위는 요금제 고유값이라 필터·정렬 후에도 불변(목록 인덱스가 아님)」 → STEP 5
 · 「비교 최대 2개 · 탭 이동 시 유지」 → STEP 11b 신설
 · 데이터 표는 판정에 쓰는 값만 남겨 9행으로 줄이고 신규 연동 1건을 첫 행에 강조

정합 정정 3건:
 1) 3장 Sticky 행의 구식 표기(「{요금제명} 월 {요금}원」 + 별도 금액 행) 삭제
    — STEP 10 과 모순이었다
 2) S-02 개발 범위의 코드 열거(C-13~C-59) → 「04 개선안 다·라·마 그룹 전건」
    — 코드가 늘 때마다 낡는 표기였고 이미 C-62~C-66 이 빠져 있었다
 3) 「범위 밖」 항목(제휴카드 · 사은품 · 동일 IP)을 1장 하단 한 곳으로 모았다
    — 종전에는 세 절에 흩어져 있었다

구조: 1 화면·개발 범위 / 2 판정 시퀀스(진입·선택·확정) / 3 예외 처리 / 4 판정 데이터 조달
      선택 구간에 「제약 판정 없음」을 소제목으로 명시해,
      판정이 진입(표기)과 확정(분기)으로 나뉘어 있다는 설계 의도를 구조로 드러냈다.

Why:  중복은 양이 아니라 신뢰의 문제다. 같은 사실이 두 곳에 있으면
      한쪽이 낡았을 때 어느 쪽이 맞는지 알 수 없다.
      실제로 3장 Sticky 표기가 STEP 10 보다 낡아 서로 모순이었다.

검증: 7탭 전환 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0 ·
      죽은 코드 칩 0건(참조 31종 전건 정의 존재) · 불변량 유지
      rt-func 표 6개 · 행 39 · 코드 칩 83
```

## [2026-08-19 · 11차] 네이버인증 시트 디자인·카피 보완 — C-63 · C-66
```text
Commit: —
Type: fix
Target: index.html — .dep-grp.ext / .dep-extl / .dep-lead / .dep-extw li /
        .dep-exti · .dep-extt / DEP.npay.lead · extLead

디자인 3건:
 ① 외부 구역이 유의사항과 같은 회색이라 「서비스 밖」 신호가 죽어 있었다.
    구역 배경과 파선을 강조 계열로 바꿔 회색 블록과 분리했다.
    문구(주소 · 구역 라벨 · 보조 문장)를 덜어낸 만큼 색과 테두리가 신호를 대신 진다.
 ② 문장이 단어 중간에서 끊겼다(「…진행됩니 / 다.」 · 「…진 / 행됩니다.」).
    리드와 안내 문장에 단어 단위 줄바꿈을 걸어 한 글자만 남는 줄을 없앴다.
 ③ 인증 수단 카드가 비어 보였다 — 칸 높이에 비해 아이콘이 작았다.
    아이콘 26 → 32px, 라벨을 본문 색·굵기로 올려 카드가 하나의 선택지로 읽히게 했다.

카피 2건:
 ④ 안내 문장 — 「네이버 로그인 페이지로 로그인 후 신청서 다음 단계로 진행됩니다.」
              → 「네이버 로그인 페이지에서 로그인을 마치면 신청서 다음 단계로 진행됩니다.」
    조사 정정(장소는 ~로가 아니라 ~에서) + 조건 명시(「로그인 후」는 시점만,
    「로그인을 마치면」은 무엇을 해야 넘어가는지를 말한다).
    강조를 결과에서 사용자가 할 일로 옮겼다 — 결과는 자동으로 일어나고
    사용자가 판단할 것은 앞쪽이기 때문이다.
 ⑤ 맞춤법 — 「돌려 받는」 → 「돌려받는」(「돌려받다」는 한 낱말)

검증: 시트 높이 463 → 477px · 세 시트 공통 골격 유지 ·
      18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃 불일치 0 · 불변량 유지
```

## [2026-08-19 · 10차] 네이버인증 문구 확정 · 군더더기 제거 — C-63
```text
Commit: —
Type: fix
Target: index.html — DEP.npay.lead · extLead / depHtml() ext 분기 /
        .dep-extc 삭제 / 05 개선안 C-63

Change (사용자 확정):
 1) 혜택 문구
    이전: 통신료를 NPay 포인트로 돌려받습니다 — 매월 10% 포인트 혜택.
          혜택 받으실 고객 정보를 확인해 주세요.
    확정: 매월 통신료의 10%를 NPay 포인트로 돌려 받는 혜택
    → 두 문장이 같은 말을 나눠 하고 있었고, 뒷문장은 입력 칸이 없어진 뒤로
      가리키는 대상이 없었다.
 2) 안내 문장
    이전: 네이버 로그인 페이지로 이동합니다.
          인증은 그 페이지에서 끝나고, 신청서는 다음 단계로 진행됩니다.
    확정: 네이버 로그인 페이지로 로그인 후 신청서 다음 단계로 진행됩니다.
 3) 구역 라벨 「이동할 페이지」 삭제 — 안내 문장이 이미 목적지를 말한다.
 4) 보조 문장 「인증 수단은 세 가지 중 하나를 고르면 됩니다」 삭제
    — 아이콘 카드 3칸이 형태로 이미 말한다.

Note: 외부 페이지 신호가 3단서 → 2단서로 줄었다.
      남은 것은 파선 테두리와 목적지를 밝힌 안내 문장이다.

검증: 시트 높이 554.7 → 463px · 18조합 PASS · JS 오류 0 · 태그 0 ·
      콜아웃 불일치 0 · 불변량 유지
```

## [2026-08-19 · 9차] 네이버인증 「이동할 페이지」 구역 재구성 — C-63
```text
Commit: —
Type: fix · refactor
Target: index.html — DEP.npay.extWays / WAY_ICON 신설 / depHtml() ext 분기 /
        .dep-exth · .dep-extb 삭제 / .dep-extw 격자화 / 05 개선안 C-63

Change:
 1) 호출 주소(URL) 표기를 삭제했다. 사용자가 확인할 수 있는 정보가 아니고,
    시트 폭에서 두 줄로 접혀 다른 안내를 밀어냈다.
 2) 인증 수단 3종을 아이콘 카드로 펼쳤다(ID 로그인 · 일회용 번호 · QR코드).
    글자만 있는 칩은 셋이 무엇이 다른지 한눈에 안 잡힌다.
    안내 문장을 한 단계 키우고 「세 가지 중 하나를 고르면 됩니다」 보조 문장을 더해,
    이동한 뒤 무엇을 하게 되는지 먼저 보이게 했다.
 3) 외부 페이지 신호는 유지 — 구역 라벨 「이동할 페이지」 · 파선 테두리 ·
    목적지를 밝힌 안내 문장 세 가지로 대체했다.
    (종전 3단서는 구역 라벨 · 호출 주소 · 파선 테두리)

Why:  주소는 신뢰의 근거처럼 보이지만 실제로는 검증 수단이 아니다.
      대신 '이동한 뒤 무엇을 하게 되는가'를 보여 주는 편이 오인을 더 줄인다.

검증: 시트 렌더 정상 · 아이콘 3종 · 주소 행 0 · 시트 높이 522.7 → 554.7px ·
      18조합 PASS · JS 오류 0 · 태그 0 · 불변량 유지
```

## [2026-08-19 · 8차] 원본 78p 제한 케이스 4건 전건 대응 — C-49
```text
Commit: —
Type: feat · fix
Target: index.html — PLANS(P15 신설) / BADGE_CLASS / 시나리오 C49 /
        05 개선안 C-49 / 06 TC-U-42

Change:
 1) 원본 78p 의 선택 시점 Alert 4건을 C-49 에 전건 대응시켰다.
    원본은 넷 모두 Alert 이다. 앞의 셋(시니어 · 이십세 · 청소년)은
    진행을 막지 않는 안내여서 제약 라인으로 흡수하고,
    넷째(셀프개통 + 청소년)만 개통 방식이 바뀌는 분기라 확인 팝업으로 남겼다.
      시니어  {요금제명}는 만 65세 이상 고객만 가입 가능합니다.
              → 제약 라인 「만 65세 이상 고객만 가입 가능」
      이십세  {요금제명}는 만 19세 이상 ~ 만 29세 이하 고객만 가입 할 수 있습니다.
              → 제약 라인 「만 19세 이상 ~ 만 29세 이하 고객만 가입 가능」
      청소년  {요금제명}는 만 19세 미만 고객만 가입 가능합니다.
              → 제약 라인 「만 19세 미만 고객만 가입 가능」
      셀프+청소년  청소년요금제의 경우 셀프개통 신청이 불가합니다.
                   온라인신청으로 진행하시겠습니까?
              → 확인 팝업 유지. 상담사 개통으로 작성 / 취소 = 선택 해제 (C-50)
 2) 이십세 요금제 신설 — 셋 중 유일하게 프로토타입에 없던 케이스다.
    LTE 이십세 요금제(P15 · 배지 「이십세 전용」)를 추가해
    네 케이스를 모두 눌러 볼 수 있게 했다.
 3) 제약 라인은 요금제명을 반복하지 않는다 — 카드 안에 있어 대상이 이미 분명하다.
    원본의 {요금제명} 치환은 팝업이 카드 밖에 뜨기 때문에 필요했던 것이다.
 4) 정정 — C-49 의 「선택 시점 Alert 5종」 표기가 틀렸다.
    원본 전수는 6종이다(78p 4건 + 데이터 0GB 차단 2건 · 85p · 86p).

검증: 제약 라인 4종 렌더 확인(시니어 · 이십세 · 청소년 · 데이터 0GB) ·
      셀프개통 + 청소년 전환 확인 팝업 동작 ·
      18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃 불일치 0 ·
      카드/목록 불변량 · 44px 전건 유지
```

## [2026-08-19 · 7차] F 영역 SYS 행 중첩 해소 — 좁은 폭 명세 붕괴
```text
Commit: —
Type: fix
Target: index.html — 01 화면설계 F 영역(#fs-f) 선택 상태 마크업

증상: 폭 640px 이하에서 F 영역 선택 상태의 요소 명세가 한 글자씩 줄바꿈되며
      읽을 수 없게 됐다. 실측 값 열 폭 43px.

원인: SYS 행이 직전 명세 행 안에 중첩되어 있었다.
      직전 행이 자기 태그를 닫지 않은 채 SYS 행이 열려,
      바깥 행의 격자(라벨 | 값) 중 값 칸 안에 행 하나가 통째로 들어간 상태였다.
      넓은 폭에서는 값 칸이 충분해 드러나지 않다가 좁아질수록 무너졌다.

범위: 원본부터 있던 결함이며 이번 작업으로 생긴 것이 아니다.
      기준 커밋(c9b8761)에서 동일하게 재현된다.
      문서 전체 전수 검사 결과 중첩 행은 이 1건뿐이었다.

조치: 직전 행을 제자리에서 닫고 SYS 행을 형제로 되돌렸다.

검증: 360 · 420 · 560 · 640 · 700 · 780 · 1180 · 1400 여덟 폭에서
      값 칸 폭 정상(최소 186px @360) · 중첩 행 0건 ·
      콜아웃 ↔ 명세 행 1:1 정합 유지 · 18조합 PASS · 태그 오류 0

교훈: 18조합 렌더링 검증은 JS 오류 · 오버레이 · 탭 전환을 봤지만
      '텍스트가 읽히는가'는 보지 않았다. 좁은 폭에서의 열 폭 하한을
      검증 항목에 추가한다.
```

## [2026-08-19 · 6차] 레이어 여백 · 행간 보정 + 프레임 초과 해소
```text
Commit: —
Type: fix
Target: index.html — .alert.agpane / .alert.ccpane 여백·행간 규칙 /
        프로토타입 전역 line-height / .fs-kind.fix / 05 개선안 C-66 보강

Change:
 ① 배너 아래 빈칸 20px
    .alert p 의 기본 문단 여백이 약정 팝업까지 내려와 히어로 아래에 쓰지 않는
    여백을 만들고 있었다. 규칙 순서상 .alert p 가 .aghero p 보다 뒤에 있어
    이겼기 때문이다. 제휴카드 레이어의 리드 문장도 같은 이유로 12px 가 아니라
    20px 로 벌어져 있었다. 팝업 안의 문단 여백을 .alert 블록 뒤에서
    단독 규칙으로 분리해 해소했다.
 ② 행간 불규칙
    행간을 지정하지 않고 상속값에 기대던 요소가 많아 같은 문단 안에서도
    줄 간격이 어긋났다. 13종을 4단(1.35 · 1.5 · 1.6 · 1.7)으로 정리하고,
    상속에 기대던 요소(.agpn · .agst · .agkey b · .agpv b · .ccmax · .cctier)에
    값을 명시했다. 세로 리듬도 바깥에서 안으로 좁아지게 재배치했다
    (헤더 12 · 히어로 8 · 금액 8 · 요약 12 · 본문 16).
 ③ 프레임 초과 — 기존 결함 해소
    스크롤 영역 높이를 뷰포트 비율(42vh · 36vh)로 잡아 세로가 짧은 기기에서
    레이어가 프레임을 넘고 헤더·버튼이 잘렸다(704 프레임에 726px).
    레이어를 세로 flex 로 두고 프레임 높이를 상한으로 걸어,
    부족한 만큼 스크롤 영역만 줄어들게 했다.
    헤더 · 리드 · 유의사항 · 버튼은 줄어들지 않는다.

Why:  ①②는 값을 안 정해서 생긴 문제이고 ③은 값을 잘못된 기준으로 정해서 생긴
      문제다. 레이어 높이의 기준은 뷰포트가 아니라 그것을 담는 프레임이다.

검증: 664 · 704 · 904 세 높이 × 레이어 3종 — 초과 0 · 버튼 노출 전건 ·
      스크롤 정상. 18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃 불일치 0
      카드 선택 전/후 360×324.78 동일 · 제휴카드 목록 427.5625px 4회 불변 ·
      44px 미만 0건 · 가로 넘침 0

실측: 약정 팝업 767.8 → 740.1px · 히어로 114.8 → 93.6px ·
      제휴카드 레이어 726.1 → 646px(704 프레임) ·
      카드 높이 324.8px · 제휴카드 목록 427.6px · 하단 고정 영역 122.2px
```

## [2026-08-19 · 5차] 타이포 · 간격 스케일 정리 — C-66
```text
Commit: —
Type: refactor
Target: index.html — 프로토타입 CSS 전역(:root ~ 리포트 크롬 직전) /
        05 개선안 C-66 신설 / 변경 이력

Change:
 프로토타입 영역의 값이 제각각이었다. 실측 —
   글자 크기 23종 · 여백(gap) 14종 · 모서리 16종 · 굵기 5종
 아래 단계로 고정하고 값을 스냅했다.
   글자   9단  10 · 11 · 12 · 13 · 15 · 17 · 19 · 22 · 28
   굵기   4단  600 · 700 · 800 · 900
   여백   8단  2 · 4 · 6 · 8 · 10 · 12 · 16 · 20   (3px 이하 미세 정렬은 예외)
   모서리 8단  6 · 8 · 10 · 12 · 14 · 18 · 22 · 99
 조정 건수 — 글자 81 · gap 21 · margin 19 · 모서리 27.
 스케일은 :root 위 주석 블록에 근거와 함께 적어 두었다.

지킨 것:
 선택 상태에서 보더가 굵어지는 요소(.card · .opt · .ccitem · .dep-f input)는
 증가분만큼 padding 을 줄여 크기를 유지한다. 이 보정 쌍이 깨지면
 '선택 전후 크기 불변'(C-26)이 무너지므로, padding 을 정리 대상에서 통째로 제외했다.
 44px 터치 타깃도 스케일보다 우선한다.

Why:  값이 제각각이면 같은 위계의 요소가 화면마다 다르게 보인다.
      사용자는 무엇이 더 중요한지 화면마다 다시 판단해야 한다.
      단계를 좁히는 것 자체가 위계를 말하는 방법이다.

검증: 18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0
      카드 선택 전/후 360×323.73 동일 · 제휴카드 목록 418.5625px 4회 선택 불변
      44px 미만 컨트롤 0건 · 가로 넘침 0
      약정 팝업 · C-53 3종 · 제휴카드 레이어 회귀 PASS

실측 갱신: 카드 높이 325.8 → 323.7px · 제휴카드 목록 420.7 → 418.6px ·
          하단 고정 영역 123.4 → 121.6px
```

## [2026-08-19 · 4차] C-50 ↔ C-65 역할 분리 · 중복 제거
```text
Commit: —
Type: fix · refactor
Target: index.html — 05 개선안 C-50 · C-65 / 01 F 확정 후 분기 SYS / 03 STEP 13b /
        부록 6 확정 시점 / 06 TC-F-29
        docs/REQUIREMENTS.md 3절

Change:
 두 코드가 같은 사실을 두 곳에서 다르게 말하고 있었다. 역할을 갈랐다 —
   C-50 = 팝업을 쓰는 기준과 버튼 귀결 (단일 출처)
   C-65 = 팝업 구성 (위계 · 표 · 배지 · 접근성)
 C-65 의 귀결 재서술을 지우고 C-50 참조로 바꿨다.

발견한 불일치 2건:
 1) 같은 대상을 「실손약정」과 「더 알뜰플랜」 두 이름으로 부르고 있었다.
    「실손약정」은 원본 Page ID(MO9_0301_실손약정 플랜 가입 안내 팝업)에만 있는
    내부 명칭이고, 화면 문구는 「더 알뜰플랜 안내」, 상위요금제 코드는 CONTRACT 다.
    → 「약정형(CONTRACT)」으로 통일.
 2) 귀결 건수가 C-50 세 경우 / C-65 네 경우로 어긋나 있었다.
    C-65 에 Esc 를 더하면서 C-50 을 갱신하지 않은 탓이다.
    → 네 경우(동의 · 동의 안함 · 닫기 · Esc)로 통일하고 반영처를 전수 갱신.

반영처: 01 F 확정 후 분기 SYS · 03 기능정의 STEP 13b · 부록 6 확정 시점 ·
        06 TC-F-29 전제 · docs/REQUIREMENTS.md

Why:  코드를 나누는 이유는 찾을 자리를 하나로 만들기 위해서다.
      같은 사실이 두 곳에 있으면 나눈 게 아니라 늘린 것이다.

검증: 18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0 ·
      「실손약정」 잔존 0건(원본 대조 문서 제외)
```

## [2026-08-19 · 3차] 약정 안내 팝업 2종 신설 — 원본 87p · 88p
```text
Commit: —
Type: feat · fix
Target: index.html — AGREE 재작성 / alertHtml() .agpane 분기 / agKey() 신설 /
        PLANS P05 · P06 · P08 / BADGE_CLASS / .bdg.ctr /
        05 개선안 C-65 신설 / 부록 6 확정 시점 / 06 TC-F-31 신설 / 시나리오 C65

Change:
 1) C-65 약정 안내 팝업 — 원본 87p 더 알뜰플랜(CONTRACT) · 88p 더 알뜰플랜2(CONTRACT2)
    전문을 프로토타입에 반영했다. 종전 AGREE 는 4줄짜리 가안 요약이었다.
    원문은 약관 성격이라 문장을 손대지 않고, 대신 읽는 순서를 만들었다 —
    히어로(무엇인가) → 이 요금제의 정상가 → 약정가 → 핵심 3칸 →
    번호 섹션(원문 1~5 / 1~6) → 반환금 비율 표.
    「정상가 → 약정가」는 원본 1-A(약정할인 반영 금액 출력)를 팝업 안에서 이행한 것이다.
    동의 대상이 곧 금액이므로 스크롤 밖 맨 위에 고정했다.
    반환금 비율은 「0~6개월: 100% / 7~12개월: 50% / …」 한 줄 나열을 표로 되돌렸다 —
    나열 상태로는 자기 구간을 짚기 어렵다. 값은 원문 그대로다.
 2) 요금제 ↔ 약정 매핑 정정 — 원본 대조 결과 LTE (1GB+/통화기본)이
    CONTRACT2 로 잘못 붙어 있었다. CONTRACT 로 바로잡고,
    CONTRACT2 대상인 LTE (7GB+/통화기본)에 약정을 부여했다.
 3) 선택 전 인지 — 요금제명에 [약정형] · [더알뜰플랜2] 표기를 붙이고 카드 배지를 신설했다.
    되돌리기 어려운 조건을 확정 시점에 처음 알리지 않는다는 점에서
    C-49 제약 라인과 같은 원칙이다.
 4) 접근성 — 다이얼로그 역할 선언 · 열릴 때 포커스 이동 · Tab 순환 ·
    Esc = 동의 안함과 같은 귀결(미동의 · 목록 복귀).

Why:  원본 팝업은 한 덩어리 텍스트라 동의 버튼까지 스크롤하는 동안
      무엇에 동의하는지가 흐려진다. 문장을 고치지 않고도 위계만으로 해결된다.

Note: 원본 반환금 비율의 17~20개월 −20% · 21~24개월 −40% 에 음수 부호가 붙어 있다.
      원본 표기 그대로 옮겼으며 부호 확인 대상이다.

검증: 18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0
      팝업 — role=dialog · 핵심 3칸 · 섹션 5/6 · 반환금 표 5행 · 프레임 내 수납(774.1px)
      Esc → 미동의 기록 후 목록 복귀 · 배지 3종 노출 확인
      시나리오 15건 · 회귀(제휴카드 · C-53 3종) PASS
```

## [2026-08-19 · 2차] 휴대폰번호 입력 서식 자동 적용 · NPay 인증 완료 단계 제거
```text
Commit: —
Type: feat · refactor
Target: index.html — phoneFmt() · phoneApply() · phoneKey() 신설 / depInput() / depField() /
        DEP.line1.f1 / DEP.npay / depHtml() / depSubmit() /
        depAuthHtml() · depAuthOk() · depAuthCancel() 삭제 /
        05 개선안 C-64 신설 · C-63 개정 / 부록 6 / 06 TC-F-30

Change:
 1) C-64 입력 서식 자동 적용 — 망내 1회선 번호 칸에서 숫자를 넣는 동안
    화면이 구분자를 넣는다. 10자리 3-3-4 · 11자리 3-4-4, 최대 11자리.
    커서 위치는 '앞에 있는 숫자 개수'로 되짚어 구분자가 끼어들어도 제자리에 남는다.
    구분자 바로 뒤에서 Backspace 를 누르면 구분자만 지워져 아무 일도 없어 보이므로,
    커서를 구분자 앞으로 옮겨 두어 숫자 한 자리가 지워지게 했다.
    안내 문구를 「- 없이 숫자만 입력」에서 「010-0000-0000」으로 교체했다 —
    구분자를 넣지 말라는 지시가 더 이상 필요 없다.
    검증(depFormat · depVerify)은 종전대로 숫자만 추출해 판정하므로 결과가 바뀌지 않는다.
 2) C-63 개정 — NPay 인증 완료 확인 단계를 없앴다. 인증 결과는 인증을 마치는 즉시
    시스템 간 동기화로 반영되므로, 신청서가 같은 사실을 다시 확인시키면 단계만 늘어난다.
    「네이버 ID 확인」을 누르면 인증 페이지를 호출하면서 그대로 다음 단계로 진행한다.
    대신 시트 본문에 「이동할 페이지」 그룹을 두어 누르기 전에 이동처가 보이게 했다 —
    누르고 나서 알게 되면 계정 정보를 어디에 넣는지 오인할 수 있기 때문이다.
    이로써 외부 인증 전용 화면(depAuthHtml)과 인증 완료/취소 핸들러가 사라졌다.

Why:  둘 다 '사람이 하지 않아도 되는 일을 화면이 대신한다'는 같은 원칙이다.
      구분자는 화면이 넣고, 시스템이 이미 아는 사실은 다시 묻지 않는다.

검증: 18조합 PASS · JS 오류 0 · 태그 0 · 콜아웃↔명세 행 불일치 0
      서식 — 11자리 010-1234-5678 / 10자리 011-123-4567 / 문자·구분자 혼입 제거 /
      중간 삽입 시 커서 유지 / 구분자 뒤 Backspace 가 숫자를 지움
      NPay — 입력 칸 0 · 단일 CTA · 클릭 시 시트 종료 + 다음 단계 + 안내 토스트
      회귀 — 망내 1회선 1칸 · 쿠팡 2칸 · 제휴카드 레이어 정상
```

## [2026-08-19] 제휴카드 S-02 밖으로 이관 · NPay 외부 인증 정정 · C-53 공통 스타일 가이드
```text
Commit: —
Type: feat · fix · refactor
Target: index.html — stickyHtml() / viewS03() / bindScroll() / DEP.npay / depHtml() /
        depAuthHtml() 신설 / depSubmit() / depAuthOk() · depAuthCancel() 신설 /
        01 F 영역 · 부록 5 · 부록 6 / 05 개선안 「바」 그룹 신설 / 06 TC-U-46 · TC-F-30

Change:
 1) C-62 제휴카드 이관 — S-02 하단 고정 영역에서 제휴카드 행과 진입 버튼을 걷어내고
    진입 · 선택 · 결과 확인을 가입신청서 작성 내용 확인(마지막 단계)으로 옮겼다.
    근거는 두 가지다. 카드 할인액이 요금제와 무관한 고정액(CARDS[].off)이라
    요금제 간 비교에 쓰이지 않는다는 점, 그리고 상시 노출의 비용이 크다는 점 —
    요금제와 카드를 모두 고른 시점에 하단 고정 영역이 123.4 → 226.8px 로 늘고
    첫 화면 카드 노출이 1.48 → 1.17개로 줄었다(394×704 실측).
    Sticky가 가장 커지는 시점이 다른 요금제와 비교하려는 시점과 겹치는 역설이 결정적이었다.
    스크롤 축약(stickyMini · .sum.mini)은 접을 대상이 사라져 함께 제거했다.
 2) C-63 NPay 정정 — 원본 93p(MO9_0301_L03) 대조 결과 네이버인증 시트에는
    입력 칸이 없고 「네이버 ID 확인」 단일 버튼이 네이버 로그인 페이지를 호출한다.
    원본 슬라이드 우측의 ID · 비밀번호 칸은 신청서가 아니라 네이버 로그인 페이지의 것이며,
    이를 신청서 입력 폼으로 옮겨 적은 것은 오독이었다.
    신청서는 계정 정보를 받지도 저장하지도 않으며, 외부 페이지임을
    구역 라벨 · 호출 주소 · 파선 테두리 세 단서로 화면에 드러낸다.
 3) C-53 세 시트 공통 스타일 가이드 — 골격을
    리드 → 본문 그룹(입력 | 외부 인증) → 유의사항 그룹 → 액션으로 고정하고,
    케이스별 차이는 그룹의 유무로만 표현한다. 영역은 전부 같은 카드 규격을 쓰고
    위계는 배경과 테두리로만 나눈다(입력 = 면 + 실선 / 유의사항 = 음영 /
    외부 인증 = 음영 + 파선). 글자 크기는 여섯 단, 세로 간격은 8의 배수로 고정했다.
 4) 문서 반영 — 05 개선안에 「바 S-02 범위 밖」 그룹 신설 후 C-52 · C-60 · C-61 이관,
    C-51을 2행 고정으로 개정, F 영역 선택 상태 콜아웃 5 → 2 재정렬(명세 행 동시 갱신),
    PC 상태의 제휴카드 레이어 SYS 행 삭제, 부록 6 「요금제 화면 밖에서 수행」에 항목 추가,
    부록 6 상태·접근성에 「공통 골격」·「타입 · 리듬」 2행 신설, TC-U-46을 미노출 검증으로 대체.

Why:  하단 고정 영역은 늘어난 만큼 탐색 영역이 그대로 깎인다. 요금제를 고르는 판단에
      쓰이지 않는 정보를 그 자리에 두면, 이 프로젝트가 하단 바로 해결한 문제
      (확정 직후 74.6% 되돌아감)를 스스로 갉아먹는다.

Note: 코드 충돌 정정 — 작업 중 신규 코드에 C-61을 붙였으나 C-61은 이미
      「제휴카드 영역 노출 조건」으로 정의되어 있었다. C-62로 재부여했다.
      O-23(제휴카드 유지 + 축약)은 C-62로 대체되었다.
```

## [2026-08-18] 프로토타입에도 코드 칩 호버 — C-nn · EX-nn 정의 확인
```text
Commit: —
Type: feat
Target: index.html — fsTipMark() 분리 / fsTipWrap() 신설 / tipw() · tipMark() 헬퍼 /
        initScn() · paintLog() · logHead() · applyDevice() / fsTipInit 재마킹
Change: 리포트 탭에서만 되던 코드 칩 툴팁을 02 프로토타입에서도 쓸 수 있게 했다.
        1) fsTipInit 안에 인라인으로 있던 마킹 루프를 fsTipMark(root) 로 분리 —
           나중에 그려지는 영역도 다시 부를 수 있게 했다. 이벤트는 document 위임이라 그대로 동작.
        2) fsTipWrap(text) 신설 — 이미 이스케이프된 문자열에서 C-nn · EX-nn · O-nn 을
           span.cid 로 감싼다. EX 는 .x, O 는 .d 클래스를 붙여 기존 표기 체계를 따른다.
        3) 적용 지점 — 시나리오 목록(제목 · 설명) · 검증 로그(STEP/코드 · 메시지 · 헤더) ·
           프로토타입 기기 배지(C-03).
        4) 시나리오 목록은 툴팁 모듈보다 먼저 그려지므로 fsTipInit 에서 initScn() 을 한 번 다시 부른다.
Fix:    fsTipInit 말미에 남아 있던 `return n;` 제거 — 마킹 루프를 함수로 빼면서 n 이 사라져
           "n is not defined" 런타임 오류가 나고 있었다(툴팁 초기화가 중간에 끊김).
검증:   콘솔 오류 0 · 배지 1건 · 시나리오 목록 13건 · 검증 로그 칩 마킹 확인 ·
        호버 시 정의 노출 · 클릭 시 정의 탭(rt-plan) 이동 확인 · 18조합 전수 PASS.
```

## [2026-08-18] FOOT STICKY 축소 — 컴팩트 행 + 스크롤 축약 (C-51 개정)
```text
Commit: —
Type: feat
Target: index.html — CSS .sum .ccrow · .sum.mini / stickyHtml() / stickyMini() · bindScroll() /
        closeCards() 포커스 복귀 / 05 C-51 · C-52 / 01 F 영역 명세 · 변경 이력
Problem: 실측 — 미선택 123px → 요금제 선택 184px → 제휴카드 선택 227px.
        목록 뷰포트 623 → 520px (-103px, -17%). 카드 걸침 2 → 1장.
        고른 뒤에도 비교하려고 탐색하는데 그 순간에 탐색 영역이 가장 좁아진다.
        직전 커밋 217px 이므로 이번 변경의 회귀가 아니라 원본부터의 구조적 문제.
Change: 1) 카드명 · 예상 할인액 · 진입을 한 줄로 병합 (.sum .ccrow).
           시각 30px, ::after 로 터치 타깃 44px 확보 — 시각 크기와 터치 타깃 분리.
           227 → 169px, 목록 520 → 577px.
        2) 스크롤 축약 — 아래로 훑는 동안 제휴카드 행만 접어 131px (목록 615px).
           상단 48px 이내는 항상 펼침, 8px 이상 이동 시에만 상태 전환(흔들림 방지).
           요금제명 · 금액 · CTA 는 항상 유지 — 확정 판단에 계속 필요.
        3) 접근성 — 접힘은 visibility:hidden 이라 탭 순서에서 빠지므로,
           Sticky 에 포커스가 들어오면 즉시 펼친다(focusin). prefers-reduced-motion 대응.
Fix:    bindScroll() 이 센티넬(sent-KEY) 없으면 조기 반환해 스크롤 핸들러 자체가 걸리지 않던 문제.
        전체 렌더 완료 시 센티넬이 없어 축약이 동작하지 않았다 — 센티넬 조회를 핸들러 안으로 이동.
        closeCards() 포커스 복귀 셀렉터 .ccbtn → .sum .ccrow.
Limit:  카드 2장 완전 노출은 미도달. 카드 379px(혜택 있음) · 324px(없음), 간격 12px 이므로
        목록 623px 기준 카드 305px 이하가 되어야 한다 → O-18 카드 압축과 함께 가야 함.
        Sticky 축소만으로는 걸침 1 → 2~3장(스크롤 어포던스 확보)까지가 한계.
검증:   콘솔 오류 0 · 18조합 전수 PASS · 시나리오 14건 PASS · 제휴카드 레이어 회귀 PASS ·
        히트 영역 44px · 축약/복귀/포커스 진입 동작 확인.
```

## [2026-08-18] C-53 요금제 종속 입력 시트 재구성 — 프로토타입 + 명세
```text
Commit: —
Type: feat
Target: index.html — CSS .dep-* / DEP · depFormat · depVerify · depBlank · depField · depHtml /
        paintDep · confirmPlan STEP 13c 훅 / depInput · depBlur · depSubmit · depSkip · depClose · depKey /
        PLANS dep 지정 / SCN + runScn / 01 부록 6 신설 절 / 03 STEP 13c / 05 C-53
Change: 원본 MO9_0301_L02 · L03 · L04 의 기능은 그대로 두고 화면만 재구성.
        1) 버튼 라벨을 실제 귀결에 맞춤 — 원본은 1회선 시트의 「취소」가 실제로는 건너뛰기여서
           라벨과 동작이 어긋나 있었다. 「나중에 입력」(건너뛰고 진행) · 「확인/다음/인증」(입력하고 진행) ·
           닫기(진행 중단 · 선택 유지) 세 갈래로 분리하고, 건너뛰기는 가능한 시트에만 노출.
        2) 검증을 두 층으로 분리 — depFormat(형식, CTA 활성 조건 + 포커스 아웃) /
           depVerify(조회, 제출 시점). 화면이 알 수 없는 값으로 CTA를 잠그지 않는다.
        3) 검증 실패를 Alert → 입력 칸 인라인 오류로 이관 (C-50 팝업 최소화).
           아이콘 + 문구 + 보더로 색상 단독 의존 회피 (C-26).
        4) 빈 칸은 포커스 아웃 시 지적하지 않고, 재입력 시작 시 오류 즉시 해제.
        5) 완료·건너뜀을 세션에 기록해 재확정 시 재노출 안 함 (C-12).
        6) 01 부록 6에 「요금제 종속 입력 시트 — 요소 명세」 신설 —
           입력 항목/형식·검증/안내 · 버튼 귀결 · 상태·접근성 8항목.
Reason: 세 시트는 05 C-53에 정의만 있고 프로토타입 0건 · 명세는 서술 1행뿐이었다.
        "기존 기능을 쓰되 화면을 재구성한다"는 사용자 확정에 따라 ① 프로토타입 → ② 명세 순서로 수행.
Note:   C-53 의 「취소/닫기 표준화 판단 필요」가 확정으로 대체되어 O-28 등록이 불필요해졌다.
Fix:    시나리오를 시트 3종으로 분리 (C53a · C53b · C53c). 처음에 C-53 한 케이스만 두어
        망내 1회선만 확인되고 NPay · 쿠팡은 시뮬레이터에서 도달할 수 없었다. 11 → 14건.
검증:   콘솔 오류 0 · node 문법 PASS · 시나리오 14건 전건 실행 ·
        망내 1회선(형식 오류 · 조회 오류 · 정상 제출 · 건너뛰기) · 쿠팡(이메일 형식 blur · CTA 활성) ·
        NPay(Esc 닫기) 동작 PASS · 18조합 전수 PASS · 제휴카드 레이어 회귀 PASS ·
        C-nn 61건 결번 0 · 죽은 칩 0 · 44px 미만 버튼 0.
```

## [2026-08-18] ③ 1차 — 05 개선안 개정 (C-60 · C-61 신설 · 6건 개정)
```text
Commit: —
Type: docs(05)
Target: index.html — rt-plan 「다 프로세스·시나리오」 · 「라 화면·UI」 정의 표 /
        docs/REQUIREMENTS.md · docs/PROJECT_STATE.md
Change: 신설 2건
        - C-60 제휴카드 선택 화면 : 목록 전개 · 최대 할인액 + 실적 구간 병기 ·
          카드명→최대할인→실적구간 3단 위계 · 「선택됨」 텍스트 라벨(색상 비의존) ·
          확인 Alert 없음 · 레이어 내 금액 계산 영역 없음(결과는 Sticky 단일) ·
          유의사항 상시 노출(접이식 금지) · 모달 선언/포커스 이동·복귀·순환/Esc/방향키/
          탭 스톱 1개 · 선택 전후 목록 높이 불변
        - C-61 제휴카드 영역 노출 조건 : 사용 카드 0건 미노출 + 전용 요금제 진입 미노출
          (원본 25.03.05 이관 · 사유 원본 미기재로 확인 필요 명시)
        개정·보강 6건
        - C-48 사은품 3분류(선택형·추첨·선착순) · 선택 액션은 선택형에만 ·
          지급 안내 툴팁 · 전용 진입 시 타이틀/유의사항 치환
        - C-49 원본 Alert 5종의 분배 원칙 — 개통 방식이 바뀌는 것만 팝업,
          정보성 안내(상담사 개통 진행)는 제약 라인에 흡수
        - C-51 Sticky 요금제명+부가세포함 금액 한 행 · 중복 행 제거 ·
          카드 선택 시 카드명+예상 할인 행 추가(미선택 2행 / 선택 3행)
        - C-52 표기 원칙만으로 축소 — 구성은 C-60, 노출 조건은 C-61로 분리
        - C-53 시트 3종 입력 조건 + 취소/닫기 귀결 표준화 판단 필요 명시
        - C-55 적용 범위(판매 채널 신청서) · C-57 대상 경로(S Plus·틴즈·쿠팡)
Reason: C-60 프로토타입이 4차에 걸쳐 완성됐으나 05에 정의가 없어 코드 칩이 죽어 있었음.
        정의의 원본은 05이고 fsTipBuild()는 04·05의 td.id > span.cid 만 수집하므로,
        ③ 단계는 05부터 시작해야 나머지 문서의 참조가 살아난다.
        원본 slide 75~93 전문화로 드러난 누락(S Plus 미노출 조건 · 사은품 3분류 ·
        시트 입력 조건 · 진입 안내 적용 범위)도 함께 반영.
Note:   시트 취소/닫기 표준화는 판단이 필요한 사안이라 O-nn 코드를 붙이지 않고
        서술로만 남겼다 — 04 탭에 O-28을 등록하기 전에 칩을 쓰면 죽은 칩이 된다.
검증:   C-nn 정의 61건 · 결번 0 · 중복 정의 0 · 죽은 칩 0 · C-60 툴팁/이동 동작 확인 ·
        18조합(6폭 × 3구성) 전수 PASS · 콘솔 오류 0 · 6개 탭 전환 정상.
```

## [2026-08-18] 원본 요금제 구간 전문 문서화 — `docs/ORIGIN_MO9_0301.md` 신설
```text
Commit: —
Type: docs
Target: docs/ORIGIN_MO9_0301.md (신설) · docs/origin-slides/slide-075~093.png (19장, 3.0MB) ·
        CLAUDE.md · docs/ORIGIN_GAP.md · docs/PROJECT_STATE.md
Change: 원본 화면설계서 v27.6 의 slide 75~93 (요금제 목록 · 팝업 · 확정 이후 bottomsheet)을
        도형·표·좌표 단위로 전량 추출해 전문 문서로 구성.
        - 슬라이드별 정의 (번호 체계 1 / 1-A … 원본 그대로)
        - 팝업 · Alert 문구 14종 전문
        - 약정 안내 팝업 본문 2종 전문 (CONTRACT · CONTRACT2)
        - CMS 관리 항목 매핑 · 요금제/모델/POS/화면 코드 전수
        - 현 S-02 설계서 C-nn 대응표
        - 원본 표기 문제 10건 (미교정 · 기록용)
        슬라이드 이미지를 함께 보관해 레이아웃·주석 위치까지 pptx 없이 확인 가능.
Reason: 원본 pptx(9.8MB · 364 slides)를 매 세션 다시 열어 확인하는 비용 제거.
        ORIGIN_GAP.md 는 '판단' 문서이고 원문 사본이 없어, 문구·코드 확인 때마다 원본이 필요했음.
Impact: 문서만 추가. index.html 변경 없음. CLAUDE.md 읽기 순서에 8번으로 편입.
```

## [2026-08-18] C-60 (4) 유의사항 상시 노출 · 레이어 금액 영역 삭제 · 접근성 보강
```text
Commit: —
Type: feat
Target: index.html — alertHtml cardPick 블록 / stickyHtml() / paintCardPick() /
        openCards() · closeCards() / ccKey() 신설 / CSS .ccnote · .cctt · .ccpane :focus-visible ·
        .ccbtn.wide · .sum .amt .lb.nm2
Change: 사용자 확정 3건.
        1) 유의사항 — details/summary 접이식을 제거하고 <section> + <h3> 로 상시 노출.
           조건을 접힘 뒤에 두면 선택 전에 읽히지 않는다. 항목 3 → 4건
           (월 납부 금액 미합산 주석을 유의사항으로 흡수).
        2) 레이어 안의 금액 계산 영역(.ccsum / .ccfoot / .cccap) 전량 삭제.
           선택 결과는 목록 화면 Sticky 한 곳에서만 확인한다.
        3) Sticky 재구성 — 미선택: 「제휴카드 할인 선택」 버튼만.
           선택: 「카드명 / - 할인금액」 행 + 「제휴카드 할인 변경」 버튼.
        4) 접근성 — role=dialog 에 aria-modal · aria-labelledby 부여, 타이틀을 h2 로.
           ccKey() 신설: Esc 닫기 · 방향키/Home/End 로 라디오 이동(WAI-ARIA radiogroup) ·
           Tab 을 레이어 안에서 순환(포커스 트랩).
           openCards 는 선택된 항목으로 포커스 이동, closeCards 는 진입 버튼으로 복귀.
           roving tabindex — radiogroup 전체가 탭 스톱 1개.
           :focus-visible 링(키보드에만 노출). 유의사항 본문 대비를 muted-2 → muted 로 상향.
           Sticky 진입 버튼을 실제 44px 로 (::after 히트 영역 대신).
Impact: Sticky 행 수 — 미선택 2행 / 선택 3행. C-51 · C-52 정의와 06 TC-U-45 · TC-U-46 갱신 대상.
        레이어에서 「선택 즉시 하단 반영」이 사라졌으므로 05 C-60 정의를 쓸 때
        결과 확인 경로를 'Sticky 단일'로 기술한다.
검증:   콘솔 오류 0 · 시나리오 11건 PASS · 6폭(1400~420) 넘침 0 · 목록 높이 420.671875px 4회 불변 ·
        44px 미만 버튼 0건 · 명암비 8항목 전건 WCAG AA(4.5:1) 통과(최저 4.66) ·
        키보드 Esc / 방향키 / Home / End / Tab 순환 / 포커스 복귀 전건 동작.
```

## [2026-08-18] C-60 (3) 제휴카드 선택 화면 — 정보 위계 강화
```text
Commit: —
Type: feat
Target: index.html — CSS .ccnm · .ccmax · .cctiers · .ccsel · .ccsum · .ccfoot · .cccap ·
        .ccnote · .ccpane .btns / alertHtml cardPick 블록
Change: 방향 = 정보 위계 강화 (사용자 확정) · 범위 = 제휴카드 레이어만 (사용자 확정)
        1) 카드 행 3단 위계 — 카드명(주) → 최대 할인액(강조, 단독 행) → 실적 구간(부속)
           실적 구간을 배경 박스로 묶어 카드명·최대 할인액과 한 단계 분리.
           선택 시 박스 배경을 surface 로 바꿔 ghost 배경 위에서도 층이 남게 함.
        2) 결과 패널 .ccsum 신설 — 예상 할인금액 · 할인 적용 시 월 금액 · 주석을
           하나의 패널로 묶어 '선택의 결과'임을 시각적으로 분리. 할인액 17px/900 로 승격.
        3) 리드 문구 1문장으로 축약, 유의사항 위에 구분선, CTA 상단 여백 확보.
Fix:    「선택됨」 라벨을 display:none → visibility 로 전환.
        선택할 때 라벨이 이름 행에 끼어들어 행이 접히면서 목록 높이가 385.9 → 410.8px 로
        변하는 문제가 실제로 발생(U+ 라이프 신한카드). 자리를 항상 잡아 높이를 고정.
        같은 이유로 최대 할인액 칩을 이름 행에서 단독 행으로 되돌림 — 카드명 길이에
        따라 행이 접히는 구조 자체를 없앰.
Impact: 02 프로토타입만 변경. 목록 높이 기준값이 368.5625 → 420.671875px 로 바뀌었으므로
        ② 목업·명세에 옮길 때 이 값을 쓴다.
검증:   콘솔 오류 0 · 시나리오 11건 전건 PASS · 카드 4회 선택 내내 목록 높이 420.671875px 불변 ·
        6폭(1400~420) 넘침 0 · 모바일 430px 에서 목록 스크롤 + 결과 패널·CTA 상시 노출 확인.
```

## [2026-08-18] C-60 (2) 제휴카드 선택 화면 형식화 · Sticky 할인 라인 제거 · 시나리오 8건 추가
```text
Commit: —
Type: feat
Target: index.html — CSS 289줄~(ccpane · ccx · ccsel · ccbtn.wide) · alertHtml cardPick 블록 ·
        stickyHtml() · SCN 배열 + runScn() 분기 · 시나리오 패널 안내 문구
Change: 1) 제휴카드 레이어를 '계산기'에서 '선택 화면' 형식으로 고도화
           - 타이틀 「제휴카드 할인 계산」 → 「제휴카드 선택」
           - 헤더 신설 — 좌측 정렬 타이틀 + 44px 닫기(×, aria-label 부여)
           - 리드 문구를 .body 밖으로 빼 스크롤과 분리, 월 납부 금액 불변을 명시(C-51)
           - 선택 행에 「선택됨」 텍스트 라벨 추가 — 색상에 의존하지 않는 단서 (C-26)
           - CTA 라벨 「확인」 → 「선택 완료」
        2) Sticky 금액 행 정리 — 「제휴카드 할인금액(예상)」 행과
           「부가세포함 월 납부 금액」 행을 **둘 다 제거**.
           후자는 1행의 「월 N원」과 같은 금액을 한 번 더 말하는 중복이었으므로,
           라벨을 1행 금액 앞에 붙여(「부가세포함 월 38,200원」) 의미만 남기고 행을 없앴다.
           결과 Sticky = 요금제명·금액 1행 + 제휴카드 진입 버튼 1행 = **2행 유지(C-52)**.
           진입 버튼만 유지
           (미선택 「제휴카드 할인 확인」 / 선택 「제휴카드 할인 변경」)
           ::after 로 히트 영역만 44px 확보해 시각 높이는 38px 유지 (C-38)
        3) 시나리오 시뮬레이터 3건 → 11건. 「조건」 태그(.tag.c) 신설.
           추가 — C-49 자격 제한 · C-50 약정 동의 · C-55 진입 안내 · C-57 단독 노출 ·
           C-58 속도 안내 · C-60 제휴카드 선택 · EX-03 경로 전환 · EX-04 동일 IP
Reason: 사용자 요청 3건. Sticky와 레이어 두 곳에 같은 예상 할인액이 있어 월 납부 금액이
        할인 반영값으로 오인될 여지가 있었음. Sticky는 요금제 기준 금액만 말하고(C-51),
        예상 할인액은 선택 화면 안에서만 보여 주도록 단일화.
        C-48~C-60으로 늘어난 조건부 화면을 시뮬레이터에서 직접 확인할 수단이 없었음.
Impact: 02 프로토타입만 변경. 01 화면설계 · 03 · 04 · 05 · 06 여전히 미반영.
        Sticky 구성이 「요금제명 + 부가세포함 금액」 1행 + 「제휴카드 진입 버튼」 1행으로 바뀌었다.
        ③ 단계에서 01 F 영역 명세 · 05 C-51 · C-52 정의 · 06 TC-U-45 · TC-U-46 갱신 필요.
        01 화면설계 F 목업은 아직 구 구조(.sum .amt 4곳 · .ccbtn 2곳)를 쓰고 있으므로
        관련 CSS는 삭제하지 않고 남겨 두었다 — ② 단계에서 목업과 함께 정리한다.
검증:   Chromium 콘솔 오류 0 · node --check PASS · 시나리오 11건 전건 실행 PASS ·
        레이어 선택 4회 목록 높이 368.5625px 불변 · 재진입 선택 유지 · × 닫기 동작 ·
        6폭(1400/1280/1180/1000/768/420) 넘침 0 · 44px 타깃 전건 충족.
        18조합 리포트 검증은 ②③ 완료 후 일괄 실행 예정.
```

## [2026-08-18] C-60 제휴카드 선택 화면 개선 — ① 프로토타입까지 완료 (②③ 미완)
```text
Commit: —
Type: feat
Target: index.html — 02 프로토타입 (CSS 251줄~ · CARDS 2515줄 · alertHtml 2677줄~ · paintCardPick 2803줄 ·
        openCards/pickCard/closeCards 3284~3296줄) / docs/RESUME_C60.md 신설
Change: 제휴카드 선택 팝업을 재설계.
        1) 카드 목록 전개 — 셀렉트·캐러셀 해체, role=radiogroup + 4행(카드 3 + 선택 안 함)
        2) 실적 구간별 할인액 병기 — CARDS 데이터를 tiers[] 구조로 분리
        3) 팝업 하단 즉시 반영 — 제휴카드 할인금액(예상) + 요금제 할인 적용 시 월 금액 2행
        4) 확인 Alert 제거 — 되돌릴 수 있는 선택이라 C-50(2버튼 확인) 대상 아님
        5) 유의사항 접이식 분리 (기본 접힘 · summary 44px)
        paintCardPick() 신설 — 선택할 때마다 render()를 부르면 .alert의 pop 애니메이션이
        재생되어 레이어가 튀는 문제를 부분 갱신으로 해소.
Reason: 사용자 확정 3건(목록 전개 / 하단 즉시 반영 / 확인 Alert 제거). 원 서비스 팝업은
        고른 결과가 월 납부 금액에 어떻게 반영되는지 팝업 안에서 확인할 수 없어,
        이 프로젝트의 핵심 문제(C-07 · C-20)와 같은 결함이 팝업 안에서 반복되고 있었음.
Fix:    .alert button(0-1-1)의 CTA 그라데이션이 .ccitem(0-1-0)에 상속되어 미선택 카드가
        보라색으로 칠해지던 문제를 .alert button.ccitem 스코프로 교정.
        보더 1→2px 증가분만큼 padding 1px 축소 — 목록 높이 368.5625px 선택 전후 불변.
Impact: 02 프로토타입만 변경. 01 화면설계 · 03 · 04 · 05 · 06 미반영.
        C-60 정의 행이 아직 없어 코드 칩이 죽어 있음 — ③에서 05 개선안에 정의 필요.
검증:   Chromium 콘솔 오류 0 · node --check PASS · 동작 8항목 PASS ·
        PC 1400/768/420 3폭 넘침 0. 18조합 검증은 ②③ 완료 후 일괄 실행 예정.
중단:   사용량 한도로 세션 중단. 이어받는 절차는 docs/RESUME_C60.md.
```

## [2026-08-17] 작업 순서 규칙 확정
```text
Commit: —
Type: docs
Target: CLAUDE.md · HANDOFF.md
Change: 새 요청 처리 순서를 ① 프로토타입 → ② 화면설계 → ③ 이외 문서로 고정.
        단계별 완료 기준과 순서의 근거, 건너뛸 수 있는 예외를 명시.
Reason: 목업이 프로토타입 CSS를 재사용하는 구조라 순서를 뒤집으면 목업을 두 번 그리게 되고,
        구조적 제약(버튼 중첩 등)이 명세 확정 이후에 발견되는 문제가 있었음.
Impact: 문서만 변경. index.html 무변경.
```

## [2026-08-17] 제휴카드 표기 정정
```text
Commit: —
Type: fix
Target: index.html — F 프레임 · 02 프로토타입 · 03 · 05 · 06 / docs/ORIGIN_GAP.md · docs/PROJECT_STATE.md
Change: "제휴카드 할인포함 월 납부 금액" 라벨 전환 삭제. 할인을 월 납부 금액에 합산하지 않고
        "제휴카드 할인금액(예상)" 행으로 분리 표기. 계산 버튼을 라벨 옆으로, 기본값 표기 0원.
Reason: 원 서비스 실제 화면에는 라벨 전환이 없음. 제휴카드 할인은 카드사 별도 신청이 필요한
        예상 금액이므로 합산하면 실제 청구액으로 오인될 수 있음.
Validation: 18조합 PASS · 카드 적용 후 월 납부 금액 불변 실측 확인
```

## [2026-08-17] 원본 누락 전건 반영 — C-48 ~ C-59
```text
Commit: —
Type: feat / fix / docs
Target: index.html — 01 화면설계 · 03 기능정의 · 04 요구사항 · 05 개선안 · 06 테스트 케이스
Change:
  확정 12건 추가 — C-48 사은품 단계 · C-49 자격 제한 표기 · C-50 조건부 팝업 · C-51 Sticky 금액
  · C-52 제휴카드 축약 · C-53 종속 입력 · C-54 IP 제한 · C-55 진입 안내 · C-56 목록 분기
  · C-57 단독 노출 모드 · C-58 속도 안내 · C-59 프로모션 문구 높이
  C-28 · C-33 · C-40 문구 보정 (사실관계)
  부록 6 「팝업 · 분기 정의」 신설 · 예외 EX-03 · EX-04 · STEP 1b · 4b · 10b · 13b · 13c
  테스트 케이스 92 → 106건
Reason:
  원본 MO9_0301에는 목록 화면 외에 팝업·분기·금액 명세 계층이 함께 정의되어 있었으나
  S-02 재설계 과정에서 이관되지 않았음. O-22 ~ O-27을 권고안으로 해소하고 전건 반영.
Design:
  개선 원칙에 맞춰 처리 형태를 재정 — 정보 전달용 Alert 5종을 카드 제약 라인으로 내리고
  경로 전환·약정 동의만 팝업으로 남김(C-18 · C-26 보존). 제휴카드는 텍스트 버튼으로 축약해
  Sticky 2행을 유지(O-18 악화 방지). 전용 요금제 진입은 탭 미노출로 정의.
Impact:
  줄수 3402 → 3534 · 콜아웃 재정렬 없음(신규 요건 전부 SYS 행) · A~G PAGE CODE 불변
Validation: V-2026-08-17-02 — 18조합 전수 PASS · 이슈 0 · 신규 칩 12종 동작 확인
```

## [2026-08-17] C-48 확정 — 사은품 선택 단계 위치
```text
Commit: —
Type: docs
Target: docs/ORIGIN_GAP.md · docs/DECISIONS.md · docs/PROJECT_STATE.md · docs/NEXT_ACTIONS.md
Change:
  O-21 해소 → C-48 확정. 사은품 선택 bottomsheet를 가입 방법 선택(S-04) 직후에 배치하고,
  선택할 사은품이 있을 경우에만 노출하도록 확정(허지원 책임).
  제안 코드를 C-49~C-59로 시프트. 파생 결정 대기 O-26 · O-27 신규.
Reason: 프로세스 재배치(C-24)로 사은품 노출 조건(개통방식+가입유형+요금제)을 요금제 선택 시점에 확정할 수 없었음.
Impact: 문서만 변경. index.html 미반영 — 04/05 탭 수정이 필요해 작업 범위 확인 후 진행.
Validation: 원본 상대 순서(가입유형 → 사은품 → 가입자유형) 보존 여부 대조 확인
```

## [2026-08-17] 원본 화면설계서 대조 — 누락 정의 식별
```text
Commit: —
Type: docs
Target: docs/ORIGIN_GAP.md (신규) · docs/PROJECT_MAP.md · docs/NEXT_ACTIONS.md · docs/PROJECT_STATE.md
Change:
  뉴다이렉트몰화면설계서 MOBILE 1.유심 가입신청서 v27.6 (364 slides) 중
  MO9_0301 요금제 선택(slide 75~91) · MO9_0800(slide 74)을 현재 S-02 명세와 항목 단위 대조.
  누락 24건 · 기존 확정 사항과의 충돌 3건 식별. 제안 C-48~C-58 · O-21~O-25 기술.
Reason:
  S-02 재설계가 목록 화면의 탐색·선택 UX에 집중하는 동안, 원본에서 요금제 선택에
  딸려 있던 팝업·분기·금액 명세 계층이 이관되지 않았음.
Impact: 문서 신규 1건. index.html 무변경. 전 항목 AI 제안 상태 — 확정 전.
Validation: 원본 slide 74~91 전문 대조 · index.html 전문 grep 교차 확인
```

## [2026-08-17] index.html — 추적성 · 표기 기준 보완
```text
Commit: —
Type: fix / docs
Target: index.html — 문서 사용 안내 · E 영역 · 부록 2 · 부록 4 · 변경 이력
Change:
  - C-26 추적성 태그 부착 3곳 (E 선택 상태 명세 · 부록 2 상태 정의 · 부록 4 접근성 명세)
  - 표기 교정 정책 명문화 — 피그마 컴포넌트·프레임 명칭 원문 유지, 일반 서술문만 교정
  - C-nn 건수 표기 정정 (44건 중 38건 → 47건 중 41건)
  - O-nn 설명에 결정 대기 0건 · O-18 이월 상태 반영
  - 변경 이력 1행 + E 프레임 HISTORY 1행 추가
Reason:
  C-26이 내용상 반영되어 있으나 코드 태그가 없어 요구사항 개정 시 영향 추적에서 누락.
  Worning·LABLE·Abled가 원문 유지인 이유가 문서에 없어 오타로 오인될 소지.
  건수 표기가 C-45~C-47 추가 이전 값으로 남아 있었음.
Impact: diff 6청크 · +4줄. 구조·스크립트 무변경.
Validation: V-2026-08-17-01 — 6폭 × 3구성 18조합 전수 PASS · 이슈 0
```

## [2026-08-17] 문서 계층 정비 — 최신 기준 재동기화
```text
Commit: —
Type: docs
Target: CLAUDE.md · HANDOFF.md · README.md · MIGRATION.md · docs/* · .gitignore
Change:
  - 문서 기준을 febb81a → aa654f0으로 갱신 (5탭·C-44·결정대기 3건 → 6탭·C-47·결정대기 0건)
  - docs/REQUIREMENTS.md 3절에 C-01~C-47 전문 47건 수록, 4절 추적성 매트릭스 신설
  - docs/MARKUP.md 신규 — rt-spec 마크업 규약 분리
  - 문서 일체를 project_structure/ 하위에서 레포 루트로 이동
  - .gitignore 신설 — _to_delete/ · *.zip · 임시 파일 제외
Reason:
  기존 문서가 febb81a 스냅샷 기준이라 06 테스트 케이스 탭 · C-45~C-47 · O-nn 해소가 누락된 상태였음.
  문서가 전제하는 경로(루트)와 실제 위치(하위 폴더)도 어긋나 있었음.
Impact: 문서 계층만 변경. index.html 무변경.
Validation: C-01~C-47 전수 존재·중복 0 · 정의 위치 41/6 실측 일치 · 문서 간 참조 링크 dead 0
```

## [2026-08-14] 문서 계층 도입
```text
Type: docs
Change: HANDOFF 단일 문서를 AI 운영 문서 계층으로 전환.
Reason: 세션 간 프로젝트 지식 유실 방지.
```

## 기준 커밋 이력 (Git)

| 커밋 | 날짜 | 내용 |
|---|---|---|
| `aa654f0` | 2026-08-14 | feat(06): 수행 기록 시트 링크 연결 |
| `3463775` | 2026-08-14 | **feat(06): 테스트 케이스 탭 신설 — 92건** |
| `dbef182` | 2026-08-14 | fix(01): 호버 · 포커스 전수 정비 |
| `10eac66` | 2026-08-14 | fix(01): 호버 스타일 보정 |
| `6de4794` | 2026-08-14 | **docs(01·04·05): O-19 해소 — 비교 시트 상세 확정(C-47) · 결정 대기 0건** |
| `49ccea5` | 2026-08-14 | fix(01): 전수 재검사 후 변경 이력 헤더 보정 |
| `5c81492` | 2026-08-14 | **docs(04·05): 미확정 사항 정리 — O-17 · O-20 해소(C-45 · C-46), O-18 이월** |
| `97d90b8` | 2026-08-14 | fix(01): D PC 필터 패널 렌더 오류 수정 · 현재 영역 표시 추가 |
| `1d182ae` | 2026-08-14 | feat(01): 코드 칩에서 정의 위치로 이동 · WCAG 기준 확장 |
| `15e93a0` | 2026-08-14 | feat(01): 코드 칩 호버 상세 노출 |
| `9b9b7c5` | 2026-08-14 | docs(01): SCREEN 기기 라벨 추가 |
| `4bcc7d3` | 2026-08-14 | docs(01): PC 상태 재정비 |
| `febb81a` | 2026-08-13 | PC 화면설계 추가 — 영역별 PC 상태 · PC 전체 화면 도면 · 레이아웃 규격 보강 |
| `28cb895` | 2026-08-13 | 구성 전환 시 sticky 툴바 고정 · 콜아웃 넘버링 1:1 정합 · 변경 이력 초기화 |
| `9fff433` | 2026-08-13 | 변경 이력 역순 · 검색 · 더보기 · Worning/Error Message 통합 정의 |
| `cf214f5` | 2026-08-13 | 화면설계 탭을 피그마 프레임 형식으로 전면 재구성 |

## 기록 원칙
새 변경은 다음 형식으로 기록합니다.

```text
[YYYY-MM-DD]
Commit:
Type: feat / fix / docs / refactor
Target:
Change:
Reason:
Impact:
Validation:
```

기존 이력의 상세 사실은 Git 커밋을 기준으로 확인합니다. 문서에 없는 내용을 추정해서 추가하지 않습니다.
