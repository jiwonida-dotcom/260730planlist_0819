# NEXT_ACTIONS — 다음 작업

> 2026-08-17에 기존 우선순위 1~3을 처리했습니다. 이력은 아래 「완료」 절 참조.

## 우선순위 -3 — 04 요구사항 정의서 KPI 갱신 ★★ (다음 작업)

2026-08-19 이관으로 확정 건수와 그룹이 바뀌었습니다.

| 항목 | 갱신 값 |
|---|---|
| KPI 건수 | 확정 사항 61 → **66건** |
| 정의 위치 안내 | **"66건 중 60건"** (04 탭 `r-note` 블록) |
| 그룹 | 05 개선안에 **「바」 S-02 범위 밖** 그룹 신설 — 4건(C-52 · C-60 · C-61 · C-62) |
| `C-38` 터치 타깃 | 서술의 "제휴카드 계산 버튼 C-52"를 **S-02 밖 항목으로 표기**하거나 목록에서 제외 |
| 결정 대기 KPI | **0건 유지** (O-23은 C-62로 대체 · O-18 이월) |

## 우선순위 -2.5 — 가입신청서 작성 내용 확인 화면 설계 ★★

`C-62`로 제휴카드가 이 화면으로 옮겨졌습니다. **해당 화면의 상세 설계는 본 프로젝트(S-02) 범위 밖**이므로,
넘길 내용을 정리해 인계해야 합니다 — 진입 조건(`C-61`) · 레이어 구성(`C-60`) · 표기 원칙(`C-52`) · 결과 행 구성.
프로토타입에는 축약 형태로만 들어가 있습니다(`viewS03()`).


## ~~우선순위 -2 — ③ 2차 `04 요구사항 정의서`~~ (2026-08-19 우선순위 -3으로 대체 · 건수 61 → 63)

05 개선안 1차는 **2026-08-18 완료**(`C-60` · `C-61` 신설, 6건 개정). 이어서 04를 처리합니다.

| 항목 | 내용 |
|---|---|
| KPI 건수 | 확정 사항 59 → **61건** |
| 정의 위치 안내 | "44건 중 38건" → **"61건 중 55건"** (04 탭 `r-note` 블록) |
| ~~`O-28` 등록~~ | **불필요 — 2026-08-18 확정으로 대체.** 시트 3종의 라벨·귀결을 `C-53`에 확정으로 기술했습니다 |
| 공통 기준 | `C-38` 터치 타깃 서술에 제휴카드 **선택 화면**과 **종속 입력 시트** 반영(현재 "제휴카드 계산 버튼") |
| 결정 대기 KPI | **0건 유지** |

이후 순서 — 3차 `03 기능정의` → 4차 `06 테스트 케이스`(`TC-F-30` 1건 → 시트 3종 분해 · C-60 케이스 추가) → `01 화면설계`(② 제휴카드 레이어 목업).

## ~~우선순위 -1 — `C-60` ② 화면설계 · ③ 이외 문서 반영~~ ✅ **2026-08-19 완료 (이관으로 대체)**

> `C-62` 이관 확정으로 **S-02에 제휴카드 목업을 두지 않습니다.** F 영역과 부록 6에는 이관 사실만 남겼고,
> 레이어 목업은 **작성 내용 확인 화면 설계**로 넘어갔습니다. 아래는 이력입니다.

제휴카드 선택 화면 개선. **① 프로토타입은 완료**되어 `index.html`에 반영돼 있고, **② ③이 남아 있습니다.**

| 단계 | 남은 일 | 판단 필요 |
|---|---|---|
| ② 화면설계 | 레이어 목업 + 요소 명세 | **목업 위치** — F 영역(`#fs-f`) vs 부록 6 「팝업 · 분기 정의」 / **콜아웃 체계** |
| ③ 이외 문서 | ~~05 `C-60` 정의 신설~~ **2026-08-18 완료** · 04 KPI 61건 + O-28 · 03 STEP · 06 케이스 · `docs/` | 없음 |

전체 절차 · 코드 위치 · 검증 결과는 **`docs/RESUME_C60.md`**에 있습니다.

## 우선순위 0 — 팝업 목업 작성 ★
원본 대조로 확인된 누락은 **2026-08-17 전건 반영 완료**(C-48 ~ C-59). 남은 것은 **목업**입니다.

`docs/ORIGIN_GAP.md` 11절 참조. 아래 5건은 **조건 · 형태 · 동작까지 정의**되어 있고 목업만 없습니다.

| 코드 | 대상 | 비고 |
|---|---|---|
| C-49 | 카드 제약 라인 | 스펙 면 하단 배치 |
| C-50 | 경로 전환 확인 · 약정 동의 팝업 | **약정 조건 카피 확정 선행** |
| ~~C-52~~ | ~~제휴카드 계산 버튼 · 레이어~~ | **2026-08-19 S-02 범위 밖으로 이관(C-62)** — 목업은 작성 내용 확인 화면 설계에서 |
| C-55 | 진입 안내 팝업 | **법적 고지 문구 — 원문 유지** |
| C-58 | 속도 안내 레이어 | 44px 터치 타깃 · 접근 가능한 이름 |

문구가 법적 고지·약정 조건에 걸린 항목이 있어 **카피 확정 후 디자인 시안 단계**에서 진행하는 것이 안전합니다.

## 우선순위 1 — A~G 프레임 접이식 적용 여부 검토
현재 A~G는 항상 펼침입니다. 프레임 7개 × 상태 23개로 문서가 길어 접이식 전환을 검토할 만합니다.
적용 시 `<details class="fs-frame">` + `<summary class="fs-fhead">` 패턴을 씁니다(`docs/MARKUP.md` 2절).
**선택 사항이며 사용자 확정 후 진행합니다.**

## 우선순위 2 — Figma 원본 확인

### 오타 5건 (문서는 교정 / 피그마 원본은 미수정 — 허지원 책임)
| 원문 | 교정 |
|---|---|
| `Compoent` | Component |
| `Hisotry.back()` | History.back() |
| `NO ACION` | NO ACTION |
| `FOOT STICIKY` | FOOT STICKY |
| `E. 요금제 코드` | E. 요금제 목록 카드 |

### 오류 1건
`B. 타이틀` 노출 조건이 "상단 고정"으로 되어 있으나 실제는 **스크롤**입니다(A에서 복사된 것으로 판단).
문서는 스크롤로 표기했습니다. **원본 확인 필요.**

> 문서 교정과 피그마 원본 수정 여부는 분리해서 판단합니다.
> 교정 정책은 2026-08-17에 문서 사용 안내에 명문화했습니다 — 피그마 컴포넌트·프레임 명칭은 원문 유지, 일반 서술문만 교정.

## 우선순위 3 — `C-41` 중복 표기 정리
`C-41`은 정의가 04 요구사항 정의서에 있으나 05 개선안에도 표기되어 있습니다.
정의를 한 곳으로 모을지, 05의 것을 참조 표기로 낮출지 판단이 필요합니다.
(코드 칩은 첫 번째 정의만 수집하므로 동작에 문제는 없습니다.)

## 우선순위 4 — O-18 재검토 시점 확인
카드 정보 밀도. 화면설계 단계에서는 목업 기준을 유지하기로 이월했고, **디자인 시안 단계**에서 스펙 면 · 혜택 면 압축 범위를 결정합니다.
시안 착수 시점에 기획 · 디자인에 재확인이 필요합니다.

## 우선순위 5 — 06 테스트 케이스 수행 준비
106건 중 **프로토타입으로 지금 확인 가능한 76건**은 개발 착수 전에 수행할 수 있습니다.
나머지 30건(세션 승계 · 백오피스 연동 · 보조기술)은 구현 이후 검증 대상입니다.
수행 기록은 외부 시트 J~M열에 남기며, 이 레포의 문서는 수행 기록을 담지 않습니다.

---

## 완료

### 2026-08-17 — `C-26` 추적성 태그 보완
선택 상태 3중 단서 요건이 내용상 반영되어 있으나 01 화면설계에 코드 태그가 없던 문제.
E 영역 선택 상태 명세 · 부록 2 상태 정의 · 부록 4 접근성 명세 **3곳**에 `C-26` 칩을 부착했습니다.
코드 칩 툴팁·이동·복귀 동작까지 검증 완료.

### 2026-08-17 — 표기 교정 정책 문서화
문서 사용 안내 「표기 의미」 표에 한 줄 규칙을 추가했습니다.
> 피그마 컴포넌트 · 프레임 명칭은 원문을 그대로 유지하고, 일반 서술문만 교정합니다.

`Worning` · `LABLE` · `Abled`가 남아 있는 이유가 문서에서 설명됩니다.

### 2026-08-17 — C-nn · O-nn 건수 표기 정정
"44건 중 38건" → **"47건 중 41건"**, O-nn 설명에 결정 대기 0건 · O-18 이월 상태를 반영했습니다.

### 2026-08-17 — 1180px 경계 표기 (해당 없음으로 종결)
기존 지적은 `febb81a` 시점 문서 기준이었습니다. 현재 `index.html` 본문에는 1180px 관련 서술이 없고(CSS에만 존재),
설계 대상 화면의 브레이크포인트는 **769px 단일 기준**(C-46)으로 일관되게 기술되어 있어 **수정 대상이 아닙니다.**
두 값의 구분은 `docs/PROJECT_CONTEXT.md`와 `docs/RULES.md` 2절 8번에 명시했습니다.
