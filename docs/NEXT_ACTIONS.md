# NEXT_ACTIONS — 다음 작업

## 우선순위 1 — `C-26` 추적성 태그 보완
선택 상태 **3중 단서(보더 · 채움 · 라디오)** 요건은 내용상 반영되어 있으나 **01 화면설계(`rt-spec`)에 코드 태그가 없습니다.**
참조가 03 기능정의 · 04 요구사항 정의서 · 06 테스트 케이스에만 있습니다(실측 — `docs/REQUIREMENTS.md` 4절).
반대로 `C-41`은 정의 탭(04) 외에 05 개선안에도 표기되어 있습니다.

## 우선순위 2 — 표기 교정 정책 문서화
현재 상태 — 교정 완료: `Hisotry.back()`, `NO ACION` / 원문 유지: `Worning`, `LABLE`, `Abled`

정책 후보 (한 줄 규칙으로 "0 문서 사용 안내"에 삽입 권장):
> 피그마 컴포넌트/프레임 명칭은 원문 유지, 일반 서술문은 교정한다.

## 우선순위 3 — 1180px 경계 표기 정합
**리포트 문서 자체**의 레이아웃 기준입니다. 문서 표현은 "1180px **미만**에서 세로 배치"이나 구현은 `max-width:1180px`이므로 **1180px 자체가 세로**입니다.
표기를 구현에 맞춥니다. **breakpoint 값 자체는 변경하지 않습니다.**

> 설계 대상 화면(S-02)의 브레이크포인트 **769px**(C-46)와 혼동하지 않도록, 문서에 어느 쪽인지 명시하는 편이 좋습니다.

## 우선순위 4 — A~G 프레임 접이식 적용 여부 검토
현재 A~G는 항상 펼침입니다. 프레임 7개 × 상태 23개로 문서가 길어 접이식 전환을 검토할 만합니다.
적용 시 `<details class="fs-frame">` + `<summary class="fs-fhead">` 패턴을 씁니다(`docs/MARKUP.md` 2절).
**선택 사항이며 사용자 확정 후 진행합니다.**

## 우선순위 5 — Figma 원본 확인

### 오타 5건 (문서는 교정 / 피그마 원본은 미수정 — 허지원 책임)
| 원문 | 교정 |
|---|---|
| `Compoent` | Component |
| `Hisotry.back()` | History.back() |
| `NO ACION` | NO ACTION |
| `FOOT STICIKY` | FOOT STICKY |
| `E. 요금제 코드` | E. 요금제 목록 카드 |

### 오류 1건
`B. 타이틀` 노출 조건이 "상단 고정"으로 되어 있으나 실제는 **스크롤**입니다(A에서 복사된 것으로 판단).
문서는 스크롤로 표기했습니다. **원본 확인 필요.**

> 문서 교정과 피그마 원본 수정 여부는 분리해서 판단합니다.

## 우선순위 6 — O-18 재검토 시점 확인
카드 정보 밀도. 화면설계 단계에서는 목업 기준을 유지하기로 이월했고, **디자인 시안 단계**에서 스펙 면 · 혜택 면 압축 범위를 결정합니다.
시안 착수 시점에 기획 · 디자인에 재확인이 필요합니다.

## 우선순위 7 — 06 테스트 케이스 수행 준비
92건 중 **프로토타입으로 지금 확인 가능한 76건**은 개발 착수 전에 수행할 수 있습니다.
나머지 16건(세션 승계 · 백오피스 연동 · 보조기술)은 구현 이후 검증 대상입니다.
수행 기록은 외부 시트 J~M열에 남기며, 이 레포의 문서는 수행 기록을 담지 않습니다.
