# PROJECT_STATE — 현재 상태

## 기준점
- 기준 커밋: **`d801de9`** (2026-08-19) — **미커밋 작업 없음** (working tree clean)
- 대표 파일: `index.html` (**650KB · 4,926줄**)
- 브랜치: `main` / 원격: `origin` → **`260730planlist_0819`** — 2026-08-19 `git remote set-url`로 전환 완료. `push.cmd`가 새 저장소로 나갑니다
- 배포: https://jiwonida-dotcom.github.io/260730planlist_0819/
- 문서 계층 갱신일: 2026-08-19

> **이전 저장소 `260730planlist_3`은 더 이상 배포처가 아닙니다.** 커밋 이력은 `d801de9`까지 양쪽이 동일합니다.

## 최근 확정 (2026-08-19)
**제휴카드 일체를 S-02 밖으로 이관** — `C-62`. 진입 · 선택 · 결과 확인을 **가입신청서 작성 내용 확인(마지막 단계)**으로 옮기고, 05 개선안에 **「바」 그룹**을 신설해 `C-52` · `C-60` · `C-61`을 이관했습니다. Sticky는 **2행 · 123.4px 고정**입니다.
**NPay 네이버인증을 외부 인증 호출로 정정** — `C-63`. 원본 93p 대조 결과 시트에 입력 칸이 없고 「네이버 ID 확인」이 네이버 로그인 페이지를 호출합니다.
**C-53 세 시트 공통 스타일 가이드** — 골격 · 타입 스케일 · 세로 리듬을 하나로 고정했습니다.

`C-60`의 ① ~ ③ 3단계는 **전부 완료**되었습니다 (`docs/RESUME_C60.md` 참조 — 이관으로 내용이 대체됨).

## 2026-08-19 작업 (미커밋) — 05 개선안 정리 (최종 프로토타입 기준)

정의 66건을 **최종 프로토타입 기준으로 맞추고, 중복·이력 서술을 걷어냈습니다.**

**1. 프로토타입 불일치 3건 정정**

| 대상 | 문제 | 조치 |
|---|---|---|
| `C-38` | 「제휴카드 계산 버튼 `C-52`」를 터치 타깃 대상에 포함 — **`C-62`로 S-02 밖으로 나간 요소** | 삭제하고 **종속 입력 시트 `C-53`**으로 교체 |
| `C-49` | 원본 Alert 건수가 **4건 / 6종** 두 곳에 나뉘어 상충 | **6종 단일 서술**로 통합 |
| 시나리오 `C53b` | 「네이버 ID · 비밀번호 두 칸 입력」 — **`C-63`(외부 인증 호출)과 모순**. 프로토타입 시트에는 입력 칸이 없음 | 「입력 칸 없는 외부 인증 호출」로 정정 |

**2. 단일 출처 정리** — 같은 규칙을 여러 행이 반복하던 것을 한 곳으로 모으고 나머지는 참조만 남겼습니다.

| 규칙 | 단일 출처 | 반복을 걷어낸 곳 |
|---|---|---|
| 제휴카드 위치 이관 | `C-62` | `C-51` · `C-52` · `C-60` · `C-61` |
| 시트 요소 명세 | 01 부록 6 | `C-53` |
| 약정 팝업 노출 조건 · 버튼 귀결 | `C-50` | `C-65` |
| 터치 타깃 · 히트 영역 분리 | `C-38` | `C-66` · `C-52` |

**3. 정의문에서 개정 이력 제거** — 「26.08.18 개정 —」 「26.08.19 보강 —」 같은 **이력 서술 12건**을 걷어냈습니다. 이력의 단일 출처는 **01 변경 이력 표 + `docs/CHANGELOG.md`**입니다. 정의문에는 **확정일만** 남겼습니다(8건).

**4. 「라 화면 · UI」 24행을 5개 하위 묶음으로 재구성** — 목록 컨트롤 · 카드 · 팝업 · 접근성이 순서 없이 섞여 있던 것을 읽는 순서대로 나눴습니다.

| 묶음 | 행 | 내용 |
|---|---:|---|
| 목록 구성 | 7 | C-13 · C-21 · C-14 · C-22 · C-27 · C-39 · C-57 |
| 카드 | 7 | C-16 · C-17 · C-15 · C-37 · C-59 · C-49 · C-58 |
| 선택 · 확정 | 5 | C-18 · C-26 · C-35 · C-20 · C-51 |
| 팝업 · 레이어 | 3 | C-55 · C-50 · C-65 |
| 공통 규격 | 2 | C-38 · C-66 |

**결과** — 정의 본문 **10,491 → 9,409자(-10%)**, 요구사항은 **한 건도 삭제하지 않았습니다**(`CLAUDE.md` 3절 — 의미 삭제 금지, 표현만 개선).

검증: C-nn 정의 **66건 · 결번 0 · 중복 정의 0 · 죽은 칩 0** · 툴팁 맵 92건 · **18조합 전수 PASS** · 콘솔 오류 0 · 파서 오류 0

## 2026-08-19 작업 2 (미커밋) — `C-49` 셀프개통 불가 표기 · `EX-03` 시점 이동 · 라벨 정정

**1. `C-49` — 셀프개통 불가를 제약 라인으로 상시 표기**
청소년 요금제는 「만 19세 미만 가입 가능」만 표기하고 **셀프개통이 안 된다는 사실은 확정 시점에야** 알 수 있었습니다. 자격 제한과 같은 성격이므로 **선택 전부터** 알립니다.
- 카드에 「**셀프개통 신청 불가 — 상담사 개통으로 작성**」 라인 추가. 요금제 자체의 사실이라 **진입 개통 방식과 무관하게 상시 노출**
- 자격 제한과 함께 걸리면 **두 줄로 나란히** (P10 청소년 · P11 통화 전용)
- P11의 `limit` 문구에서 중복 표기(「데이터 미제공 — 셀프개통 신청 불가」)를 제거하고 `selfBlock`에서 자동 생성

**2. `EX-03` — 경로 전환 확인을 선택 시점 → 확정 시점으로 이동**
`STEP 10b`(선택 시)에서 **`STEP 13a`(확정 시)**로 옮겼습니다. 선택 시점에는 어떤 팝업도 뜨지 않아 **`C-49`의 「선택은 그 자리에서 완결」 원칙이 예외 없이 성립**합니다.
- 「**상담사 개통하기**」 → 개통 방식 전환 후 **확정을 이어서 진행**(CTA를 다시 누르게 하지 않음)
- 「취소」 → 셀프개통 유지 · 선택 해제 + 재선택 안내 (기존 귀결 유지)

**3. 버튼 라벨 정정** — 「상담사 개통으로 작성」이 **두 줄로 접혀** 「**상담사 개통하기**」로 변경. 실측 버튼 높이 48px 한 줄.

**05 개선안 반영** — `C-49`(셀프개통 불가 표기 규칙 + 원본 대조표 행) · `C-50`(경로 전환 = 확정 시점 · 버튼 귀결 · 라벨 길이 규칙)

> **이외 문서 미반영** — 03 기능정의의 `STEP 10b`를 **`STEP 13a`로 이동**해야 하고, 06 `TC-E-06` ~ `TC-E-08`의 전제(선택 시 팝업)와 01 E 영역 SYS 행도 갱신 대상입니다. 순차 진행 예정.

검증: 콘솔 오류 0 · 시나리오 15건 PASS · 선택 시 Alert 0건 · 확정 시 팝업 1건 · 「상담사 개통하기」 → S-03 진행 · 「취소」 → 선택 해제 확인 · **18조합 전수 PASS** · C-nn 66건 죽은 칩 0

## 2026-08-19 작업 3 (미커밋) — 03 기능정의 정리

**1. 프로토타입 정합 — 낡은 서술 4건**

| 위치 | 문제 | 조치 |
|---|---|---|
| 1 영역 구성 「Sticky 바」 | **제휴카드 계산 버튼**이 구성에 남아 있었음 — `C-62`로 S-02 밖으로 이관 | 삭제 · 「2행 고정」 + `C-62` 참조 |
| `STEP 10` Sticky 갱신 | **「제휴카드 할인금액(예상)은 별도 행에 분리 표기」** — 이 화면에 없는 행 | 삭제 · 「2행 고정 · 높이 불변」으로 교체 |
| `STEP 10b` 경로 제약 판정 | **선택 시점** 판정 — `EX-03`가 확정 시점으로 이동했으므로 위치 불일치 | **`STEP 13a`로 이동** (13 사용 여부 재검증 직후) |
| `EX-03` 예외 행 | 발생 조건 `(STEP 10b)` · 버튼 라벨 「상담사 개통으로 작성」 | `STEP 13a` · 「**상담사 개통하기**」 |

**2. 누락 반영 3건** — `C-63`(NPay 외부 인증 호출) · `C-64`(입력 서식 자동 적용)를 `STEP 13c`에, `C-65`(약정 팝업 구성) 참조를 `STEP 13b`에 추가했습니다. 셋 다 03에 **참조 0건**이었습니다.

**3. `C-49` 셀프개통 불가 표기 반영 4곳** — `STEP 6` 카드 렌더 · 1 영역 구성 카드 행 · 3 노출 데이터 제약 라인 행 · 5 데이터 연동(「셀프개통 가능 여부」 추가, 자격 검증은 S-05 · 경로 판정은 `STEP 13a`로 구분).

**4. 소제목 정정** — 4 예외 처리의 「처리 규칙」 · 「Alert을 두지 않는 근거」가 **`EX-02` 전용**인데 일반 규칙처럼 읽혀 `EX-02` 태그를 붙였습니다.

검증 시퀀스는 이제 **1 → 1b → 2 → 3 → 4 → 4b → 5 → 6 → 7 → 8 → 9 → 10 → 11 → 12 → 13 → 13a → 13b → 13c → 14** 입니다.

> **다음 순번** — 06 테스트 케이스(`TC-E-06`~`TC-E-08` 전제가 「선택 시 팝업」) · 01 화면설계(E 영역 SYS 행 · F 영역 목업).

검증: C-nn 정의 66건 · 죽은 칩 0 · **18조합 전수 PASS** · 시나리오 15건 PASS · 콘솔 오류 0

## 확정 / 이월
- **C-01~C-66**: 확정 66건 (60건 05 개선안 / 6건 04 요구사항 정의서) — **전건 index.html 정의 반영**
- 그중 **C-52 · C-60 · C-61 · C-62** 4건은 **S-02 범위 밖**(「바」 그룹) — 01 화면설계 · 06 테스트 케이스 적용 대상 아님
- **결정 대기 0건** — O-17 · O-19 ~ O-27 해소, **O-18 이월**
- **P1~P5**: 전건 해소
- 테스트 케이스 **106건** (06 탭)

## 물량 (실측 · `rt-spec`)
| 항목 | 수 |
|---|---:|
| 프레임 `.fs-frame` | 11 |
| 상태 블록 `.fs-state` | **24** |
| 요소 명세 행 `.fs-row` | **91** |
| 콜아웃 `.fs-cal` | **70** |
| 영역 마커 `.fs-am` | 24 |
| 접이식 `<details>` | 3 |

## 최근 진행 (2026-08-14)
| 커밋 | 내용 |
|---|---|
| `aa654f0` | 06 수행 기록 시트 링크 연결 |
| `3463775` | **06 테스트 케이스 탭 신설 — 92건** |
| `dbef182` · `10eac66` | 01 호버 · 포커스 전수 정비 |
| `6de4794` | **O-19 해소 — 비교 시트 상세 확정(C-47) · 결정 대기 0건** |
| `49ccea5` | 01 변경 이력 헤더 보정 |
| `5c81492` | **O-17 · O-20 해소(C-45 · C-46), O-18 이월** |
| `97d90b8` | 01 D PC 필터 패널 렌더 오류 수정 · 현재 영역 표시 |
| `1d182ae` · `15e93a0` | **코드 칩 호버 상세 + 정의 위치 이동** |

## 2026-08-17 작업 (미커밋)
`index.html` 수정 — diff 6청크, +4줄
1. **`C-26` 추적성 태그 부착** — E 영역 선택 상태 명세 · 부록 2 상태 정의 · 부록 4 접근성 명세 3곳
2. **표기 교정 정책 명문화** — 문서 사용 안내 「표기 의미」 표에 행 추가
3. **건수 표기 정정** — C-nn "44건 중 38건" → "47건 중 41건", O-nn 설명에 결정 대기 0건 · O-18 이월 반영
4. 변경 이력 행 1건 + E 프레임 HISTORY 1행 추가

검증: 18조합 전수 **PASS** · 이슈 0 (`docs/VALIDATION.md` 5절 V-2026-08-17-01)

## 2026-08-17 작업 3 (미커밋) — 원본 누락 전건 반영
`index.html` 4개 탭 수정. **C-48 ~ C-59 확정 12건** + C-28 · C-33 · C-40 문구 보정.
- 05 개선안 — 정의 12건 · 다이어그램에 사은품 단계
- 04 요구사항 — C-28 보정 · KPI 59건
- 03 기능정의 — STEP 1b · 4b · 10b · 13b · 13c · 예외 EX-03 · EX-04 · 노출 데이터 · 데이터 연동
- 01 화면설계 — SYS 행 7개 · **부록 6 「팝업 · 분기 정의」 신설**
- 06 테스트 케이스 — 92 → **106건**

**마크업 규약 준수** — 콜아웃 재정렬 없음(신규 요건은 전부 SYS 행), A~G PAGE CODE 불변(팝업은 부록 6으로 분리).
검증: 18조합 전수 **PASS** · 이슈 0 (V-2026-08-17-02)

## 2026-08-17 작업 4 (미커밋) — 프로토타입 · 목업 구성
명세만 있던 신규 정의를 **실제 동작과 SCREEN 목업으로 구현**.
- **02 프로토타입** — 제약 라인 · 속도 안내 레이어 · Sticky 금액 명세 · 제휴카드 계산 · 약정 동의 · 상담사 개통 전환 · 진입 안내 · 단독 노출 모드
- **01 화면설계** — E 카드 상태 콜아웃 10 → 12 재정렬, **자격 제한 요금제 상태 신설**, F 선택 상태 콜아웃 2 → 5 재정렬
- **EX-03 정정** — 셀프개통 불가 요금제도 목록 노출, 취소 시 선택 해제 + 재선택 안내
- **제휴카드 표기 정정** — 원 서비스 화면 기준. "제휴카드 할인포함 월 납부 금액" 라벨 전환을 **삭제**하고, 할인은 **예상 금액으로 분리 표기**(합산하지 않음). 계산 버튼은 라벨 옆, 선택 후 버튼 라벨만 "변경"으로 전환

검증: 18조합 PASS · 콜아웃 불일치 0 · 신규 칩 12종 동작 · 프로모션 문구 미등록 시 높이 유지 확인

## 2026-08-18 작업 (미커밋) — C-60 제휴카드 선택 화면 개선 ①
사용자가 원 서비스 제휴카드 선택 팝업 스크린샷을 첨부하고 개선안을 요청. **사용자 확정 3건** — 카드 목록 전개 · 팝업 하단 즉시 반영 · 확인 Alert 제거.

- **02 프로토타입** — 셀렉트·캐러셀을 해체하고 `role=radiogroup` 목록 4행(카드 3 + 선택 안 함)으로 재편. `CARDS` 데이터를 `tiers[]` 구조로 분리해 **실적 구간별 할인액**을 카드 안에 병기. 팝업 하단에 **제휴카드 할인금액(예상) + 요금제 할인 적용 시 월 금액** 2행을 두어 선택 즉시 반영. 유의사항 접이식 분리. `paintCardPick()` 신설(부분 갱신 — `render()` 재호출 시 `.alert` pop 애니메이션 재생 문제 해소)
- **버그 수정 2건** — `.alert button`의 CTA 그라데이션이 카드행에 상속되던 문제(`.alert button.ccitem` 스코프), 선택 시 목록 높이 1px 변동(보더 증가분만큼 padding 축소)
- **미반영** — 01 화면설계 · 03 · 04 · 05 · 06. **C-60 정의 행이 없어 코드 칩이 죽어 있음**

검증: Chromium 오류 0 · `node --check` PASS · 동작 8항목 PASS · PC 1400/768/420 넘침 0. **18조합 검증은 ②③ 완료 후 일괄 실행**

## 2026-08-18 작업 2 (미커밋) — C-60 (2) 선택 화면 형식화 · Sticky 정리 · 시나리오 확장
사용자 요청 3건 반영. **02 프로토타입만 변경**.
1. **제휴카드 레이어 고도화** — 타이틀 「제휴카드 할인 계산」 → **「제휴카드 선택」**, 헤더(타이틀 + 44px 닫기) 신설, 리드 문구를 스크롤 영역 밖으로 분리, 선택 행에 **「선택됨」 텍스트 라벨**(색상 비의존 단서 · C-26), CTA 「확인」 → 「선택 완료」
2. **Sticky 금액 행 2개 제거** — 「제휴카드 할인금액(예상)」 행(같은 예상 할인액이 레이어와 두 곳에 있어 월 납부 금액이 할인 반영값으로 오인될 여지)과 「부가세포함 월 납부 금액」 행(1행의 「월 N원」과 같은 금액 중복)을 모두 삭제. **부가세포함 라벨은 1행 금액 앞으로 이동**해 의미를 유지. 남은 것은 진입 버튼(「제휴카드 할인 확인」 / 「변경」)뿐이며 히트 영역은 `::after`로 44px 확보(C-38). **Sticky = 2행 유지(C-52)**
3. **시나리오 시뮬레이터 3건 → 11건** — 「조건」 태그 신설, C-49 · C-50 · C-55 · C-57 · C-58 · C-60 · EX-03 · EX-04 추가

검증: 콘솔 오류 0 · `node --check` PASS · 시나리오 11건 전건 실행 · 목록 높이 368.5625px 불변 · 6폭 넘침 0

> **③ 단계 영향** — Sticky 구성이 **「요금제명 + 부가세포함 월 금액」 1행 + 「제휴카드 진입 버튼」 1행**으로 바뀌었습니다. 01 화면설계 F 영역 명세 · 05 `C-51` · `C-52` 정의 · 06 `TC-U-45` · `TC-U-46`이 갱신 대상입니다.
> 01 F 목업은 아직 구 구조(`.sum .amt` 4곳 · `.ccbtn` 2곳)를 쓰고 있어 **관련 CSS는 삭제하지 않았습니다** — ② 단계에서 목업과 함께 정리합니다.

## 2026-08-18 작업 3 (미커밋) — C-60 (3) 정보 위계 강화
사용자 확정 — **방향: 정보 위계 강화 / 범위: 제휴카드 레이어만**.
- **카드 행 3단 위계** — 카드명(주) → 최대 할인액(강조·단독 행) → 실적 구간(부속·배경 박스)
- **결과 패널 `.ccsum` 신설** — 예상 할인금액 · 할인 적용 시 월 금액 · 주석을 한 패널로 묶어 목록과 분리. 할인액 승격
- **리드 1문장 축약** · 유의사항 구분선 · CTA 상단 여백
- **버그 1건 수정** — 「선택됨」 라벨이 이름 행에 끼어들며 목록 높이가 385.9 → 410.8px로 변하던 문제. `visibility`로 자리를 고정하고, 최대 할인액을 단독 행으로 되돌려 접힘 구조 자체를 제거

> **목록 높이 기준값이 `368.5625px` → `420.671875px`로 바뀌었습니다.** ② 목업·명세에 이 값을 씁니다.

검증: 콘솔 오류 0 · 시나리오 11건 PASS · 4회 선택 높이 불변 · 6폭 넘침 0 · 모바일 430px 스크롤/CTA 상시 노출

## 2026-08-18 작업 4 (미커밋) — C-60 (4) 유의사항 상시 노출 · 레이어 금액 영역 삭제 · 접근성
사용자 확정 3건.
1. **유의사항 상시 노출** — 접이식(`details`) 제거, `<section>` + `<h3>`. 항목 3 → 4건(월 납부 금액 미합산 주석 흡수). 조건을 접힘 뒤에 두면 선택 전에 읽히지 않기 때문
2. **레이어 안 금액 계산 영역 전량 삭제** — `.ccsum` · `.ccfoot` · `.cccap`. 결과 확인은 Sticky 한 곳으로 일원화
3. **Sticky 재구성** — 미선택: 「제휴카드 할인 선택」 버튼만 / 선택: 「카드명 · − 할인금액」 행 + 「제휴카드 할인 변경」 버튼
4. **접근성 보강** — `aria-modal` · `aria-labelledby` · 타이틀 `h2` · `ccKey()` 신설(Esc 닫기 · 방향키/Home/End 라디오 이동 · Tab 포커스 트랩) · 열림/닫힘 포커스 이동 · roving tabindex · `:focus-visible` 링 · 유의사항 대비 상향 · Sticky 버튼 실제 44px

검증: 콘솔 오류 0 · 시나리오 11건 PASS · 6폭 넘침 0 · 목록 높이 420.671875px 불변 · **44px 미만 버튼 0건** · **명암비 8항목 전건 AA 통과(최저 4.66:1)** · 키보드 전건 동작

> **③ 단계 영향** — Sticky 행 수가 **미선택 2행 / 선택 3행**이 되었습니다. `C-51` · `C-52` 정의, 01 F 영역 명세, 06 `TC-U-45` · `TC-U-46` 갱신 대상입니다.
> 레이어에서 「선택 즉시 하단 반영」이 사라졌으므로, 05 `C-60` 정의를 쓸 때 **결과 확인 경로를 'Sticky 단일'**로 기술합니다.

## 2026-08-18 작업 5 (미커밋) — ③ 1차 : 05 개선안 개정
`CLAUDE.md` 2절 ③ 단계의 **1차 = 05 개선안**. 정의의 원본이 05이고 코드 칩도 04·05에서만 수집되므로 여기부터 시작.

**신설 2건**
- **`C-60` 제휴카드 선택 화면** — 목록 전개 · 실적 구간 병기 · 3단 위계 · 「선택됨」 텍스트 라벨 · 확인 Alert 없음 · **레이어 내 금액 영역 없음** · 유의사항 상시 노출 · 접근성 요건(모달 선언 · 포커스 이동/복귀/순환 · Esc · 방향키 · 탭 스톱 1개) · 선택 전후 높이 불변
- **`C-61` 제휴카드 영역 노출 조건** — 사용 카드 0건 / 전용 요금제 진입 시 미노출. **원본 25.03.05 이관** (사유 원본 미기재 · 확인 필요)

**개정 · 보강 6건** — `C-48`(사은품 3분류 · 툴팁 · 전용 진입 문구 치환) · `C-49`(원본 Alert 5종 분배 원칙) · `C-51`(Sticky 중복 행 제거 · 카드명+할인 행) · `C-52`(표기 원칙만으로 축소) · `C-53`(시트 3종 입력 조건 · 취소/닫기 표준화 판단 필요) · `C-55`(적용 범위) · `C-57`(대상 경로)

**→ 코드 칩 61건 전건 생존 · 죽은 칩 0건.** C-60 툴팁·이동 동작 확인 완료.

검증: **18조합(6폭 × 3구성) 전수 PASS** · 콜아웃/마커 잘림 0 · 예상 밖 오버레이 0 · 콘솔 오류 0 · 6개 탭 전환 정상

> **다음 차수(2026-08-19 갱신)** — `04 요구사항 정의서` KPI **63건** · 정의 위치 **"63건 중 57건"** · `C-38` 서술 정리 → **작성 내용 확인 화면 인계 정리**. `01 화면설계` 제휴카드 목업은 **S-02 범위 밖이라 작성하지 않습니다**(C-62)

## 2026-08-18 작업 6 (미커밋) — `C-53` 요금제 종속 입력 시트 재구성 (① + ② + ③ 일부)
**기존 기능 · 화면 재구성** — 망내 1회선 번호지정 · NPay 인증 · 쿠팡 회원정보.

**① 02 프로토타입 — 신설**
- `DEP` 데이터 + `depHtml()` · `paintDep()` · `depSubmit/depSkip/depClose/depInput/depBlur/depKey` 신설
- 대상 요금제 지정(샘플) — `P03` 망내 1회선 / `P02` NPay / `P04` 쿠팡. `confirmPlan()` STEP 13c 훅
- 검증 2층 분리 — **`depFormat()` 형식**(CTA 활성 조건 · 포커스 아웃) / **`depVerify()` 조회**(제출 시점). 조회로만 알 수 있는 값으로 CTA를 잠그지 않는다
- 버튼 귀결 — 확인/다음/인증 = 입력하고 진행 · **「나중에 입력」** = 건너뛰고 진행(건너뛸 수 있는 시트만) · 닫기(X·딤) = 진행 중단 후 목록 복귀, 선택 유지
- 접근성 — 대화상자 선언 · 첫 입력 칸 포커스 · 닫을 때 CTA 복귀 · Tab 순환 · Esc · Enter = CTA · 오류는 아이콘+문구+보더(색상 비의존) · `aria-invalid` + `aria-describedby` + `role=alert`
- 시나리오 시뮬레이터 **11 → 14건** — `C-53`을 **시트 3종으로 분리**(`C53a` 망내 1회선 · `C53b` NPay · `C53c` 쿠팡). 한 케이스로 묶으면 나머지 두 시트를 시뮬레이터에서 확인할 수 없다

**② 01 화면설계 — 부록 6에 「요금제 종속 입력 시트 — 요소 명세」 신설**
입력 항목 / 형식·검증 / 안내 3열 표 · 버튼 구성과 귀결 표 · 상태·접근성 8항목. F 영역 SYS 행에 귀결 요약 추가. **콜아웃 재정렬 없음**(부록은 콜아웃 미사용)

**③ 05 `C-53` 개정 · 03 STEP 13c 분해** — 「표준화 판단 필요」를 **확정으로 대체**했으므로 `O-28` 등록은 불필요해졌다

검증: 콘솔 오류 0 · 시나리오 12건 전건 PASS · 시트 3종 동작(형식/조회 오류 · 건너뛰기 · Esc · 재진입) PASS · **18조합 전수 PASS** · C-nn 61건 죽은 칩 0 · 제휴카드 레이어 회귀 PASS

## 2026-08-18 작업 7 (미커밋) — FOOT STICKY 축소 (`C-51` 개정)

**문제** — 실측(모바일 394 · 뷰포트 940)

| 상태 | Sticky | 목록 뷰포트 |
|---|---:|---:|
| 미선택 | 123px | 623px |
| 요금제 선택 | 184px | 562px |
| + 제휴카드 선택 | **227px** | **520px** |

요금제를 고르는 순간 Sticky가 **85% 커지고** 목록이 **103px(-17%)** 줄어듭니다. 사용자는 고른 뒤에도 비교하려고 계속 탐색하는데, **탐색이 가장 필요한 순간에 탐색 영역이 가장 좁아집니다.** 직전 커밋 기준 217px이었으므로 이번 변경이 만든 문제가 아니라 원본부터의 **구조적 문제**입니다.

**대안 실측**

| 안 | Sticky | 목록 | 회복 |
|---|---:|---:|---:|
| 현재 | 227px | 520px | — |
| A 1행 병합(44px 버튼) | 184px | 562px | +42px |
| **B 컴팩트 행(30px · 히트 44px)** | **169px** | **577px** | **+57px** |
| C 제휴카드 Sticky 밖 | 123px | 623px | +103px |

**채택 — B + 스크롤 축약** (사용자 확정)
- 카드명 · 예상 할인액 · 진입을 **한 줄로 병합**(`.sum .ccrow`). 시각 30px, `::after`로 **터치 타깃 44px** 확보
- **스크롤 축약** — 아래로 훑는 동안 제휴카드 행만 접어 **131px**. 멈추거나 위로 올리면 펼침. 상단 48px 이내에서는 항상 펼침. 8px 이상 움직였을 때만 상태 전환(흔들림 방지)
- 요금제명 · 금액 · CTA는 **항상 유지** — 확정 판단에 계속 필요
- **포커스가 Sticky에 들어오면 즉시 펼침** — 접힘 상태는 `visibility:hidden`이라 탭 순서에서 빠지므로 키보드 접근을 막지 않기 위함
- `prefers-reduced-motion`에서 전환 제거

> **한계 — 카드 2장 완전 노출은 아직 불가.** 카드 높이가 **혜택 있음 379px · 없음 324px**, 간격 12px이라 목록 623px에 2장을 넣으려면 카드가 **305px 이하**여야 합니다. Sticky 축소만으로는 **걸침 1 → 2~3장**(스크롤 어포던스 확보)까지가 한계이며, 2장 완전 노출은 `O-18` 카드 압축과 함께 가야 합니다.

검증: 콘솔 오류 0 · 18조합 전수 PASS · 시나리오 14건 PASS · 제휴카드 레이어 회귀 PASS(포커스 복귀 셀렉터 `.ccbtn` → `.sum .ccrow` 수정) · 히트 영역 44px 확인 · 스크롤 축약/복귀/포커스 진입 확인

> **③ 영향** — 01 F 영역 명세는 갱신 완료. **F 영역 SCREEN 목업은 아직 구 구조**(`.sum .amt` · `.ccbtn`)이므로 ② 단계에서 새 구조로 교체해야 합니다.

## 2026-08-18 작업 8 (미커밋) — 프로토타입 코드 칩 호버
리포트 탭에서만 되던 **코드 칩 툴팁을 02 프로토타입에서도** 쓸 수 있게 했습니다.

- `fsTipMark(root)` 분리 — 런타임에 그려지는 영역도 재마킹 가능. 이벤트는 `document` 위임이라 그대로 동작
- `fsTipWrap(text)` 신설 — 이스케이프된 문자열의 `C-nn` · `EX-nn` · `O-nn`을 `span.cid`로 감쌈(EX는 `.x`, O는 `.d`)
- 적용 — **시나리오 목록**(제목·설명) · **검증 로그**(STEP/코드·메시지·헤더) · **기기 배지**(`C-03`)
- 시나리오 목록은 툴팁 모듈보다 먼저 그려지므로 `fsTipInit`에서 `initScn()`을 한 번 다시 호출

> **버그 1건 수정** — `fsTipInit` 말미의 `return n;`이 남아 있어 **"n is not defined"** 오류로 툴팁 초기화가 중간에 끊기고 있었습니다. 마킹 루프를 함수로 빼면서 생긴 잔재입니다.

검증: 콘솔 오류 0 · 배지 1건 · 시나리오 목록 13건 마킹 · 호버 시 정의 노출 · 클릭 시 `rt-plan` 이동 · 18조합 전수 PASS

## 현재 미처리
0. **`C-60` ② 화면설계 · ③ 이외 문서 반영 ★** — `docs/RESUME_C60.md`
0. **C-55 · C-50 카피 확정** — 법적 고지·약정 조건 문구가 가안 상태
1. A~G 프레임 접이식 적용 여부 (선택 사항)
2. Figma 원본 오타 5건 · 오류 1건 미조치
3. `C-41` 정의 탭(04) 외 05 중복 표기 정리
4. O-18 — 디자인 시안 단계에서 재검토
5. 06 테스트 케이스 76건 수행 (프로토타입 확인 가능분)

> 1180px 경계 표기 건은 **해당 없음으로 종결**했습니다 — `febb81a` 시점 지적이며 현재 본문에 해당 서술이 없습니다.

## 문서 구조 상태
- `CLAUDE.md` / `HANDOFF.md` / `README.md` / `MIGRATION.md` + `docs/` 12종, **레포 루트 배치**
- `docs/MARKUP.md` — 마크업 규약 분리
- `docs/REQUIREMENTS.md` 3절 — **C-01~C-47 전문 사본** (원본은 `index.html`, 불일치 시 HTML 우선)
- `.gitignore` — `_to_delete/` · `*.zip` · 임시 파일 제외
- **`docs/ORIGIN_MO9_0301.md` — 원본 v27.6 요금제 구간 slide 75~93 전문 (신설 2026-08-18).** 정의·문구·요금제 코드·CMS 경로·팝업 전문 + `docs/origin-slides/` 슬라이드 이미지 19장. **원본 pptx를 다시 열 필요가 없습니다.**
- `docs/ORIGIN_GAP.md` — 원본 v27.6 대조 결과 및 설계 판단 기록. **C-48~C-59 전건 확정 · 반영 완료**

> **중요** — 2026-08-17 갱신 이전 문서는 `febb81a`(5탭 · C-01~C-44 · 결정 대기 3건) 기준이었습니다. 그 수치를 인용한 자료가 있으면 폐기하세요.
