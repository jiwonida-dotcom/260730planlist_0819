# OPEN_QUESTIONS — 외부 확인 대기

> 2026-08-20 신설. **프로젝트 밖에 답이 있는 항목의 단일 창구**입니다.
> 종전에는 같은 항목이 `HANDOFF` 6절 · `NEXT_ACTIONS` 우선순위 0·2 · `PROJECT_STATE` 미처리
> 세 곳에 흩어져 있어 **한 번에 물어볼 수 없었고**, 그래서 몇 주째 이동만 하고 있었습니다.
>
> 규칙 — 확인이 필요한 것을 발견하면 **여기에 등록하고 다른 문서에는 링크만** 둡니다.
> 답이 오면 이 문서에서 「해소」로 옮기고 반영처를 적습니다.

## 대기 중

### Q-01 · 카피 확정 (법무 · 기획) ★★
| | |
|---|---|
| 무엇 | `C-55` 진입 안내 팝업 — 정보통신망법 조항 문구 / `C-50` · `C-65` 약정 안내 — 반환금 조건 문구 |
| 현재 | **가안 문구**로 프로토타입 · 도면 · 명세에 들어가 있습니다 |
| 차단 대상 | 팝업 목업 확정(`NEXT_ACTIONS` 3) · 디자인 시안 착수 |
| 필요한 답 | 법무 · 기획 확정 문구 원문 |
| 비고 | 원문을 받으면 프로토타입 `AGREE` · `alertHtml()` 한 곳만 고치면 도면까지 함께 반영됩니다(H 프레임이 같은 렌더 함수를 씁니다) |

### Q-02 · 원본 반환금 비율 부호 (기획 · 원본 작성자) ★
| | |
|---|---|
| 무엇 | 원본 87p · 88p 「17~20개월 **−20%** / 21~24개월 **−40%**」의 음수 부호 |
| 현재 | 원본 표기 그대로 옮겨 두었습니다 (`C-65` 반환금 비율 표) |
| 차단 대상 | `C-65` 약정 안내 팝업 문구 확정 |
| 필요한 답 | 부호가 의도인가, 원본 오기인가 |

### Q-03 · 피그마 원본 오타 5건 · 오류 1건 (허지원 책임)
문서는 **교정 완료**, 피그마 원본은 미수정입니다. 교정 정책 — 컴포넌트 · 프레임 명칭은 원문 유지, 일반 서술문만 교정.

| 원문 | 교정 |
|---|---|
| `Compoent` | Component |
| `Hisotry.back()` | History.back() |
| `NO ACION` | NO ACTION |
| `FOOT STICIKY` | FOOT STICKY |
| `E. 요금제 코드` | E. 요금제 목록 카드 |

**오류 1건** — `B. 타이틀` 노출 조건이 「상단 고정」으로 되어 있으나 실제는 **스크롤**입니다
(A 에서 복사된 것으로 판단). 문서는 스크롤로 표기했습니다. **원본 확인 필요.**

### Q-04 · `C-61` 노출 조건의 사유 (원본 작성자)
| | |
|---|---|
| 무엇 | 제휴카드 영역 미노출 조건(사용 카드 0건 / 전용 요금제 진입)의 **사유가 원본에 없습니다** |
| 현재 | 원본 25.03.05 정의를 그대로 이관하고 「확인 필요」로 표기 |
| 차단 대상 | 작성 내용 확인 화면 인계 시 근거 설명 |

### Q-05 · `O-18` 카드 정보 밀도 (기획 · 디자인)
| | |
|---|---|
| 무엇 | 카드 높이 압축으로 첫 화면 2장 노출을 만들 것인가, 정보량을 어디까지 줄일 것인가 |
| 현재 | **이월** — 화면설계는 목업 단계 기준 유지 |
| 차단 대상 | 없음 (디자인 시안 단계 과제) |
| 비고 | `C-62`(제휴카드 이관)로 하단 고정 영역이 2행으로 줄어 악화는 막았습니다. 남은 한계는 카드 자체 높이입니다 |

### Q-06 · A~H 프레임 접이식 적용 여부 (허지원 책임)
| | |
|---|---|
| 무엇 | 현재 프레임은 항상 펼침입니다. 프레임 10개 · 상태 30여 개로 문서가 길어 접이식 전환 검토 대상 |
| 현재 | 미적용 |
| 필요한 답 | 적용 여부 (적용 시 `<details class="fs-frame">` + `<summary class="fs-fhead">` — `MARKUP.md` 2절) |
| 비고 | **선택 사항**입니다. 기능·정합에 영향 없습니다 |

### Q-07 · 목업을 렌더 함수에서 파생시킬 범위 (허지원 책임)
| | |
|---|---|
| 무엇 | `H` 프레임처럼 A~G 목업도 프로토타입 렌더 함수(`cardHtml` · `stickyHtml` 등)에서 파생시킬 것인가 |
| 현재 | A~G 는 직접 쓴 목업이라 프로토타입과 갈라집니다 — F 영역이 구 구조로 남아 관련 CSS 를 지우지 못했고, 2026-08-19 에 정합 작업을 따로 해야 했습니다 |
| 필요한 답 | 전면 전환 / **F 영역 시범** / 현행 유지 |
| 비고 | 근거와 예상 효과는 `docs/PROCESS_REVIEW.md` S-3. 「디자인 확정안이 아닌 화면설계」 성격(`RULES.md` 1절)과 충돌하지 않는지 함께 확인이 필요합니다 |

## 해소

*(아직 없음 — 답이 온 항목을 여기로 옮기고 반영처를 적습니다)*

## 기록 형식

```text
### Q-nn · 제목 (담당)
| 무엇 | 확인이 필요한 사실 |
| 현재 | 지금 문서·프로토타입에 들어가 있는 상태 |
| 차단 대상 | 이 답이 없어서 못 하는 일 |
| 필요한 답 | 무엇을 받아야 끝나는가 |
```
