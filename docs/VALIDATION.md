# VALIDATION — 검증 기준

## 0. 실행 환경
Chromium은 `/opt/pw-browsers/chromium`에 있습니다. Playwright 브라우저 별도 설치가 불필요합니다.

## 1. 기본 검증 매트릭스
**6개 폭 × 3개 구성 = 18조합**

### Viewport
1400 / 1280 / 1180 / 1000 / 768 / 420

### 구성
문서정보 / 화면설계 / 부록

## 2. 반드시 확인

| # | 항목 | 판정 기준 |
|---|---|---|
| 1 | 콜아웃 · 마커 잘림 | **0건**. 상위 요소를 거슬러 올라가며 `overflow != visible`인 조상과 경계를 비교. **단, 가로 스크롤 가능한 조상(`scrollWidth > clientWidth`)은 제외** — 스크롤로 도달 가능하므로 잘림이 아님 |
| 2 | 예상 밖 오버레이 | 뷰포트 **35% 이상**을 덮는 오버레이 0건. 비교 시트 딤 `.fs-veil`은 목업 내부이므로 정상 |
| 3 | 1180px 경계 | 리포트 문서의 좌우 ↔ 세로 레이아웃 전환 동작 |
| 4 | JS 오류 | 0건 |
| 5 | 태그 정합성 | `html.parser` 기준 0건 |
| 6 | 리포트 탭 | **6개 탭** 전환 동작 (`spec` · `proto` · `func` · `req` · `plan` · `tc`) |
| 7 | 프로토타입 | 카드 선택 동작 |
| 8 | 앵커 이동 | 프레임 상단이 툴바 하단 **+18px**(768 이하 **+22px**)에 정지 |
| 9 | 접이식 | 접기/펼치기 왕복, **접힌 프레임으로 앵커 이동 시 자동 펼침** |
| 10 | 구성 전환 sticky | 문서정보 ↔ 화면설계 ↔ 부록 전환 시 툴바가 화면에서 움직이지 않을 것. **툴바를 지나 스크롤한 상태 / 아직 위에 있는 상태 두 경우 모두** |
| 11 | 번호 정합 | 콜아웃 번호 ↔ 명세 행 번호 1:1 (SYS 행 제외) |
| 12 | **코드 칩** | `C-nn` · `O-nn` 호버 시 정의 툴팁 노출, 키보드 Tab 포커스에서도 동일. 클릭(Enter) 시 정의 탭·구성으로 이동하고 되돌아오기 동작 |
| 13 | **변경 이력 헤더** | 건수 · 최근 일자가 실제 행과 일치 (`fsLogHead`) |
| 14 | **현재 영역 표시** | 스크롤에 따라 툴바의 현재 영역 표시가 갱신 (`fsSpy`) |

## 3. 검증 원칙
**요소 단위 스크린샷만 믿지 않습니다.**
과거 페이지 전면 오버레이 사고를 요소 스크린샷으로 놓쳤습니다. **페이지 전체 스크린샷**을 함께 확인합니다.

새 변경이 있으면 **이전 PASS 결과를 자동 승계하지 않습니다.**

## 4. 06 테스트 케이스 탭과의 관계
`rt-tc` 탭의 **92건**은 **설계 대상 화면(S-02)의 기능 검증**입니다.
이 문서의 매트릭스는 **리포트 문서 자체의 렌더링 검증**입니다. 둘은 별개이며 둘 다 수행합니다.

| | 대상 | 수행 시점 |
|---|---|---|
| 이 문서 18조합 | `index.html` 렌더링 | 문서 수정 직후 |
| 06 탭 92건 | S-02 화면 동작 | 프로토타입 확인 76건 / 나머지는 개발 착수 후 |

06 탭 케이스의 **정의는 `index.html`**, **수행 기록은 외부 시트**가 담당합니다. 명세가 바뀌면 탭을 먼저 고치고 시트 A~I열을 갱신하며, 수행 기록(J~M열)은 덮어쓰지 않습니다.

## 5. 검증 결과 기록 형식
```text
Validation ID:
Commit:
Date:
Viewport:
Configuration:
Result:
Issues:
Evidence:
```
